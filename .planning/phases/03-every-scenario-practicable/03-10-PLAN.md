---
phase: 03-every-scenario-practicable
plan: 10
type: execute
wave: 9
depends_on: ["03-09"]
files_modified:
  - src/lib/content/scenario-speaking.ts
autonomous: true
requirements: [CONT-01]

must_haves:
  truths:
    - "All thirty scenarios that declare speaking offer a rehearsal task written for that scenario"
    - "Every one of the fifty-two declared pairs has its own exercise"
    - "No two scenarios share a rehearsal task or a move list"
    - "The coverage registry reports 52 of 52 because the banks hold 52, not because anything says so"
  artifacts:
    - path: "src/lib/content/scenario-speaking.ts"
      provides: "The last sixteen rehearsal tasks, closing CONT-01"
      contains: "native/pronunciation"
  key_links:
    - from: "src/lib/content/scenario-speaking.ts"
      to: "src/lib/scenario-coverage.ts"
      via: "the 52/52 reading is derived from the four exercise banks"
      pattern: "getScenarioSpeaking"
---

<objective>
Close CONT-01: the last sixteen speaking pairs — all of Travel & Errands, all of Practical Life and
all of Sounding Native — get rehearsal tasks written for them, and the coverage registry reads 52 of
52 because the banks hold fifty-two exercises.

Purpose: these sixteen include every scenario that was previously handed a per-world generic set, so
this is where the last of D-01's rejected outcome disappears. Two scenarios can no longer return the
same exercise, and the harness asserts it rather than the author remembering it.

Boundary, restated: Sounding Native's deeper native-level treatment is CONT-04 and belongs to Phase
4. These five are authored at their declared levels and to the same shape as the other twenty-five.

Output: thirty of thirty speaking pairs, and CONT-01 closed.

**What the learner gains if execution stops right after this plan:** every scenario in all six worlds
is practicable in every skill it declares, and every scenario's phrases and vocabulary feed the
review queue. That is the phase, minus the gate that proves it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-every-scenario-practicable/03-CONTEXT.md
@.planning/phases/03-every-scenario-practicable/03-09-SUMMARY.md
@AGENTS.md

Source files this plan reads or edits:
@src/lib/curriculum.ts
@src/lib/content/phrases.ts
@src/lib/content/scenario-speaking.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Travel &amp; Errands and Practical Life — eleven rehearsal tasks</name>
  <files>src/lib/content/scenario-speaking.ts</files>
  <read_first>
    `src/lib/curriculum.ts` lines 89-102 and 116-128 (these eleven scenarios, their blurbs and their
    levels — four of them are A2), `src/lib/content/phrases.ts` for the six phrases each already
    carries, so the moves give those phrases somewhere to be used, and plan 09's tasks as the anchor
    for shape and length.
  </read_first>
  <action>
    Author eleven rehearsal tasks in the shape plan 09 established: a setup, exactly three ordered
    moves, a success check, all fields required, around sixty words each, at the scenario's own CEFR
    level.

    Travel & Errands: `travel/airport` (A2), `travel/hotel` (B1), `travel/restaurant` (A2),
    `travel/directions` (A2), `travel/emergencies` (B1), `travel/shopping` (B1).
    Practical Life: `practical/phone-calls` (B1), `practical/tech-support` (B1),
    `practical/housing` (B2), `practical/banking` (B1), `practical/appointments` (A2).

    Four of these are A2 and are where a task most easily drifts upward: at A2 the three moves must be
    doable in short, simple sentences under real pressure at a counter or a desk. `travel/emergencies`
    and `practical/phone-calls` share a difficulty worth writing into their moves — the learner cannot
    control the pace and cannot see the other person — so one move in each should be about getting a
    repeat or a confirmation rather than about producing more language.

    Every rule from plan 09 applies unchanged, including that nothing may be byte-identical to another
    scenario's task or move list. These eleven are precisely the scenarios that used to share a
    per-world generic set, so this is where a copy between two of them would be most tempting and most
    wrong.

    Commit once.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit && npm run lint</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/scenario-coverage.ts').then((m)=>{const pend=m.pendingPairs();const speaking=pend.filter((p)=>p.skill==='speaking');if(speaking.length!==5)throw new Error('expected 5 speaking pairs still pending, got '+speaking.length+': '+speaking.map((p)=>p.key).join(', '));if(!pend.every((p)=>p.key.startsWith('native/')))throw new Error('only Sounding Native pairs should remain, got '+pend.map((p)=>p.key+':'+p.skill).join(', '));console.log('25 of 30 speaking pairs written; only Sounding Native remains');})"</automated>
  </verify>
  <done>
    Eleven more speaking pairs are written, twenty-five of the thirty in total; only Sounding Native
    pairs remain unwritten, in any skill; the harness passes including the no-shared-task assertion.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sounding Native — the last five, and CONT-01 closes at 52 of 52</name>
  <files>src/lib/content/scenario-speaking.ts</files>
  <read_first>
    `src/lib/curriculum.ts` lines 129-141 (the five Sounding Native scenarios and their levels),
    `src/lib/content/phrases.ts` for the tongue-twister set on `native/pronunciation`, which is a
    legitimate warm-up but a poor model for a rehearsal task, and plan 09's tasks as the anchor.
  </read_first>
  <action>
    Author the last five rehearsal tasks, closing CONT-01.

    `native/idioms` (C1) — use two idioms correctly inside a single spoken turn, which is harder and
    more useful than reciting them.
    `native/phrasal-verbs` (B2) — say the same thing twice, once with the phrasal verb and once with
    its single-word twin, and name which one fits the situation.
    `native/pronunciation` (B2) — a rehearsal task about word stress, connected speech or a specific
    sound in a real utterance, not a tongue-twister; the warm-up step already drills those and this is
    the exercise, not the drill.
    `native/register` (C1) — say one message twice, formally and casually, and name what changed.
    `native/culture` (C1) — ask what a reference means without breaking the flow of the conversation,
    which is the skill this scenario actually needs.

    Every rule from plan 09 applies unchanged, including the Phase 4 boundary in this plan's objective.
    Each must be practisable aloud alone, with no AI and no microphone.

    Commit once. This commit closes CONT-01: every one of the fifty-two declared pairs has its own
    exercise, written for its own scenario.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/scenario-coverage.ts').then((m)=>{const pend=m.pendingPairs();if(pend.length)throw new Error('CONT-01 not closed, still unwritten: '+pend.map((p)=>p.key+':'+p.skill).join(', '));const t=m.COVERAGE_TOTALS;if(t.pairsWritten!==52||t.pairsTotal!==52)throw new Error('expected 52/52, got '+t.pairsWritten+'/'+t.pairsTotal);const incomplete=m.SCENARIO_COVERAGE.filter((c)=>!c.complete);if(incomplete.length)throw new Error('scenarios not complete: '+incomplete.map((c)=>c.key).join(', '));console.log('CONT-01 closed: 52/52 pairs, 35/35 scenarios complete');})"</automated>
  </verify>
  <done>
    No declared pair is reported unwritten; the registry reports 52 of 52 pairs and 35 of 35 scenarios
    complete; the full suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored task bank → served HTML and client bundle | The last sixteen tasks are compiled into the page |
| coverage registry → skill pages and structured data | 52 of 52 becomes a public claim of completeness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Tampering | two of the sixteen previously-shared scenarios ending up with the same task | high | mitigate | These are exactly the scenarios that used to share a per-world set. The harness asserts neither the whole task nor the move list is shared, on every run. |
| T-03-23 | Tampering | a completeness claim outrunning the banks | high | mitigate | 52 of 52 is derived from four bank lookups and proved by a control assertion that an emptied bank flips a pair back to unwritten; nothing anywhere sets a completion flag. |
| T-03-10 | Tampering | authoring past the phase boundary into CONT-04's Phase 4 scope | low | accept | Stated in the objective and Task 2's action; levels come from the curriculum entry. |
| T-03-04 | Information disclosure | coverage summaries assembled from task text | medium | mitigate | Summaries stay counts and units. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed; the phase gate asserts 11 + 11. |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-scenario-content.mts` passes and reports 35/35 phrases, 35/35 vocabulary, 52/52 pairs.
- All other harnesses unchanged. `npx tsc --noEmit`, `npm run lint`, `npm run build` exit 0. Dependency count 11 + 11.
- Task 1's gate counts **speaking** pending pairs, per plan 05's convention. Task 2's is the one place in the phase where a global total is the right assertion rather than a fragile one: this is wave 9, every other plan has landed, and 52 of 52 with nothing pending is precisely the claim the task exists to make. A per-skill assertion here would be weaker than the plan's own subject.
</verification>

<success_criteria>
- Every one of the fifty-two declared pairs has its own exercise.
- No two scenarios share an exercise, asserted rather than assumed.
- The 52 of 52 reading would change by itself the moment a bank changed.
</success_criteria>

<output>
Create `.planning/phases/03-every-scenario-practicable/03-10-SUMMARY.md` when done. State that
CONT-01's content is complete and that the requirement itself closes at the gate, not here — the
browser pass is still owed.
</output>
