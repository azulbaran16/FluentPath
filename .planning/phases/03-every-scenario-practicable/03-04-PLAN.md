---
phase: 03-every-scenario-practicable
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/lib/content/phrases.ts
  - src/lib/content/scenario-vocabulary.ts
autonomous: true
requirements: [CONT-02]

must_haves:
  truths:
    - "All five Reading & Ideas scenarios have their own six phrases and eight vocabulary cards"
    - "All five Sounding Native scenarios have their own six phrases and eight vocabulary cards"
    - "Every one of the thirty-five scenarios reports at least six phrases and eight vocabulary cards"
    - "CONT-02 is satisfied by reading the banks, not by claiming it"
  artifacts:
    - path: "src/lib/content/phrases.ts"
      provides: "Curated phrase sets for the last eight scenarios that had none"
      contains: "academic/articles"
    - path: "src/lib/content/scenario-vocabulary.ts"
      provides: "Vocabulary decks completing all thirty-five scenarios"
      contains: "native/register"
  key_links:
    - from: "src/lib/scenario-coverage.ts"
      to: "src/lib/content/scenario-vocabulary.ts"
      via: "the 35/35 reading is derived from deck lengths"
      pattern: "getScenarioVocabulary"
---

<objective>
Close CONT-02: the last ten scenarios — all of Reading & Ideas and all of Sounding Native — reach
D-04's ratified floors, and the coverage meter reads 35/35 for both phrases and vocabulary because
the banks say so.

Purpose: D-02 ordered this whole block ahead of the fifty-two exercises so that every scenario
starts feeding the spaced-repetition queue before any exercise is written. This plan is where that
becomes true for all thirty-five rather than for most of them.

Boundary worth naming, because it is easy to overshoot here: Sounding Native's *fuller* native-level
treatment — the deeper idiom, phrasal-verb, pronunciation-drill and register work — is CONT-04 and
belongs to Phase 4, and CONTEXT defers it explicitly. This plan authors those five scenarios at
their own declared CEFR levels, the same floors as every other scenario, and does not pre-empt
Phase 4's depth.

Output: five Reading & Ideas scenarios and five Sounding Native scenarios at the floors; CONT-02
closed.

**What the learner gains if execution stops right after this plan:** every scenario in all six
worlds carries phrases and vocabulary written for it, all of it feeding the review queue, and every
scenario page is honest about the fact that its skill exercise is still to come. This is a coherent
resting point — the whole of CONT-02, and none of CONT-01 overclaimed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-every-scenario-practicable/03-CONTEXT.md
@.planning/phases/03-every-scenario-practicable/03-01-SUMMARY.md
@AGENTS.md

Source files this plan reads or edits:
@src/lib/curriculum.ts
@src/lib/content/phrases.ts
@src/lib/content/scenario-lessons.ts
@src/lib/content/scenario-vocabulary.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reading &amp; Ideas — five scenarios reach the floor</name>
  <files>src/lib/content/phrases.ts, src/lib/content/scenario-vocabulary.ts</files>
  <read_first>
    `src/lib/curriculum.ts` lines 103-115 (the five Reading & Ideas scenarios, their blurbs, their
    levels and — importantly — which skills each declares), `src/lib/content/scenario-lessons.ts` for
    their briefings, and the sets committed by plans 01 through 03 as the style anchor.
  </read_first>
  <action>
    Author phrases and vocabulary for Reading & Ideas at D-04's ratified floors of six phrases and
    eight cards per scenario. None of these five has a curated set today, so all five need both.

    Scenarios: `academic/news` (B2), `academic/articles` (C1), `academic/stories` (B2),
    `academic/summaries` (B2), `academic/debate` (C1).

    Four of these five declare reading and not speaking, which shapes what their phrases should be.
    CONT-02 asks for phrases in every scenario, and for a reading scenario the useful phrases are the
    language of *working with* a text out loud — asking what a paragraph is driving at, hedging an
    inference, marking a shift in an argument, saying what a story's narrator is doing. Do not write
    a comprehension exercise here; that is the reading pair, and plans 07 and 08 own it.
    `academic/debate` is the one that declares speaking, and its phrases are conceding, rebutting and
    building.

    Every authoring rule from plans 02 and 03 applies unchanged: the scenario's own CEFR level, a
    Spanish gloss on every phrase, a tip only where there is a real trap, an example on every card
    showing the word working in this scenario, an authored slug id on every item that is unique
    within its scenario and never derived from position, vocabulary that complements the phrases
    rather than restating them, nothing byte-identical to another scenario's set, all original.

    `academic/articles` is C1 and is the level where an author most easily writes vocabulary that is
    merely long rather than genuinely advanced: prefer the words that let a learner track an argument
    across paragraphs over impressive nouns.

    Commit once.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit && npm run lint</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/scenario-coverage.ts').then((m)=>{const a=m.SCENARIO_COVERAGE.filter((c)=>c.key.startsWith('academic/'));if(a.length!==5)throw new Error('expected 5 academic scenarios, got '+a.length);const bad=a.filter((c)=>c.phrases<6||c.vocabulary<8);if(bad.length)throw new Error('below floor: '+bad.map((c)=>c.key).join(', '));console.log('academic: 5 scenarios at floor');})"</automated>
  </verify>
  <done>
    All five Reading & Ideas scenarios report at least six phrases and eight vocabulary cards; the
    coverage meter reports 32/35 scenarios with phrases and 30/35 with vocabulary.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sounding Native — five scenarios reach the floor, and CONT-02 closes</name>
  <files>src/lib/content/phrases.ts, src/lib/content/scenario-vocabulary.ts</files>
  <read_first>
    `src/lib/curriculum.ts` lines 129-141 (the five Sounding Native scenarios and their levels),
    `src/lib/content/scenario-lessons.ts` for their briefings, `src/lib/content/phrases.ts` for the
    two sets that already exist here, and `.planning/phases/03-every-scenario-practicable/03-CONTEXT.md`
    for the Phase 4 boundary named in this plan's objective.
  </read_first>
  <action>
    Author the last of CONT-02. Three scenarios need phrase sets; all five need vocabulary decks.

    Phrase sets needed — three scenarios, six entries each: `native/phrasal-verbs` (B2),
    `native/register` (C1), `native/culture` (C1). `native/idioms` and `native/pronunciation` already
    carry their six from plan 01.

    Vocabulary decks needed — five scenarios, eight cards each: `native/idioms`,
    `native/phrasal-verbs`, `native/pronunciation`, `native/register`, `native/culture`.

    Every authoring rule from the previous plans applies unchanged. Two things are specific to this
    world. First, the Phase 4 boundary: write these five at their declared CEFR levels and to the
    same floors as every other scenario. The deeper native-level treatment of this world is CONT-04
    and CONTEXT defers it to Phase 4; a set that tries to be that here would both overshoot this
    phase and leave Phase 4 nothing to add. Second, `native/pronunciation`'s existing set is
    tongue-twisters, which is a legitimate shape for that scenario but a poor model for the other
    four — do not spread it. `native/register` is about switching tone for the situation, so its
    phrases should come in contrasting pairs rather than as a flat list, and its cards should be the
    markers that signal which register the learner is in.

    Commit once. This commit closes CONT-02: every one of the thirty-five scenarios now has its own
    phrases and its own vocabulary, and every one of them feeds the review queue.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/scenario-coverage.ts').then((m)=>{const t=m.COVERAGE_TOTALS;if(t.scenariosWithPhrases!==35)throw new Error('CONT-02 not closed: '+t.scenariosWithPhrases+'/35 with phrases');if(t.scenariosWithVocabulary!==35)throw new Error('CONT-02 not closed: '+t.scenariosWithVocabulary+'/35 with vocabulary');console.log('CONT-02 closed',JSON.stringify(t));})"</automated>
    <automated>node --experimental-strip-types scripts/verify-merge.mts && node --experimental-strip-types scripts/verify-schema.mts && node --experimental-strip-types scripts/verify-queue.mts</automated>
  </verify>
  <done>
    All thirty-five scenarios report at least six phrases and eight vocabulary cards; the coverage
    meter reports 35/35 and 35/35; the merge, schema and queue harnesses are unchanged; CONT-02 is
    satisfied by derivation rather than by claim.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/api/progress` | The `srs` and `attempts` maps reach their CONT-02 size here — roughly 490 scenario keys |
| authored content module → served HTML | The last ten scenarios' authored strings enter the served document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02 | Denial of Service | progress blob size once every scenario item is schedulable | medium | mitigate | Measured at 42 KiB for the CONT-02 shape against a 1 MiB cap; a 413 is treated as a permanent drop by the sync queue, so the gate plan measures a saturated blob rather than assuming. |
| T-03-05 | Tampering | id collision across ~490 scenario keys plus the existing banks | high | mitigate | The harness asserts global uniqueness and disjointness on every run. |
| T-03-06 | Repudiation | an id derived from position re-pointing a learner's schedule | high | mitigate | Authored slugs only, asserted unique within each scenario. |
| T-03-09 | Tampering | two scenarios ending up with the same set | medium | mitigate | The byte-identical-set assertion mechanises D-01. |
| T-03-10 | Tampering | authoring past the phase boundary into CONT-04's Phase 4 scope | low | accept | Named in the objective and in Task 2's action; the levels come from the curriculum entry, which bounds it. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed; the phase gate asserts 11 + 11. |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-scenario-content.mts` passes and reports 35/35 phrases, 35/35 vocabulary, 0/52 pairs.
- `verify-schema` 309, `verify-queue` 173, `verify-headers` 24, `verify-celpip-sections` 43, `verify-celpip-content` 648, `verify-celpip-speech` 50 unchanged; `verify-merge` at plan 02's recorded total.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` exit 0. Dependency count 11 + 11.
</verification>

<success_criteria>
- CONT-02 is closed and the closure is derived from bank contents.
- Sounding Native is at the floors without pre-empting Phase 4's CONT-04 depth.
- Every scenario page that still lacks an exercise says so; none presents generic material as scenario practice.
</success_criteria>

<output>
Create `.planning/phases/03-every-scenario-practicable/03-04-SUMMARY.md` when done. State plainly
that CONT-02 is closed and CONT-01 is not, and leave `REQUIREMENTS.md` untouched — marking CONT-01
here would be exactly the overclaim this phase's derivation exists to make impossible.
</output>
