---
phase: 03-every-scenario-practicable
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/lib/content/phrases.ts
  - src/lib/content/scenario-vocabulary.ts
autonomous: true
requirements: [CONT-02]

must_haves:
  truths:
    - "All seven Work & Professional scenarios have their own six phrases and eight vocabulary cards"
    - "All five Practical Life scenarios have their own six phrases and eight vocabulary cards"
    - "No work scenario is handed the per-world generic set any more"
    - "Every new item is schedulable under an id that names its own scenario"
  artifacts:
    - path: "src/lib/content/phrases.ts"
      provides: "Curated phrase sets for the ten Work and Practical scenarios that had none"
      contains: "practical/banking"
    - path: "src/lib/content/scenario-vocabulary.ts"
      provides: "Vocabulary decks for all twelve Work and Practical scenarios"
      contains: "work/negotiating"
  key_links:
    - from: "src/lib/content/phrases.ts"
      to: "src/lib/scenario-coverage.ts"
      via: "coverage counts read the curated sets directly"
      pattern: "getScenarioPhrases"
---

<objective>
Land CONT-02 for Work & Professional and Practical Life: twelve scenarios, six phrases and eight
vocabulary cards each, at D-04's ratified floors.

Purpose: these twelve are where the generic per-world fallback did the most damage. Every
un-curated work scenario — emails, presentations, negotiating, networking, feedback — was handed the
same three lines, which is literally two scenarios returning the same exercise and the outcome D-01
was chosen to prevent. D-02 puts this content ahead of the fifty-two exercises because these items
feed the review queue immediately.

Output: `work/emails`, `work/presentations`, `work/negotiating`, `work/networking`,
`work/feedback` and all five Practical Life scenarios with phrase sets of their own, and vocabulary
decks for all twelve.

**What the learner gains if execution stops right after this plan:** twenty-five of the
thirty-five scenarios — the whole Social, Travel, Work and Practical Life catalogue, which is every
scenario she would use in a working week — carry their own phrases and vocabulary and feed spaced
repetition. Only Reading & Ideas and Sounding Native still say their phrases are not written yet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-every-scenario-practicable/03-CONTEXT.md
@.planning/phases/03-every-scenario-practicable/03-01-SUMMARY.md
@AGENTS.md

Source files this plan reads or edits:
@src/lib/curriculum.ts
@src/lib/content/phrases.ts
@src/lib/content/scenario-lessons.ts
@src/lib/content/scenario-vocabulary.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Work &amp; Professional — seven scenarios reach the floor</name>
  <files>src/lib/content/phrases.ts, src/lib/content/scenario-vocabulary.ts</files>
  <read_first>
    `src/lib/curriculum.ts` lines 74-88 (the seven Work scenarios, their blurbs and their CEFR
    levels), `src/lib/content/scenario-lessons.ts` for their briefings, and the sets committed by
    plans 01 and 02 as the style anchor.
  </read_first>
  <action>
    Author phrases and vocabulary for Work & Professional at D-04's ratified floors of six phrases
    and eight cards per scenario.

    Phrase sets needed — five scenarios, six entries each: `work/emails` (B1),
    `work/presentations` (B2), `work/negotiating` (C1), `work/networking` (B2),
    `work/feedback` (B2). `work/interviews` and `work/meetings` already carry their six from plan 01.

    Vocabulary decks needed — seven scenarios, eight cards each: `work/interviews`, `work/meetings`,
    `work/emails`, `work/presentations`, `work/negotiating`, `work/networking`, `work/feedback`.

    The authoring rules from plan 02 apply unchanged and are not optional: write at the scenario's own
    CEFR level taken from the curriculum entry; a Spanish gloss on every phrase; a tip only where
    there is a real trap; an example sentence on every card showing the word doing its job in this
    scenario; an authored slug id on every item, unique within its scenario and never derived from
    position; vocabulary that complements the phrases rather than restating them; nothing
    byte-identical to another scenario's set, which the harness asserts because D-01 rejected two
    scenarios sharing an exercise; all content original.

    Two of these carry a specific burden. `work/emails` is B1 and is a *writing* scenario as much as
    a speaking one — its phrases are the spoken half (following up a message out loud, chasing a
    reply on a call) and must not pre-empt the writing prompt plan 06 will author for it.
    `work/negotiating` is C1, the highest level in this world, and its language is concession and
    counter-offer rather than assertion; the cards should be the words that let a learner push back
    without closing a door.

    Commit once.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit && npm run lint</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/scenario-coverage.ts').then((m)=>{const w=m.SCENARIO_COVERAGE.filter((c)=>c.key.startsWith('work/'));if(w.length!==7)throw new Error('expected 7 work scenarios, got '+w.length);const bad=w.filter((c)=>c.phrases<6||c.vocabulary<8);if(bad.length)throw new Error('below floor: '+bad.map((c)=>c.key).join(', '));console.log('work: 7 scenarios at floor');})"</automated>
  </verify>
  <done>
    All seven Work scenarios report at least six phrases and eight vocabulary cards; the harness
    passes; the coverage meter reports 22/35 scenarios with phrases and 20/35 with vocabulary.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Practical Life — five scenarios reach the floor</name>
  <files>src/lib/content/phrases.ts, src/lib/content/scenario-vocabulary.ts</files>
  <read_first>
    `src/lib/curriculum.ts` lines 116-128 (the five Practical Life scenarios and their levels),
    `src/lib/content/scenario-lessons.ts` for their briefings, and Task 1's Work sets as the
    immediate anchor.
  </read_first>
  <action>
    Author phrases and vocabulary for Practical Life at the same floors — none of these five has a
    curated set today, so all five need both.

    Scenarios: `practical/phone-calls` (B1), `practical/tech-support` (B1),
    `practical/housing` (B2), `practical/banking` (B1), `practical/appointments` (A2).

    The same rules apply in full. `practical/phone-calls` is the one scenario where the learner cannot
    see the other person, so its phrases should be the ones that survive a bad line — asking for a
    repeat, spelling out, confirming a number. `practical/appointments` is A2 and must stay A2:
    short, concrete, produceable under pressure. `practical/tech-support` and `practical/housing`
    both also declare a second skill that later plans will fill, so keep the phrases spoken and leave
    the written and read material to plans 06 and 07 rather than duplicating it here.

    Commit once.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/scenario-coverage.ts').then((m)=>{const t=m.COVERAGE_TOTALS;if(t.scenariosWithPhrases!==27)throw new Error('expected 27 scenarios with phrases, got '+t.scenariosWithPhrases);if(t.scenariosWithVocabulary!==25)throw new Error('expected 25 with vocabulary, got '+t.scenariosWithVocabulary);console.log('coverage ok',JSON.stringify(t));})"</automated>
  </verify>
  <done>
    All five Practical Life scenarios report at least six phrases and eight vocabulary cards; the
    coverage meter reports 27/35 with phrases and 25/35 with vocabulary; the full suite is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/api/progress` | The widened `srs` and `attempts` maps cross here as an untrusted JSON body |
| authored content module → served HTML | Twelve more scenarios' authored strings enter the served document |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | id collision as the key space passes 350 items | high | mitigate | The harness asserts composed ids are globally unique and disjoint from the existing banks, and runs before every commit here. |
| T-03-06 | Repudiation | an id derived from position re-pointing a learner's schedule | high | mitigate | Authored slugs only; asserted unique within each scenario. |
| T-03-09 | Tampering | two scenarios ending up with the same set under authoring pressure | medium | mitigate | The byte-identical-set assertion mechanises D-01 rather than trusting care; a copy between two related work scenarios fails the harness. |
| T-03-02 | Denial of Service | progress blob growth against the 1 MiB body cap | medium | mitigate | Additive keys only; measured worst case for the phase is 19% of the cap. |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed; the phase gate asserts 11 + 11. |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-scenario-content.mts` passes and reports 27/35 phrases, 25/35 vocabulary, 0/52 pairs.
- All other harnesses unchanged: `verify-schema` 309, `verify-queue` 173, `verify-headers` 24, `verify-celpip-sections` 43, `verify-celpip-content` 648, `verify-celpip-speech` 50; `verify-merge` at whatever total plan 02 recorded.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` exit 0. Dependency count 11 + 11.
</verification>

<success_criteria>
- Twelve more scenarios are at D-04's floors, read off the banks rather than claimed.
- No work scenario is served the per-world generic set on its own page.
</success_criteria>

<output>
Create `.planning/phases/03-every-scenario-practicable/03-03-SUMMARY.md` when done, recording the
coverage meter reading and any editorial decision plan 04 should follow.
</output>
