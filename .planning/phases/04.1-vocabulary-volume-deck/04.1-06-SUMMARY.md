---
phase: 04.1-vocabulary-volume-deck
plan: 06
subsystem: core-vocabulary
tags: [content, ngsl, srs, id-stability, payload, volume, cognate, frame-diversity, subject-shape, ceiling]
status: complete

requires:
  - "src/lib/content/core-vocabulary.ts — 380 cards to NGSL rank 515 (04.1-01 + 03 + 04 + 05)"
  - "src/lib/content/core-vocabulary-skips.ts — the skip register, 135 entries, four declared reasons"
  - "scripts/fixtures/ngsl-headwords.tsv — NGSL 1.2, 2,809 headwords, the provenance source"
  - "scripts/verify-scenario-content.mts — the quality floor, frame diversity, rank completeness, the payload stop line"
  - "scripts/verify-id-stability.mts — the `vocab:` key space under a committed hash of the whole record"
  - "04.1-05-SUMMARY.md — the opening-word histogram, the subject-shape table, and the finding that a frame rewrite is not inert"
provides:
  - "500 volume cards, NGSL ranks 8 to 648 — L3's number, reached and stopped at"
  - "13 more declared skips (register 148), so rank completeness below 648 is total: 500 + 148 = 648, exactly"
  - "481 permanent `vocab:` keys in the committed hash fixture (1,151 scheduled ids in all, 16 retired)"
  - "the FINAL payload measured from the harness: 317,038 B = 30.2% of the cap"
  - "the marginal cost measured at FIVE points, 220.6 -> 224.9 B/id, against CONTEXT's 272.8"
  - "the remaining ceiling restated from measurement: ~450 more cards under the 40% rule, ~3,100 under the hard cap"
  - "the derivation demonstrated by four independent deck moves that edited no surface copy"
affects:
  - "plan 07 (the phase gate): 120 more ids and 1,706 more content assertions to re-run 45 mutations against; nothing else in this phase authors content"
  - "any future decision about a second batch: the measured input, not the modelled one"

tech-stack:
  added: []
  patterns:
    - "the subject-shape table computed and ACTED ON mid-batch rather than only reported: 17 examples rewritten while their ids were still free, because after `--update` the same fix costs a retire-and-re-add"
    - "the near-duplicate scan run TWICE — once to find, once after every rewrite — which is the only thing that could have caught a rewrite creating a new collision"
    - "additions-only proved by FAILURE LABELS before regeneration: 120 on `is recorded in the fixture`, zero on `still holds the content it was recorded with`, zero on `left the banks and is declared retired`"
    - "a taken FRONT resolved by re-glossing rather than by skipping — 15 times, producing zero skips, because a word with an honest gloss is a card even when the obvious gloss is spoken for"

key-files:
  created: []
  modified:
    - src/lib/content/core-vocabulary.ts
    - src/lib/content/core-vocabulary-skips.ts
    - scripts/fixtures/scheduled-item-ids.json

decisions:
  - "The constraint that decides a gloss below rank 500 is neither grammar nor cognate status but the TAKEN FRONT, and that is a gloss problem rather than a skip problem — so it produced fifteen re-glossed cards and zero skips. `complete` is \"rellenar\" because `whole` (310) holds \"completo\"; `finish` is \"acabar\" because `end` (152) holds \"terminar\"; `response` is \"reacción\" because `answer` (355) holds \"respuesta\"; `store` is \"almacenar\" because `shop` (437) holds \"tienda\"; `piece` is \"pieza\" because `bit` (352) holds \"trozo\"; `chance` is \"casualidad\" because `opportunity` (520) takes \"oportunidad\"; `demand` is \"demanda\" because `require` (402) holds \"exigir\"; `establish`-style stem clashes were avoided the same way throughout."
  - "A DERIVATIONAL PAIR IS NOT A COLLISION, and the line is now written into the bank header. `job` \"empleo\" / `employee` \"empleado\" is 1:1 in both languages and both fronts are answerable; what is forbidden is two fronts a letter apart whose answers are UNRELATED, which is the `policy`/`political` case 04.1-05 avoided. `relate` was still glossed \"vincular\" rather than \"relacionar\", because that one WOULD compete with `relationship` (502, \"relación\")."
  - "`standard` was CARDED as \"habitual\", not skipped as a cognate. \"estándar\" differs from the English word as a string, so the cognate reason does not apply by its own narrow line — but a front that is the English word wearing an accent teaches nothing, so the adjective's real sense was glossed instead. The narrow line held: five cognates were skipped (total, material, personal, animal, similar, factor — six) and everything else with a differing Spanish was carded."
  - "`particularly` (601) was skipped as a function word rather than carded beside `especially` (460). It is a degree/focus intensifier, the class `quite` and `rather` were skipped on, AND its honest focus gloss \"sobre todo\" is already `especially`'s front — so a card could only duplicate a front or fudge one. `accord` (522) likewise: the rank-522 frequency is the complex preposition \"according to\", and the noun sense's gloss \"acuerdo\" belongs to `deal` (321)."
  - "SEVENTEEN examples were rewritten purely to correct a subject-shape lean this batch created, and the decision to act rather than report is the point. The first draft put bare plural/mass/inanimate subjects at 40.8% — worse than the 04.1-04 lean 04.1-05 was written to correct. The harness cannot see this shape and has no ceiling on it, the ids were not yet in the fixture, and after `--update` the same fix is a retire-and-re-add. Bare subjects 40.8% -> 26.7%, pronouns 14.2% -> 24.2%, deck-wide 16.1% -> 18.6%."
  - "`wear` was re-glossed from \"llevar puesto\" to \"llevar (ropa)\" on the scan's evidence: the first form scored 0.500 against `position` (426, \"puesto\") — a front wholly containing another live front. The parenthetical is the device `morning`, `class` and `person` already use."

metrics:
  duration: ~2h
  completed: 2026-08-05
  tasks_completed: 2
  tasks_total: 2
  commits: 1
---

# Phase 04.1 Plan 06: The last volume batch — Summary

The deck holds **500 cards** and reaches **NGSL rank 648**. Rank completeness
below 648 is **total and exact**: 500 carded plus 148 declared equals 648 with
nothing left over. The final payload was measured from the harness rather than
extrapolated — **30.2 % of the cap**, against a 40 % stop line, which is within
0.0 points of what the four earlier measurements projected.

**And the deck stops here.** Five hundred is a ceiling decision taken against a
measured number, not a point somebody got tired at, and the bank's own header
now says so to whoever opens it next.

| | |
|---|---|
| Commit | `05242e6` — content, skips and fixture in one commit |
| Cards added | **120**, NGSL ranks **516 → 648** |
| Cards removed | **0** |
| Deck now | **500** cards, ranks 8 → 648 |
| Skips added | **13** (register now **148**) |
| Rank completeness | 500 carded + 148 declared = **648** = the deepest rank, exactly |

## Both gates, actual output

```
verify-scenario-content: all 21905 assertions passed.     (was 20,199 — +1,706)
verify-id-stability:     all 3524 assertions passed.      (was 3,164 — +360)
                         1151 scheduled ids in the banks, 1151 in the fixture, 16 retired
verify-merge:            all 25647 assertions passed.
verify-schema:           all 309 assertions passed.
verify-queue:            OK — 173/173 assertions passed
verify-headers:          all 24 assertions passed.
verify-celpip-sections:  all 43 assertions passed.
verify-celpip-content:   all 648 assertions passed.
verify-celpip-speech:    all 50 assertions passed.
npx tsc --noEmit         exit 0, no output
npm run lint             exit 0, no output
npm run build            exit 0, ✓ Compiled successfully in 8.7s, 114 static pages
dependencies             11 + 11, unchanged; zero packages installed
```

**The +1,706 decomposes exactly**, and unlike 04.1-05 it is clean multiplication
again, because nothing left the bank this time:

| Source | Arithmetic | Assertions |
|---|---|---|
| the per-card quality floor (7 × 120) | id, word, gloss, example, gloss≠word, shows-own-form, ≥6 words | +840 |
| provenance (1 × 120) | every word on the committed NGSL list | +120 |
| containment (5 × 120) | own resolver, not `resolveReviewItem`, not `parseScenarioItemId`, not the shared queue, well-formed key | +600 |
| rank completeness | the deepest rank moved 515 → 648 | +133 |
| a skip carries a reason (1 × 13) | | +13 |
| | | **+1,706** |

The +360 on the id gate is **120 × 3** — scope, rule 1, rule 3, one of each per
new id — with no correction term, because 04.1-05's `+2` came from a retirement
and this batch has none.

## The payload, verbatim from the harness — and this is criterion 4

```
  payload:    317,038 B saturated over 1252 STORAGE id(s) = 752 shared-queue + 500 volume
              — 30.2% of the 1,048,576 B cap (stop line 40%)
```

**30.2 %. Under the 40 % stop line, with 9.8 points of margin.** Printed by the
gate after the batch, not calculated here.

### The marginal cost, measured at five points

| | bytes | ids | B/id |
|---|---|---|---|
| plan 02, 20 volume cards | 209,586 − 205,174 = 4,412 | 20 | **220.6** |
| plan 03, 120 volume cards | 236,314 − 209,586 = 26,728 | 120 | **222.7** |
| plan 04, 120 volume cards | 263,120 − 236,314 = 26,806 | 120 | **223.4** |
| plan 05, net +120 volume cards | 290,056 − 263,120 = 26,936 | 120 | **224.5** |
| plan 06, 120 volume cards | 317,038 − 290,056 = **26,982** | 120 | **224.9** |

**Mean over all 500 volume ids: 111,864 B ÷ 500 = 223.7 B/id.**

**Against CONTEXT's 272.8 B/id, a volume card costs 49.1 B less — 82 % of it.**
The reason is structural and was predicted at plan 02: a `vocab:private` key is
far shorter than a `work/interviews#grammar#tell-me-about-yourself` one, and the
tier stores one short topic string for the whole deck rather than one per
scenario. **272.8 B/id is an average over the scenario ids and remains the wrong
number to extrapolate a volume card from** — it is conservative in the safe
direction, so nothing was ever decided wrongly on it, but it over-states this
deck's cost by 22 %.

The drift is real and small: **+0.8 B per batch across four 120-card batches**,
which is what a lengthening mean slug looks like as the frequency list runs out
of short words (`opportunity`, `technology`, `international` against `have` and
`go`). At five points it is a trend, not noise, and it is small enough to model.

### The remaining ceiling, restated from measurement — a FINDING, not a plan

At the measured ~225 B/id with the drift folded in:

| | headroom | more cards | deck would be |
|---|---|---|---|
| **the 40 % rule** | 419,430 − 317,038 = 102,392 B | **~450** | ~950 |
| **the hard 1 MiB cap** | 1,048,576 − 317,038 = 731,538 B | **~3,100** | ~3,600 |

Three things follow, and all three are findings rather than proposals:

1. **A second batch of 500 would fit under the 40 % rule** — barely, landing near
   36 %. That is a fact about arithmetic, not a recommendation: L3 says 500 and
   stop, and **growing this deck is a fresh decision with the payload re-measured
   first**. This plan does not take it and did not prepare for it.
2. **The hard-cap ceiling is nearer 3,600 volume cards than CONTEXT's 3,090**,
   because CONTEXT computed at 272.8 B/id. The correction moves in the permissive
   direction, which is exactly why it should not be acted on without a fresh
   measurement — a ceiling that keeps rising when you re-measure is the shape of
   a number somebody wants to be bigger.
3. **The 8,000–11,000-card fluency deck is still unreachable**, at any of these
   rates, in the JSON-column design. That needs DATA-01, a normalised progress
   schema, which sits in the v2 backlog. The correction does not change this.

**It does not fail gracefully**, and that is why the margin matters: the route
answers 413 above the cap, `sync-queue` classifies 413 as permanent and drops the
slot, so crossing the cap does not degrade — it silently stops syncing the
learner's progress.

## The derivation, demonstrated four times

The claim under this whole surface is that every count it shows is read off the
bank. Five commits have now moved the deck **20 → 140 → 260 → 380 → 500**, and:

```
0cf6b24  04.1-05  +121 / −1  ->  (no surface file touched)
a05bc0c  04.1-04  +120       ->  (no surface file touched)
52f8552  04.1-03  +120       ->  (no surface file touched)
ff1f6ca  04.1-01  +20        ->  CREATED both surface files
05242e6  04.1-06  +120       ->  (no surface file touched)
```

**The four deck MOVES that happened after the surface existed edited no line of
it.** `ff1f6ca` is the commit that built the surface alongside the tracer twenty,
so it is a creation and not a count edit — and the plan's check, read literally
over "the four authoring commits", would have reported it as a failure. See
"things the plan got wrong", item 3.

Untouched is weaker than derived, so the surface was also read directly:

```
words the surface will show: 500
bands the surface will show: 10   sizes: 50/50/50/50/50/50/50/50/50/50
last band range: leader -> private
```

Every one of those comes from `ALL_ITEMS.length`, `BANDS.length`, `band.length`
or `band.filter(...).length`. The only numeric literals anywhere in
`CoreVocabView.tsx` or the page are Tailwind classes and icon sizes — `h-11`,
`w-5`, `size={92}`, `16%`. **There is no hand-written count to find.**

## The near-duplicate scan, run twice

Scanned: this batch's 120 glosses and 120 examples, by exact match and by Jaccard
over word sets, against **each other, the 380 cards already in this deck, all 352
scenario vocabulary cards (gloss and example), every scenario phrase (`es` and
`text`), and every deck-browser card** — **1,252 entries per field**.

| Field | Exact matches | Highest pair, after rewriting | Over threshold |
|---|---|---|---|
| `es` (threshold 0.60) | **0** | **0.500** — a cluster of six, none a defect | **0** |
| `example` (threshold 0.50) | **0** | **0.308** — `accept` vs a hotel-deposit card | **0** |

**Zero exact matches on either field, and nothing over threshold on either.**

**Twelve examples and one gloss were rewritten anyway**, all below the acting
line, because they are the defect class 04.1-03's `turn`, 04.1-04's `price` and
04.1-05's `raise` established — the same sentence with a noun swapped:

| Card | Was | Against | Score | Now |
|---|---|---|---|---|
| `attention` | "Pay attention to the small print on the contract." | `collocations:2` "Pay attention to the details." | **0.444** | "Small print rarely gets the attention it deserves." |
| `arrive` | "Guests arrive an hour before the ceremony starts." | the committed `present` "Wrap the present before the guests arrive." | **0.400** | "Winter arrives suddenly in these mountain villages." |
| `employee` | "Employees park behind the warehouse for free." | the committed `member` "Members of the club park for free." | **0.400** | "Employees clock in through the side entrance." |
| `contact` | "Contact the landlord before you pay anything." | `talk`, a housing card and an emails card — a **four-way** "…before you ⟨verb⟩ anything" frame | **0.364** | "Contact the surgery first thing on Monday morning." |
| `piece` | "One piece of the puzzle is missing." | the committed `step` "One step is missing from the fire escape." | **0.364** | "A missing piece ruined the whole jigsaw." |
| `firm` | "This mattress feels far too firm for me." | the committed `strong` "That coffee is far too strong for me." | **0.333** | "Press the soil down until it feels firm." |
| `pretty` | "What a pretty garden the corner house has!" | the committed `house` "Their house has a garden full of lemons." | **0.333** | "Somebody hung pretty lanterns along the whole terrace." |
| `due` | "Her baby is due in the middle of March." | the committed `start` "Classes start again in the middle of September." | **0.308** | "Her baby is due the week after Easter." |
| `street` | "Which street has the cheapest parking meters?" | the committed `country` "Which country has the longest coastline?" | **0.300** | "Cyclists use that street to avoid the roundabout." |
| `police` | "Police closed the road after the accident." | the committed `main`, plus this batch's own `road` — three road sentences | **0.300** | "Police stopped three drivers outside the school gate." |
| `recently` | "Recently the buses have started running on time." | `daily:7` "The train arrived on time." | **0.300** | "Recently somebody has been feeding the stray cats." |
| `wear` (gloss) | "llevar puesto" | the committed `position` "puesto" — a front wholly containing another front | **0.500** | "llevar (ropa)" |

`contact` and `street` are the sharpest. `street`'s was a literal frame clone —
*"Which ⟨noun⟩ has the ⟨superlative⟩…?"* with the noun swapped — at a score of
0.300, less than two thirds of the acting line, and it would have shipped.
`contact`'s was a frame the corpus already held **four** times.

**The scan was then re-run over the whole deck**, exactly as 04.1-05's ninth-row
finding demands, because a frame rewrite is not inert. **Every rewrite was clean
and the highest example pair fell 0.444 → 0.308.** This time no rewrite created
a new collision — which is a result of the re-run, not a reason to skip it.

### The six surviving `es` pairs at 0.500, and why none is a defect

All six are the same shape 04.1-05 recorded: a one-word volume gloss sitting
inside a longer, more specific phrase that carries the discriminating word.

`project` "proyecto" vs `plan` "planear / proyecto"; `realize` "darse cuenta" vs
`account` "cuenta"; `save` "ahorrar" vs the deck browser's "ahorrar (para)";
`limit` "límite" vs "fecha límite"; `attention` "atención" vs "prestar atención";
`contact` "contactar" vs "conseguir contactar". In every case the longer front is
a different string a learner would not answer the same way.

**`wear` was the seventh and it was fixed**, because it was the one where the
shorter front was the *live* card: `position` (426) holds "puesto" and cannot be
touched, so the new card moved instead.

## The opening-signature histogram at 500

The harness's own printed lines:

```
  volume deck: 500 card(s), deepest NGSL rank 648, 499 distinct opening shape(s);
               ceilings 25 per shape / 100 per opening word
               — 5 word(s) also in the deck browser (book, kind, order, already, maybe),
                 reported not asserted
  top openings: "give me" x2, "a blister" x1, "a crack" x1, "a customer" x1, "a dog" x1,
                "a fine" x1, "a girl" x1, "a group" x1, "a letter" x1, "a new" x1
```

**499 distinct shapes over 500 cards.** The single repeat, `"give me" x2`, is
plan 03's and predates this batch: **this batch introduced zero repeated two-word
openings, and still zero after twenty-nine rewrites** — which was checked, not
assumed.

The printed top ten is ten singletons again, so here is the figure that actually
constrains anything after this — the **opening WORD** histogram over the whole
deck, every count ≥ 2, computed by hand because the harness prints bigrams:

```
the x30, a x12, her x10, our x10, that x10, she x9, they x9, we x9, i x8, my x8,
his x7, nobody x7, both x6, he x6, nothing x6, one x6, their x6, somebody x5,
this x5, who x5, you x5, an x4, does x4, every x4, there x4, which x4, by x3,
can x3, children x3, everyone x3, give x3, how x3, in x3, only x3, someone x3,
what x3, at x2, bring x2, buses x2, cut x2, did x2, don't x2, everything x2,
few x2, from x2, half x2, is x2, kids x2, let's x2, local x2, money x2,
prices x2, rain x2, ring x2, send x2, street x2, these x2, those x2, two x2,
wait x2, whose x2, wind x2, your x2
distinct opening words: 280 of 500
```

**This batch's own contribution:**

```
this batch (120): the x7, a x5, i x5, our x5, he x4, you x4, both x3, her x3,
                  she x3, and four words at x2
                  distinct opening words: 86 of 120
                  repeated two-word shapes WITHIN the batch: 0
"the" openers:            deck 30/500 (6.0%), this batch 7/120 (5.8%)
pronoun/possessive:       deck 93/500 (18.6%), this batch 29/120 (24.2%)
```

**`the` finished at 6.0 %, comfortably inside the one-in-ten budget** and at 30
against a ceiling of 100. Nothing in the deck is close to firing any ceiling: the
worst bigram is 2 against 25, the worst opening word is 30 against 100, and 499
distinct shapes sit against a floor of 100.

## Subject-shape balance — measured, and then CORRECTED mid-batch

This is the section worth reading, because the first draft failed here and
nothing in the harness would have said so.

**The first draft put bare plural/mass/inanimate subjects at 49 of 120 —
40.8 %.** That is two and a half times 04.1-05's 16.5 %, and it is *worse than
the 04.1-04 lean that 04.1-05 was written to correct*. Pronouns were at 14.2 %,
which would have stalled the deck-wide correction at 16.1 %.

The cause is real and is a property of this band rather than of the author:
ranks 516–648 are heavily abstract and Latinate — `industry`, `technology`,
`culture`, `demand`, `performance`, `society` — and every one of those writes
itself with an inanimate subject. **That is exactly the excuse 04.1-05 refused,
so seventeen examples were rewritten**, while their ids were still outside the
fixture and the fix was free. After `--update` the same correction is a
retire-and-re-add on seventeen permanent keys.

Classified mechanically, not by eye — the counts below sum to 120:

| Subject shape | this batch | share |
|---|---|---|
| bare plural / mass / inanimate subject | 32 | 26.7 % |
| personal pronoun / possessive | 29 | 24.2 % |
| imperative | 13 | 10.8 % |
| numeral / quantifier NP | 8 | 6.7 % |
| indefinite-pronoun subject (`nobody`, `somebody`, `everybody`, `anybody`) | 8 | 6.7 % |
| definite NP (`The …`) | 7 | 5.8 % |
| question (yes/no + wh) | 6 | 5.0 % |
| indefinite NP (`A …`, `An …`) | 6 | 5.0 % |
| gerund / clausal subject | 4 | 3.3 % |
| demonstrative NP (`That …`, `This …`, `Those …`) | 4 | 3.3 % |
| adverbial / PP / fronted opener | 3 | 2.5 % |

**The deck-wide pronoun share moved 16.1 % → 18.6 %**, continuing 04.1-05's
correction rather than merely holding it, and bare subjects were brought to
26.7 % of the batch. The seventeen converted are `quality`, `fund`, `wear`,
`industry`, `national`, `field`, `security`, `trade`, `news`, `standard`, `vote`,
`usually`, `performance`, `accept`, `technology`, `choice` and `foreign` — listed
so a reader can check the shape rather than trust the number.

**Neither shape has a ceiling and the harness cannot see either.** That is still
true at 500, and it is the single most important thing to hand forward: the two
quality problems this phase actually hit — repeated frames and subject-shape
lean — were both caught by a human reading a printed line or a hand-written
classifier, and neither could ever fail a gate.

## The 13 new skips — and the load went DOWN, which is the evidence that matters

`function-word` × 6, `cognate` × 6, and one more of each below. **13 skips
against 120 cards over 133 ranks is 9.8 %**, down from 11.7 % in the previous 137
ranks and 14 % before that.

**The plan named fatigue as this batch's own hazard and named an unusual late
skip load as its signal.** The rate fell. Reaching for the register more often
near the finish line did not happen, and the last twenty cards (`bank` through
`private`) are concrete, high-utility words with examples held to the same bar as
the first twenty — three of them are among the seventeen deliberately reshaped.

| Rank | word | reason | note |
|---|---|---|---|
| 522 | accord | function-word | the rank-522 frequency is **"according to"**, a complex preposition; the noun sense would gloss "acuerdo", which `deal` (321) holds |
| 525 | therefore | function-word | conjunctive adverb, the class `however` (215) was skipped on |
| 538 | itself | function-word | reflexive pronoun, pairs with `himself` (458) and `themselves` (480) |
| 553 | instead | function-word | connective adverb of the `however`/`therefore` class. The deck browser also holds it — but that is **not** the reason recorded, because overlap with that deck is reported and never asserted |
| **569** | **total** | **cognate** | "total" — **predicted by name in 04.1-05 and met** |
| **571** | **material** | **cognate** | "material" — likewise |
| 579 | upon | function-word | preposition |
| 585 | personal | cognate | "personal" |
| 587 | animal | cognate | "animal" |
| 594 | similar | cognate | "similar" |
| **601** | **particularly** | **function-word** | a degree/focus intensifier, the class `quite` and `rather` were skipped on — **and** its focus gloss "sobre todo" is already `especially`'s (460) front, so a card could only duplicate a front or fudge one |
| 626 | simply | function-word | focus particle, the class `just` (49) and `really` (104) were skipped on |
| 636 | factor | cognate | "factor" |

**Only six of thirteen are function words, and the closed class is now spent.**
What replaced it is not the cognate either — six cognates in 133 ranks is a small
residue. **What actually does the deciding below rank 500 is the TAKEN FRONT**,
and because that is a gloss problem rather than a skip problem it produced
**fifteen re-glossed cards and zero skips**. Every one carries its reason beside
the card in the bank, so the next author meets them in the file:

`complete` → "rellenar" (`whole` holds "completo") · `finish` → "acabar" (`end`
holds "terminar") · `response` → "reacción" (`answer` holds "respuesta") ·
`store` → "almacenar" (`shop` holds "tienda") · `piece` → "pieza" (`bit` holds
"trozo") · `chance` → "casualidad" (`opportunity` takes "oportunidad") ·
`demand` → "demanda" (`require` holds "exigir") · `choice` → "opción" (`choose`
holds "elegir") · `pick` → "recoger" ("escoger" is a true Spanish synonym of
`choose`'s front) · `evidence` → "indicios" (`test` holds "prueba") · `leader` →
"dirigente" ("líder" is the English word with an accent) · `performance` →
"rendimiento" ("actuación" is one suffix from `act`'s "actuar") · `relate` →
"vincular" ("relacionar" would compete with `relationship`'s "relación") ·
`firm` → "firme" (`company` holds "empresa") · `cell` → "celda" ("célula" is the
English word with an accent).

### A headword whose second sense is deliberately untaught

Two more, joining `type`, `pass` and `bear`:

- **`complete` (581) teaches "rellenar", the form-filling verb.** The adjective
  is the frequent sense and it is untaught here on purpose: "completo" is the
  second half of `whole`'s front (310, "entero / completo").
- **`pretty` (637) teaches the adjective "bonito", not the degree particle.** A
  particle is the class `quite` and `rather` were skipped on; this word has a
  real adjective a card can hold, so it is carded rather than skipped.

`due` (574) is a near-miss worth the same note: it is glossed "previsto para",
the adjective, because "due to" is a complex preposition whose Spanish
("debido a") is grammar rather than a lexical meaning a front/back card can hold.

## Things the plan or the gates got wrong

1. **The plan's Task 1 idempotence check is STILL listed before the scan** — the
   **fifth** plan in a row to carry it (02 item 3, 03 item 2, 04 item 3, 05 item
   3), and the second to carry an `<ordering_correction>` block that fixes it in
   prose while leaving the `<verify>` block wrong. This is the last authoring
   plan, so nobody will hit it again in this phase; recorded a fifth time because
   **a defect recorded five times and never fixed is a process finding, not a
   plan finding.** The correction block does work — it was followed to the letter
   here and `--update` ran exactly once — but it works by asking every author to
   read past a check that is wrong where it stands.
2. **The plan's `<verification>` section says "1,151 ids (651 scenario + 500
   volume)" and that is exactly right** — worth recording because it is the first
   count in this phase's plans that needed no correction. The fixture holds 1,151.
3. **The no-copy-edit check names the wrong four commits, and read literally it
   FAILS.** It runs `git log --oneline -n 4`, but the last four commits at plan
   start were `ef5288a`, `a5a8ba6`, `0cf6b24`, `917f804` — three docs commits and
   one batch. The authoring commits have to be found by `git log -- <the bank>`.
   Done properly, one of the four — `ff1f6ca`, 04.1-01 — **does** show both
   surface files, because that commit **created** them alongside the tracer
   twenty. A check that cannot tell a file's creation from an edit of its counts
   will fail on any plan that looks back far enough to include plan 01. The right
   shape is to check the commits that MOVED the deck after the surface existed,
   which is what was done here, and to read the surface for numeric literals —
   which is the actual claim and is stronger than "untouched".
4. **The two known removed-line defects did NOT fire this time, and both are
   still latent.** The check passed clean on both files. That is not because
   either was fixed: (a) the retirement false positive needs a removed card line,
   and this batch removed none — it will fire again on the next plan that retires
   an id, and the right fix is still to assert that a removed card line has a
   matching hand-declared entry in the fixture's `retired` list; (b) the skip
   register false positive needs the `SkipReason` union to grow, and this batch
   declared no fifth reason — it will fire on whichever plan adds one, because
   appending `| "x"` moves the semicolon on the previous line. **Both remain
   exactly as reported at 04.1-05.** A check that happens not to fire is not a
   check that works.
5. **The harness's "top ten openings" line earned its keep for the THIRD batch
   running and is still printed without comment.** It is the line that revealed
   this batch has zero repeated bigrams — and it is also the line that says
   nothing at all about the two shapes that actually went wrong here. 04.1-04
   proposed the one-line fix (print the top ten opening WORDS beside the shapes,
   and mark any shape at x2 or more); it is still not done. **Worth adding to
   it:** printing the opening-word histogram would have caught neither of this
   batch's real problems. The subject-shape lean needed a classifier nobody has
   written, and it is the second time in three batches that this shape has gone
   wrong on the first draft.
6. **The plan's own projection was right to one decimal place and that is worth
   saying out loud.** 04.1-05 projected "~317,100 B = ~30.2 %" from four
   measurements; the harness printed **317,038 B = 30.2 %**, 62 bytes low. After
   two corrections to the per-id figure earlier in this phase, the model finally
   predicted the measurement — which is what four measured points buy over one
   extrapolated table, and is the argument for re-measuring rather than the
   argument for trusting the model next time.

## Known stubs

None. Every one of the 120 cards is real content at the volume bar: a Spanish
gloss that is not the English word and is not a list of near-synonyms, and one
sentence somebody would say that contains the headword in a form the matcher
accepts and runs at least six words. No placeholder, no TODO, no card referring
to content that does not exist. Nothing was removed and nothing was retired.

## Deviations from plan

**1. [Following the plan's own correction] The near-duplicate scan ran before the
fixture regeneration.** This is what the `<ordering_correction>` block instructs,
so it is a deviation from the task *bodies* and not from the plan. `--update` ran
exactly once, after all twenty-nine rewrites, and the idempotence check was run
after it — which is the only point at which it can pass.

**2. [Rule 1 — pre-emptive, ×12] Twelve rewrites below any failing threshold**,
scoring 0.300 to 0.500 against acting lines of 0.50 and 0.60. All were free at
that moment and would have cost a retire-and-re-add afterwards. Details in the
scan section.

**3. [Rule 2 — the harness cannot see this, ×17] Seventeen examples rewritten to
correct a subject-shape lean of the author's own making.** No gate failed, no
threshold was crossed, and nothing would ever have caught it. It is filed as a
deviation rather than as routine because it is a *correctness* requirement in
this phase's own terms — 04.1-05 recorded the shape as a known blind spot and
told this plan to steer it, and the first draft did the opposite.

**4. [Rule 1 — caught by the gate] `prepare` (rank 604) was dropped between
drafting and editing**, leaving 499 cards. The rank-completeness assertion caught
it on its own label — `volume: NGSL rank 604 ("prepare") is carded or declared
skipped` — one failure out of 21,892. This is the assertion doing precisely the
job it was written for: a word passed over in silence, found in the first run.

**5. [Recorded, not fixed] The bank's doc comment was rewritten** — the deck-size
sentence, the batch arithmetic, and a new paragraph stating that 500 is a ceiling
decision taken against a measured number. The removed-line check permits it
(comment lines are excluded) and the id hash does not cover it (it hashes `id`,
`word`, `es`, `example`).

**6. [Recorded on the card, in the file] The reasoning for the fifteen
front-driven glosses is written into the bank as comments beside the cards**, not
only here, along with the derivational-pair rule in the batch header. The next
author meets them in the file.

### Authentication gates

None.

## Self-Check: PASSED

Files asserted to exist — all found:

```
FOUND: src/lib/content/core-vocabulary.ts
FOUND: src/lib/content/core-vocabulary-skips.ts
FOUND: scripts/fixtures/scheduled-item-ids.json
FOUND: .planning/phases/04.1-vocabulary-volume-deck/04.1-06-SUMMARY.md
```

Commit asserted to exist — found:

```
FOUND: 05242e6  feat(04.1-06): the last volume batch — 120 cards, the deck to 500, and it stops
```

`git diff --diff-filter=D HEAD~1 HEAD` is empty: no file was deleted. The four
scratch scripts written for the candidate analysis, the openings histogram, the
corpus scan and the shape classifier were removed before the commit;
`git status --short` shows only the three intended files plus the pre-existing
untracked `.claude/`.

## What is still outstanding

**No requirement was marked complete** — not VOCAB-02, VOCAB-04 or VOCAB-05,
which this plan's frontmatter names. That is on instruction, and it is still
correct for the reason plans 03, 04 and 05 gave: **04.1-01's task 3 human
checkpoint has still not been performed.** There are now **500** permanent
spaced-repetition keys and one orphaned one, and nobody has opened
`/core-vocabulary` in a browser since the deck was twenty cards. The surface
derives every count from the bank and will report 500 words in ten bands of
fifty — but "will" is the word this project does not accept, and no browser pass
is claimed here.

**Plan 07 owns the phase gate**, and what it inherits is: 45 mutations to re-run
against final content — where the frame ceilings are now 25/100/100 rather than
the 2/4/4 they were proved at, and where the distinct-shape floor is at last
independently falsifiable — plus the mutation-sweep hazard of 03-08 (run it in a
worktree; rebuild from the committed tree before claiming anything from a build).

**One consequence of 04.1-05 is still newly checkable and still unchecked:** the
retirement means the app can hold a learner `srs` entry under a key no bank
emits. `mergeProgress` unions keys blindly and will preserve it forever; nothing
renders it. `ef5288a` recorded that a retired id must not inflate the volume
surface, but **no one has confirmed that `/core-vocabulary` ignores an orphaned
key rather than counting it.** With the deck now at 500 and the surface's every
number derived from `coreVocabIds()`, that is a five-minute check somebody should
actually run.
