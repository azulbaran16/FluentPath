---
phase: 04.1-vocabulary-volume-deck
plan: 05
subsystem: core-vocabulary
tags: [content, ngsl, srs, id-stability, payload, volume, cognate, retirement, frame-diversity]
status: complete

requires:
  - "src/lib/content/core-vocabulary.ts — 260 cards to NGSL rank 378 (04.1-01 + 04.1-03 + 04.1-04)"
  - "src/lib/content/core-vocabulary-skips.ts — the skip register, 118 entries, three declared reasons"
  - "scripts/fixtures/ngsl-headwords.tsv — NGSL 1.2, 2,809 headwords, the provenance source"
  - "scripts/verify-scenario-content.mts — the quality floor, frame diversity, rank completeness, the payload stop line"
  - "scripts/verify-id-stability.mts — the `vocab:` key space under a committed hash of the whole record"
  - "04.1-04-SUMMARY.md — the recorded opening-WORD histogram, and the `color` fudge it recorded rather than hid"
  - "the user's decision of 2026-08-05 — `cognate` is a declared skip reason; `color` is repaired by retirement"
provides:
  - "380 volume cards, NGSL ranks 8 to 515 — 76% of L3's 500"
  - "`cognate`, the register's FOURTH reason, declared with its line drawn before first use"
  - "135 declared skips, so rank completeness below 515 is total (380 + 135 = 515, exactly)"
  - "the first card this deck has ever UN-carded: `vocab:color`, retired and not edited"
  - "361 permanent `vocab:` keys in the committed hash fixture (1,031 scheduled ids in all, 16 retired)"
  - "the payload re-measured from the harness at 380 cards: 27.7% of the cap"
  - "the opening-WORD histogram AND the subject-shape balance at 380, for plan 06 to author against"
affects:
  - "plan 06: it continues from rank 516 into a deck whose 226 distinct openers are spent, and it will meet `total` (569) and `material` (571) as cognates on the reason this plan declared"
  - "plan 07 (the phase gate): 121 more ids, one retirement and 1,714 more content assertions to re-run 45 mutations against"

tech-stack:
  added: []
  patterns:
    - "a fudge repaired by RETIREMENT rather than by an edit — the hand-written retirement declared BEFORE `--update`, so the id gate reports it as a retirement and never as a re-point"
    - "the additions-only property proved by FAILURE LABELS rather than by a diff: 121 on `is recorded in the fixture`, zero on `still holds the content it was recorded with`, zero on `left the banks and is declared retired`"
    - "the repeated-FRAME scan run against the whole 380-card deck rather than only the batch, which is what caught nine collisions that were batch-vs-committed and would have been invisible from inside the batch"
    - "subject-shape balance measured and steered by hand, because the harness has no ceiling on it and cannot see it"

key-files:
  created: []
  modified:
    - src/lib/content/core-vocabulary.ts
    - src/lib/content/core-vocabulary-skips.ts
    - scripts/fixtures/scheduled-item-ids.json

decisions:
  - "`cognate` was declared with a NARROW line and the line is written into the register beside the reason: it means no honest Spanish gloss DIFFERS from the English word, not that a word looks Spanish. `individual` → \"individuo\" and `particular` → \"concreto\" are CARDED in this very batch on that line, as are `economic`, `political`, `special`, `condition`, `action` and eleven more whose Spanish differs by an accent or an ending. Only `social` (381) and `general` (404) met it."
  - "`vocab:color` was retired and NOT edited, and the order was the load-bearing part: the retirement was written into the fixture's `retired` list BY HAND, with a reason, BEFORE `--update` ran. Run the other way round, `--update` refuses; run this way, the id gate's own labels show a retirement rather than a re-point. No replacement id was added — a cognate is not re-authored under a new slug, it is passed over."
  - "`agree` is \"coincidir\" and not \"estar de acuerdo\". The scan put the original at Jaccard 0.667 against the deck browser's `on the same page` (\"de acuerdo\"), over the 0.60 acting line. Unlike 04.1-04's `already` collision — kept because no honest alternative existed — an honest alternative existed here, so it was taken and the example was rewritten with it so gloss and sentence teach the same sense."
  - "Near-synonyms separated at authoring time because `es` is the card FRONT: `type` is \"teclear\" (kind holds \"tipo / clase\"), `policy` is \"normativa\" (political two ranks up is \"político\", and two fronts one letter apart is the same defect as two identical ones), `role` is \"función\" (paper holds \"papel\"), `difficult` is \"complicado\" (hard holds \"difícil / duro\"), `regard` is \"estima\" (consider holds \"considerar\"), `approach` is \"enfoque\" so `focus` at rank 550 can own \"centrarse\", `certain` is \"determinado\" (sure holds \"seguro (convencido)\"), `major` is \"mayor (en importancia)\" (main holds \"principal\", important holds \"importante\"), `perhaps` is \"quizás\" and `maybe` is \"a lo mejor\" — the neutral hedge and the colloquial one, not stacked behind one slash."
  - "`morning` carries the parenthetical \"mañana (antes del mediodía)\" because a bare \"mañana\" is the front of *tomorrow* and the learner would answer the wrong word. Same device as `class` → \"clase (de alumnos)\" and `person` → \"persona (un individuo)\"."
  - "`please` (471) is skipped as a function word on the politeness-particle reading and not carded as the verb \"agradar\": at that rank it is the request marker, which is the discourse class `just` (49) and `actually` (291) were skipped on. `forward` (452) is skipped as a directional particle on the same ground as `away`, while `outside` (491) is CARDED — it names a place rather than pointing along an axis."

metrics:
  duration: ~2h
  completed: 2026-08-05
  tasks_completed: 2
  tasks_total: 2
  commits: 1
---

# Phase 04.1 Plan 05: The third volume batch — Summary

The deck holds **380 cards** and reaches **NGSL rank 515**. Rank completeness
below 515 is **total and exact**: 380 carded plus 135 declared equals 515 with
nothing left over. The payload was re-measured from the harness rather than
extrapolated — **27.7 % of the cap**, against a 40 % stop line — and the batch
also **un-carded a word for the first time in this deck's life**: `vocab:color`
is retired, by hand, with a reason, and not edited.

| | |
|---|---|
| Commit | `0cf6b24` — content, skips and fixture in one commit |
| Cards added | **121**, NGSL ranks **379 → 515** |
| Cards removed | **1** — `color` (rank 378), retired |
| Deck now | **380** cards, ranks 8 → 515 |
| Skips added | **17** (register now **135**) |
| Rank completeness | 380 carded + 135 declared = **515** = the deepest rank, exactly |

**The arithmetic does not add up by one, and that is the point.**
20 + 120 + 120 + 121 = 381, not 380. The difference is the retirement, and it is
written into the bank's own header so the next author meets it in the file rather
than in this document.

## Both gates, actual output

```
verify-scenario-content: all 20199 assertions passed.     (was 18,485 — +1,714)
verify-id-stability:     all 3164 assertions passed.      (was 2,802 — +362)
                         1031 scheduled ids in the banks, 1031 in the fixture, 16 retired
verify-merge:            all 25647 assertions passed.
verify-schema:           all 309 assertions passed.
verify-queue:            OK — 173/173 assertions passed
verify-headers:          all 24 assertions passed.
verify-celpip-sections:  all 43 assertions passed.
verify-celpip-content:   all 648 assertions passed.
verify-celpip-speech:    all 50 assertions passed.
npx tsc --noEmit         exit 0, no output
npm run lint             exit 0, no output
npm run build            exit 0, ✓ Compiled successfully in 10.1s, 114 static pages
dependencies             11 + 11, unchanged; zero packages installed
```

**The +1,714 decomposes exactly**, the discipline plans 03 and 04 applied — a
count that cannot be accounted for is a count nobody checked. It is *not*
121 × the per-card rate, because one card also **left**:

| Source | Arithmetic | Assertions |
|---|---|---|
| the per-card quality floor (7 × **+120** net) | id, word, gloss, example, gloss≠word, shows-own-form, ≥6 words | +840 |
| provenance (1 × +120 net) | every word on the committed NGSL list | +120 |
| containment (5 × +120 net) | own resolver, not `resolveReviewItem`, not `parseScenarioItemId`, not the shared queue, well-formed key | +600 |
| rank completeness | the deepest rank moved 378 → 515 | +137 |
| a skip carries a reason (1 × 17) | | +17 |
| | | **+1,714** |

The **net +120** is 121 added minus 1 retired, and it is the reason the four
per-card rows are not multiples of 121. The +362 on the id gate is
**120 × 3** (scope, rule 1, rule 3 — one of each per net new id) **plus 2**: the
two assertions `color` now attracts as a retired id (rule 2's *left the banks and
is declared retired*, rule 4's *stays retired*) minus the one it no longer
attracts as a live one.

## The `color` retirement, as it appears in the fixture

It is a hand-written entry in `retired`, added **before** `--update` ran:

```json
{
  "id": "vocab:color",
  "reason": "04.1-05, on the user's decision of 2026-08-05 declaring `cognate` as a
    fourth skip reason. The card shipped at 04.1-04 (rank 378) glossed \"color / tono\",
    and Spanish for *color* is *color*: it passed the `es !== word` assertion only by
    appending a near-synonym it did not need, which makes the FRONT of a recall card a
    small lie about what the learner is being asked to answer. 04.1-04's own summary
    recorded it as a fudge rather than presenting it as solved, and named the decision
    this retirement acts on. RETIRED AND NOT EDITED, deliberately: AGENTS.md's
    one-way-door rule forbids rewriting a live id's content, verify-id-stability.mts
    hashes the whole authored record so an in-place gloss fix would fail as a re-point,
    and `--update` refuses to launder a changed hash. The word is now declared in
    core-vocabulary-skips.ts with reason \"cognate\", so rank completeness below the
    deepest rank stays total. No replacement id was added — a cognate is not
    re-authored under a new slug, it is passed over. This is the first card this deck
    has ever un-carded; any learner schedule on this key is orphaned, permanently and
    deliberately."
}
```

(Line-wrapped here for reading; it is one string in the file.)

The corresponding line **left** `ids`:

```
-    "vocab:color": "a6692162ee56d0dc9200724fd8d9280a2c909114c5b6ab32bf91a2d22e5bc8be",
```

**The order is the whole proof, and it is worth being explicit about what each
order produces.** Declared by hand first, the id gate — run before `--update` —
reports `color` on rule 4 (*is not also recorded as live*), which is the exact
signature of a retirement awaiting regeneration. Had the card been **edited in
place** instead, the failure would have read `vocab:color still holds the content
it was recorded with`, `--update` would have refused, and the only way past it
would have been the 04-07 mistake.

## The fixture: additions only, proved by labels before it was written

The plan's `<ordering_correction>` was followed to the letter: **author → scan →
rewrite → declare the retirement → `--update` once → commit.** Before
regenerating, the id gate was run as-is against final content:

```
verify-id-stability: 122 of 3045 assertions FAILED
```

and all 122, classified by label:

```
    121 FAIL  <id> is recorded in the fixture
      1 FAIL  vocab:color is not also recorded as live
```

**Zero** on `still holds the content it was recorded with`, and **zero** on
`left the banks and is declared retired`. That is the additions-only property
proved by the gate itself rather than read off a diff — and this batch is the
strongest form of that proof so far, because **fifteen examples and one gloss
were rewritten** during it. Every one of those rewrites landed on an id that was
not yet in the fixture; if a single one had touched a live card, rule 1 would
have fired on a different label and named it.

`--update` was then run **once**:

```
verify-id-stability --update: 1031 scheduled id(s) recorded (121 new), 16 retired.
```

| Check | Result |
|---|---|
| fixture diff | **125 additions, 1 removal** (`git diff --numstat` = `125  1`) |
| the one removal | `"vocab:color": "a669…"` — the retirement, and nothing else |
| added lines | 121 match `"vocab:` + 4 lines of the retirement record = 125 |
| changed hashes | none — no id appears as both a removal and an addition |
| `--update` idempotence | fixture byte-identical after re-running (`92a249c…`), and the second run printed *"is unchanged — the fixture was already regenerated alongside the content"* |
| `git diff --diff-filter=D HEAD~1 HEAD` | empty — no file deleted |

## The payload, verbatim from the harness

```
  payload:    290,056 B saturated over 1132 STORAGE id(s) = 752 shared-queue + 380 volume
              — 27.7% of the 1,048,576 B cap (stop line 40%)
```

**27.7 %. Below the 40 % stop line, so the phase continues.**

**Fourth measurement of the marginal cost, and the drift is now a trend rather
than noise:**

| | bytes | ids | B/id |
|---|---|---|---|
| plan 02, 20 volume cards | 209,586 − 205,174 = 4,412 | 20 | **220.6** |
| plan 03, 120 volume cards | 236,314 − 209,586 = 26,728 | 120 | **222.7** |
| plan 04, 120 volume cards | 263,120 − 236,314 = 26,806 | 120 | **223.4** |
| plan 05, **net +120** volume cards | 290,056 − 263,120 = **26,936** | **120** | **224.5** |

Four measurements inside 4 B of each other, rising about **+0.8 B per batch**.
That is not noise and it is not a problem: it is what a lengthening mean slug
looks like as the frequency list runs out of short words
(`organization`, `relationship`, `especially` against `have` and `go`), and it is
small enough to model.

**The projection to 500, restated from the four measurements rather than from the
original model:** the remaining 120 cards at ~225.5 B/id add ~27,060 B, landing
the full deck at **~317,100 B = ~30.2 %**. That is the same answer plan 02
projected from twenty cards and plans 03 and 04 confirmed — the fourth point does
not move it. **CONTEXT's 272.8 B/id remains an average over the scenario ids and
remains the wrong number to extrapolate a volume card from.**

## The opening-signature histogram at 380 — read this before writing plan 06

The harness's own printed lines:

```
  volume deck: 380 card(s), deepest NGSL rank 515, 379 distinct opening shape(s);
               ceilings 19 per shape / 76 per opening word
               — 5 word(s) also in the deck browser (book, kind, order, already, maybe),
                 reported not asserted
  top openings: "give me" x2, "a crack" x1, "a customer" x1, "a girl" x1, "a group" x1,
                "a letter" x1, "a new" x1, "a small" x1, "add salt" x1, "airports outside" x1
```

**379 distinct shapes over 380 cards.** The single repeat, `"give me" x2`, is
plan 03's and predates this batch: **this batch introduced zero surviving bigram
collisions**, after eleven were found and rewritten (below).

The printed top ten is again ten singletons, so here is the figure that actually
constrains plan 06 — the **opening WORD** histogram over the whole deck, every
count ≥ 2, computed by hand because the harness prints bigrams:

```
the x23, my x8, that x8, they x8, we x8, a x7, her x7, his x6, she x6, nobody x5,
nothing x5, one x5, our x5, every x4, somebody x4, their x4, there x4, this x4,
who x4, an x3, both x3, can x3, children x3, does x3, everyone x3, give x3, how x3,
i x3, in x3, only x3, what x3, at x2, bring x2, buses x2, by x2, cut x2, did x2,
don't x2, everything x2, from x2, half x2, he x2, kids x2, let's x2, local x2,
money x2, prices x2, rain x2, ring x2, someone x2, these x2, wait x2, which x2,
whose x2, your x2
distinct opening words: 226 of 380
```

**This batch's own contribution, which is the number plan 06 should judge the
process by:**

```
this batch (121): the x8, that x5, we x5, a x4, her x4, my x4, his x3, one x3,
                  she x3, they x3, and fourteen words at x2
                  distinct opening words: 79 of 121
                  repeated two-word shapes WITHIN the batch: 0
"the" openers:            deck 23/380 (6.1%), this batch 8/121 (6.6%)
pronoun/possessive:       deck 61/380 (16.1%), this batch 30/121 (24.8%)
```

### Subject-shape balance — the thing the harness cannot see, measured

Plan 04 recorded the risk it created: it drove the pronoun share down to 11.9 %
and left the deck leaning on bare plural and inanimate subjects, with **no
ceiling on that shape and no way for the harness to see it**. This batch steered
against it deliberately, and here is the steer as a number:

Classified mechanically, not by eye — the counts below sum to 121:

| Subject shape | this batch | share |
|---|---|---|
| personal pronoun / possessive | 30 | 24.8 % |
| bare plural / mass / inanimate subject | 20 | 16.5 % |
| numeral / quantifier NP | 12 | 9.9 % |
| imperative | 9 | 7.4 % |
| indefinite-pronoun subject (`nobody`, `nothing`, `somebody`, `who`) | 9 | 7.4 % |
| adverbial / PP opener | 8 | 6.6 % |
| demonstrative NP (`That …`, `This …`, `These …`) | 8 | 6.6 % |
| definite NP (`The …`) | 8 | 6.6 % |
| question (yes/no + wh) | 7 | 5.8 % |
| indefinite NP (`A …`, `An …`) | 5 | 4.1 % |
| gerund / clausal subject | 3 | 2.5 % |
| existential `There` | 2 | 1.7 % |

**The pronoun share moved 11.9 % → 16.1 % deck-wide**, which is the correction,
and the bare-plural share was held to **16.5 %** of the batch rather than allowed
to become the default. The twenty that are bare-subject are `eye`, `game`, `free`,
`policy`, `body`, `rule`, `produce`, `human`, `account`, `bear`, `describe`,
`rise`, `role`, `action`, `health`, `draw`, `white`, `practice`, `economic` and
`regard` — listed so plan 06 can see the shape rather than trust the number. **Plan 06's habit risk is neither of plan 03's nor plan 04's**:
with `the` at 23, `my`/`that`/`they`/`we` at 8 and 226 distinct openers already
spent, the pressure moves to the *second* word — the frames themselves, not the
first token. The nine batch-vs-committed collisions below are all of that kind and
none of them could fail.

**Openings this batch spent, so plan 06 knows what is gone:** gerund subjects
(`Fixing`, `Flooding`, `Reading`), bare imperatives (`Type`, `Add`, `Cover`,
`Choose`, `Cut`, `Explain`, `Slam`, `Ring`, `Mind`, `Don't carry`), wh- and yes/no
questions (`Who`, `Whose`, `What`, `Why`, `Did`, `Can`), adverbial openers
(`During`, `For a moment`, `By Friday`, `Perhaps`, `Sometimes`, `Maybe`,
`Finally`, `Tonight`), bare adjectives (`Certain`, `White`, `Economic`, `Quick`,
`Cold`, `Thin`, `Nearly`), and quantifier subjects (`Most`, `None`, `Several`,
`Every`, `Both`, `Half`, `Only`).

At n=500 the ceilings become **25 per shape / 100 per opening word / 100 distinct
shapes**. Nothing is close to firing; `the` at 23 against 100 is still the only
opener with any accumulation, and at 6.1 % it is comfortably inside the one-in-ten
budget.

## The near-duplicate scan the harness cannot do

Scanned: this batch's 121 glosses and 121 examples, by exact match and by Jaccard
over word sets, against **each other, the 259 cards already in this deck, all 352
scenario vocabulary cards (gloss and example), every scenario phrase (`es` and
`text`), and every deck-browser card** — **1,132 entries per field**.

| Field | Exact matches | Highest pair, after rewriting | Over threshold |
|---|---|---|---|
| `es` (threshold 0.60) | **0** | **0.500** — a cluster of eight, none a defect | **0** |
| `example` (threshold 0.50) | **0** | **0.364** — `paper` vs a housing phrase; `charge` vs `leave` | **0** |

**Zero exact matches on either field.** 04.1-04's one exact `es` collision
(`already`) is still there and is still deliberate; this batch added none.

**The one over-threshold pair, and it was rewritten:** `agree` glossed
"estar de acuerdo" scored **0.667** against the deck browser's `on the same page`
("de acuerdo"). Unlike `already` — kept at 1.000 because skipping it would have
cost a high-frequency word and no honest alternative existed — an honest
alternative existed here. `agree` is now **"coincidir"**, and its example was
rewritten with it so that gloss and sentence teach the same sense rather than the
gloss teaching one and the sentence another.

The surviving `es` cluster at **0.500** is eight pairs, none a defect, and all
eight are the same shape: a one-word volume gloss inside a longer, more specific
phrase that carries the discriminating word. `appear` "aparecer" vs
"aparecer, presentarse"; `account` "cuenta" vs "la cuenta" (twice); `table` "mesa"
vs "reservar mesa"; `bear` "aguantar" vs "aguantar / tolerar"; `date` "fecha" vs
"fecha límite"; `raise` "levantar" vs "levantar acta"; `door` "puerta" vs
"puerta (embarque)". In every case the longer front is a different string a
learner would not answer the same way.

**One `es` pair below threshold is worth naming anyway**, because it is the only
one where the shorter front is the *volume* card and the longer one contains it:
`individual` → "individuo" against `person` → "persona (un individuo)", at
**0.333**. `person` is live and cannot be touched. `individual` was kept as
"individuo" rather than padded to "individuo (una sola persona)", which would have
*raised* the score to 0.400 and made the front worse. Recorded, not fixed.

### Fifteen rewrites, all before their ids committed

Eleven for repeated FRAMES caught by the histogram rather than by the scan — the
signal 04.1-04 said was being printed without anyone being told to read it. All
eleven were batch-vs-committed pairs at x2 against a ceiling of 19, so **nothing
failed and nothing would ever have failed**:

| Card | Frame it repeated | Now opens |
|---|---|---|
| `involve` | `"her new"` × 2 vs the committed `new` | "Fixing the roof involves…" |
| `receive` | `"did you"` × 2 vs the committed `hear` | "We received the parcel…" |
| `team` | `"our team"` × 2 vs the committed `meet` | "Their team lost…" |
| `staff` | `"half the"` × 2 vs the committed `world` | "Nearly all the staff…" |
| `computer` | `"turn the"` × 2 vs the committed `turn` | "My computer freezes…" |
| `bear` | `"i cannot"` × 2 vs the committed `find` | "Thin walls make…" |
| `amount` | `"a small"` × 2 vs the committed `small` | "Only a tiny amount…" |
| `agree` | `"do you"` × 2 vs the committed `have` | "Every expert agreed…" |
| `staff` (2nd) | `"most of"` × 2 — introduced by the *first* rewrite, against `money` | "Nearly all the staff…" |
| `wait` (2nd) | thematic echo of a complaining phrase ("waiting over an hour") | "She waited an hour…" |

**The ninth row is the one worth reading twice.** The first `staff` rewrite fixed
one collision and created another against a card in a different batch. A frame
rewrite is not free of the problem it is fixing, and the only thing that caught it
was **re-running the whole-deck scan after rewriting** rather than trusting the
first pass. Plan 06 should re-run, not assume.

Four more were the scan's own catches, all under the acting line and all the same
defect class as 04.1-03's `turn` and 04.1-04's `price` — the same sentence with a
noun swapped:

| Card | Was | Against | Score | Now |
|---|---|---|---|---|
| `raise` | "Raise your hand if you can't hear me." | the committed `hand` "Raise your hand if you need more time." | **0.417** | "Somebody raised the barrier for the ambulance." |
| `step` | "Mind the step on your way out." | the committed `head` "Mind your head on the low beam." | **0.400** | "One step is missing from the fire escape." |
| `note` | "I left a note on the kitchen table." | `travel/restaurant#vocab#a tip` "We left a small tip on the table." | **0.455** | "Her note said the key was under the mat." |
| `wait` | "Wait outside; the doctor is running late." | `practical/appointments#vocab#be running late` "The doctor is running twenty minutes late." | **0.556** | (see above) |

`raise` is the sharpest of them: two cards asking the learner to picture the
*identical* gesture with the conditional swapped. It scored 0.417 against an
acting line of 0.50 and would have shipped.

## The 17 new skips — and the load is NOT unusual

`function-word` × 15, `already-taught` × 1, `cognate` × 2 — but **`color` is one
of the two cognates and is a repair of a rank already walked**, so this batch's
own 137 ranks produced 16 skips, or **11.7 %**, down from 14 % in the previous
140 ranks. **Nothing here was skipped because it was tiring to write.**

| Rank | word | reason | note |
|---|---|---|---|
| **378** | **color** | **cognate** | **out of batch, and a repair.** Carded at 04.1-04 as "color / tono"; retired, not edited. See above. |
| 381 | social | cognate | Spanish is "social" |
| 383 | across | function-word | preposition |
| 388 | along | function-word | preposition / particle |
| 400 | further | function-word | comparative marker of `far` (245), the ground `less` (253) and `least` (281) were skipped on |
| 404 | general | cognate | Spanish is "general" |
| 406 | toward | function-word | preposition |
| 422 | everything | function-word | indefinite pronoun, pairs with `anything` (318) and `nothing` (336) |
| 439 | someone | function-word | indefinite pronoun |
| 440 | above | function-word | preposition |
| 452 | forward | function-word | **directional particle**, the class `away` was skipped on — while `outside` (491) is CARDED, because it names a place rather than pointing along an axis |
| 458 | himself | function-word | reflexive pronoun |
| **459** | **available** | **already-taught** | only the SECOND use of this reason in 380 cards. `scenario-vocabulary.ts` teaches it with a tip and a scenario, which is strictly the better card; the duplicate assertion would have forced it anyway |
| 466 | else | function-word | postnominal determiner ("something else") |
| **471** | **please** | function-word | **the politeness particle, not the verb "agradar".** At rank 471 it marks how a request is delivered — the discourse class `just` (49) and `actually` (291) were skipped on |
| 480 | themselves | function-word | reflexive pronoun |
| 492 | behind | function-word | preposition |

**The declared line held, and the constraint changed underneath it exactly as
plan 04 predicted.** Below rank 400 the closed class really is exhausted: only 15
of 17 are function words, and eleven of those fifteen are prepositions, reflexives
or indefinite pronouns — the residue. What replaced "is this word grammar?" is
"does this word have a Spanish that differs from it?", and the answer was **yes**
far more often than the cognate worry suggested: `economic` → "económico",
`political` → "político", `special` → "especial", `condition` → "condición",
`action` → "acción", `situation` → "situación", `activity` → "actividad",
`organization` → "organización", `relationship` → "relación", `decision` →
"decisión", `practice` → "práctica", `individual` → "individuo", `particular` →
"concreto", `model` → "modelo", `detail` → "detalle", `design` → "diseño" and
more all carry honest, differing glosses. **Two words in 137 ranks met the new
reason.** Plan 06 will meet `total` (569) and `material` (571).

## A headword whose second sense is deliberately untaught

Three, and each is recorded rather than left for a reader to notice:

- **`type` (389) teaches the VERB, "teclear".** The noun sense is the frequent
  one, and it is untaught here on purpose: `kind` (rank 87) already holds
  "tipo / clase", and a second card with the same front expecting a different
  answer is the usability defect this deck has refused since plan 01.
- **`pass` (416) teaches "aprobar (un examen)".** The motion sense is untaught:
  `happen` (rank 231) holds "pasar".
- **`bear` (455) teaches the verb "aguantar".** The animal is untaught, and no
  card will ever carry it — no duplicate word is an assertion.

`policy` (399) is a near-miss worth the same note: it is glossed "normativa" and
not "política", because `political` two ranks above is "político" and two fronts
one letter apart are as unanswerable as two identical ones.

## Things the plan or the gates got wrong

1. **The plan's own removed-line check FAILS on this plan by design, and no one
   reconciled the two.** Task 1's `<verify>` block exits non-zero on any removed
   non-comment line in the bank — and the `<cognate_decision>` block at the top of
   the same `<tasks>` element *instructs* removing the `color` card. The check
   found exactly one line and it is exactly that one:
   ```
   -  card("color", "color / tono", "This color looks orange under artificial light."),
   ```
   The tripwire is doing its job; the plan asked for the one deletion it exists to
   catch and did not adjust the check. **The right shape is not to weaken it** —
   it should assert that any removed card line has a matching hand-declared entry
   in the fixture's `retired` list, which is checkable and is the actual rule.
   Until then a plan authorising a retirement has to explain a red check in prose,
   which is exactly how a real deletion eventually gets waved through.
2. **The same check produces a FALSE POSITIVE on the skip register**, and it will
   fire on any future plan that adds a fifth reason. Appending `| "cognate"` to
   the `SkipReason` union moves the semicolon, so `- | "no-base-form-example";`
   reads as a removed non-comment line. Nothing was deleted. A check whose
   failures need prose to distinguish real from formatting is a check people learn
   to skim.
3. **The plan's Task 1 idempotence check is STILL listed before the scan** — the
   fourth plan in a row to carry it (02 item 3, 03 item 2, 04 item 3), and the
   second to carry an `<ordering_correction>` block that fixes it in prose while
   leaving the `<verify>` block wrong. Plan 06 carries the same pair. Recorded
   again because a defect recorded four times and never fixed is a process
   finding, not a plan finding.
4. **The harness's "top ten openings" line earned its keep for the second batch
   running, and is still printed without comment.** It showed **ten** x2 shapes on
   this batch's first run, nine of them genuine repeated frames against committed
   cards. They cannot fail — the ceiling is 19 — so the single most productive
   quality signal in this phase is a line nobody is instructed to read. 04.1-04
   proposed the one-line fix (print the top ten opening WORDS beside the shapes,
   and mark any shape at x2 or more); it is still not done, and every authoring
   plan still has to be told in prose to read it.
5. **The whole-deck scan must be re-run after rewriting, and this batch proves
   why.** The first `staff` rewrite resolved `"half the"` × 2 and created
   `"most of"` × 2 against a card from a different batch. One pass would have
   shipped it. The scan is cheap; the assumption that a fix is inert is not.
6. **`+1,714` is not `121 × the rate`, and a reader checking the arithmetic the
   way plans 03 and 04 taught them to will get the wrong answer first.** The
   per-card assertion groups scale with the **net** +120, because a retirement
   removes a card from every one of them. This is the first batch in this phase
   where added ≠ net, and it is the first time the decomposition needed a
   subtraction.

## Known stubs

None. Every one of the 121 cards is real content at the volume bar: a Spanish
gloss that is not the English word and is not a list of near-synonyms, and one
sentence somebody would say that contains the headword in a form the matcher
accepts and runs at least six words. No placeholder, no TODO, no card referring to
content that does not exist. The one card **removed** is removed because it was
below the bar, not because it was unfinished.

## Deviations from plan

**1. [Following the plan's own correction] The near-duplicate scan ran before the
fixture regeneration, and the retirement was declared before it too.** This is
what the `<ordering_correction>` and `<cognate_decision>` blocks instruct, so it
is a deviation from the task *bodies* and not from the plan. `--update` ran
exactly once, after all fifteen rewrites and after the hand-written retirement.

**2. [Instructed by the plan, and it breaks a check] 121 cards were added rather
than 120, and one was removed.** The plan says "120 cards, taking the deck from
260 to 380"; the `<cognate_decision>` block added later requires removing `color`.
Both cannot hold, and **380 is the number the plan gates on**
(`if(n!==380)`), so 121 were authored. The removed-line check fails on the one
removal — item 1 above.

**3. [Rule 1 — pre-emptive, ×15] Fifteen rewrites below any failing threshold.**
Eleven for repeated frames at x2 against a ceiling of 19, four at Jaccard 0.400 to
0.556 against an acting line of 0.50, and one gloss at 0.667 which was genuinely
over. All were free at that moment and would have cost a retire-and-re-add
afterwards. Details in the scan section.

**4. [Recorded, not fixed] The bank's doc comment was rewritten** — the deck-size
sentence, plus a new paragraph explaining why 20 + 120 + 120 + 121 ≠ 380. The
removed-line check permits it (comment lines are excluded) and the id hash does
not cover it (it hashes `id`, `word`, `es`, `example`).

**5. [Recorded on the card, in the file] The reasoning for six separated glosses
is written into the bank as comments beside the cards**, not only here: `type`,
`policy`, `role`, `regard`, `approach` and the perhaps/maybe pair. The next author
meets them in the file.

### Authentication gates

None.

## Self-Check: PASSED

Files asserted to exist — all found:

```
FOUND: src/lib/content/core-vocabulary.ts
FOUND: src/lib/content/core-vocabulary-skips.ts
FOUND: scripts/fixtures/scheduled-item-ids.json
FOUND: .planning/phases/04.1-vocabulary-volume-deck/04.1-05-SUMMARY.md
```

Commit asserted to exist — found:

```
FOUND: 0cf6b24  feat(04.1-05): the third volume batch — 121 cards, one retirement, the deck to 380
```

`git diff --diff-filter=D HEAD~1 HEAD` is empty: no file was deleted. The six
scratch scripts written for the candidate analysis, the rank comments, the
rewrites, the retirement and the scan were removed before the commit;
`git status --short` shows only the three intended files plus the pre-existing
untracked `.claude/`.

## What is still outstanding

**No requirement was marked complete** — not VOCAB-02, VOCAB-04 or VOCAB-05,
which this plan's frontmatter names. That is on instruction, and it is still
correct for the reason plans 03 and 04 gave: **04.1-01's task 3 human checkpoint
has still not been performed.** There are now **380** permanent
spaced-repetition keys and one orphaned one, and nobody has opened
`/core-vocabulary` in a browser since the deck was twenty cards. The surface
derives every count from the bank, so it should now say 380 and band accordingly
— but "should" is the word this project does not accept, and no browser pass is
claimed here.

**One consequence of this plan is newly checkable and was not checked:** the
retirement means the app can now, for the first time, hold a learner `srs` entry
under a key no bank emits. `mergeProgress` unions keys blindly and will preserve
it forever; nothing renders it. That is the designed behaviour of a retirement and
it is what the other fifteen retired ids already do — but this is the first one in
the `vocab:` space, and no one has confirmed that `/core-vocabulary` ignores an
orphaned key rather than counting it.
