---
phase: 02.1-celpip-remaining-skills
plan: 12
subsystem: celpip
tags: [phase-gate, verification, originality, csp, requirements]
status: complete
requires:
  - 02.1-03 (scripts/verify-celpip-content.mts — the shared content harness)
  - 02.1-10 (Reading set 1 complete at 4/4 part shapes)
  - 02.1-11 (Listening set 1 complete at 6/6 part shapes)
provides:
  - the phase verification record (this file)
  - AGENTS.md describing the shipped CELPIP surface
  - per-criterion gate record in ROADMAP.md, with an explicit did-not-ship list
  - a `[~]` met-with-stated-limitation convention in REQUIREMENTS.md
affects:
  - .planning/REQUIREMENTS.md (CELPIP-06 … CELPIP-10 statuses)
  - .planning/ROADMAP.md (Phase 2.1 gate record)
  - AGENTS.md
tech-stack:
  added: []
  patterns:
    - "the served-header check reads the CSP off a running production server, never off next.config.ts"
    - "assertion counts are compared against both the phase baseline and the last recorded figure, so a removal is visible"
key-files:
  created:
    - .planning/phases/02.1-celpip-remaining-skills/02.1-12-SUMMARY.md
  modified:
    - src/lib/celpip/reading-set-1.ts
    - .planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md
    - AGENTS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
decisions:
  - "CELPIP-07 and CELPIP-08 stay [~] after the browser pass — both rest on hearing, and nobody has heard a recording play back"
  - "The four Speaking rubric axis names stay VERBATIM (user, 2026-07-31): they are how the exam is scored and renaming would hurt recognition on the day. A settled decision, not an open item"
  - "Positional prose fixed in objective-question explanations too, not just the drop-down blanks plan 10 covered — confirmed absent from the rendered DOM"
metrics:
  duration: ~50 min automated + browser pass
  completed: 2026-07-31
---

# Phase 2.1 Plan 12: Phase Verification Gate Summary

**Phase 2.1 closes. The automated gate passes completely, the originality constraint holds under
both grep and a reading, and all six success criteria are met — four observed directly in a
browser, two met with stated limitations. The phase's largest remaining debt is that nothing has
ever run on a phone or Safari.**

## What this run actually did

Task 1 (automated gate) ran in full. Task 2, the blocking human checkpoint, **was performed by
the user** against `npm run build && npm start`; its results are recorded below in the user's
terms, and nothing in this document claims an observation I made myself. Task 3 (statuses and
documentation) ran twice — once from the automated evidence, then revised from the browser pass.

**Headline:** all six criteria are met. Four were observed directly — Reading end to end on
desktop, the landing in full, cross-device persistence end to end, and originality by grep plus
a reading. Two — Listening and Speaking — are met with the limitations recorded under
phase-exit debt: their machinery, content and gates are all in place, but nobody has heard a
Speaking recording play back and the Listening results screen is unobserved.

## Task 1 — the automated gate

### The suite

| Gate | Result |
|------|--------|
| `npx tsc --noEmit` | **PASS** |
| `npm run lint` | **PASS** |
| `npm run build` | **PASS** — 113 static pages, all four CELPIP routes present |
| `verify-headers` | **PASS** — 24/24 |
| `verify-celpip-content` | **PASS** — 648/648 |
| `verify-merge` | **PASS** — 20,146/20,146 (phase baseline 4,313) |
| `verify-schema` | **PASS** — 309/309 (phase baseline 158) |
| `verify-celpip-speech` | **PASS** — 50/50 |
| `verify-queue` | **PASS** — 173/173 |
| `verify-celpip-sections` | **PASS** — 43/43 |

**No count went down.** Every figure is at the high-water mark recorded by the last plan that
touched it (T-02.1-58: assertions removed to make a proof pass would show here and do not).

### The grep gates

| Gate | Result |
|------|--------|
| Academy name (`IELTS…PTE…Viv`) in `src` | **0** |
| Six prep-site names in `src` | **0** |
| Official archive filenames in `src` | **0** |
| `dangerouslySetInnerHTML` in CELPIP components and routes | **0** |
| Recorder/blob/object-URL identifiers in the three persistence modules | **0** |
| Dependency counts | **11 prod / 11 dev** — exact |
| `package.json` + lockfile diff across the whole phase | **empty** (T-02.1-SC) |

All gate paths were confirmed non-vacuous — 11 CELPIP components, 5 routes and 7 content
modules exist under the greps' paths, so a zero is a real zero and not a missing directory.

I also ran a **broader sweep** than the plan required — the awarding body's own domain, two
publisher names, and the strings `copyright`, `all rights reserved`, `reproduced with
permission`, `adapted from`, `excerpt`. Three hits, all false positives from ordinary English
in code comments ("Not an excerpt", "NOT ADAPTED FROM A LISTENING SCRIPT", "the official
material settled it"). No attribution anywhere.

### The served production headers — the check no dev server can perform

`npm start`, then `curl -D -` against `/celpip`:

```
Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline'
  https://www.googletagmanager.com https://connect.facebook.net; style-src 'self'
  'unsafe-inline'; img-src 'self' data: https:; media-src 'self' blob:; font-src 'self'
  data:; connect-src 'self' …; frame-ancestors 'none'; base-uri 'self'; form-action 'self';
  object-src 'none'; upgrade-insecure-requests
Permissions-Policy: camera=(), geolocation=(), microphone=(self)
```

**`media-src 'self' blob:` is present in what the server actually sends** (T-02.1-57), which is
what lets the Speaking section play a recording back. `microphone=(self)` is there too. The
server was shut down and the port confirmed free; one orphaned `next start` wrapper survived
the first kill and was killed explicitly. **No stray server.**

### What else the running server proved

Because a production server was up anyway, I took more from it than the plan asked for:

- **Routes:** `/celpip`, `/celpip/reading/reading-set-1`, `/celpip/listening/listening-set-1`
  all 200; unknown ids 404.
- **D-04/D-05 at the served boundary (Listening):** of 142 authored script turns, **0** appear
  in the served HTML, and no speaker name leaks either; of 37 question stems, **0** appear.
  The learner cannot read ahead by viewing source.
- **RSC boundary (Reading):** 90 probed passage lines, stems and blank options — **0** leak.
- **Landing coverage lines, read off the server rather than the source:** Writing "17 prompts
  across the exam's two writing tasks", Reading "1 set covering 4 of the 4 exam part shapes",
  Listening "1 set covering 6 of the 6 exam part shapes", Speaking "8 prompts covering 8 of
  the 8 exam task shapes". All four are true.
- **Speaking timings, all eight prompts:** task 1 → 30/90, tasks 2/3/4/8 → 30/60, tasks 5/6 →
  60/60, task 7 → 30/90. Exactly the exam's confirmed table.

### The live-data property, named explicitly rather than left implied

`verify-schema` asserts, and this run re-ran, that **a blob stored before this phase — with
`listeningAttempts`, `readingAttempts` and the speaking fields all absent — parses to the same
writing attempts and drafts it always did, with all three new fields recovering to empty
records** (T-02.1-62). The beta user's live Writing history survives the schema change by
proof, not by hope. The remaining half of that criterion — seeing it intact on screen — is
part of the browser pass.

## The D-06 originality judgement — my honest read

The grep is the weaker half and the plan is right to say so. I read the authored prose rather
than scanning it: **the whole Speaking self-evaluation rubric** (all 16 items with their
explanations), **all four Reading parts in full** (passages, 23 stems, 15 blanks and all 38
explanations), the **Listening news item** in full and the **three-speaker discussion** in
full, and **all eight Speaking prompts** with their strategy tips.

**My judgement: this is original work, and I am not close to the line on it.** The reasons,
so you can check my reasoning rather than take my word:

- **It has an author's voice, sustained across 2,600 lines.** A lifted-and-reworded bank reads
  flat and interchangeable. This does not. Explanations argue: *"the grammar never tells you,
  the surrounding facts do"*, *"a pause between two ideas reads as composure; a pause
  mid-clause reads as being stuck"*, *"coherence is heard, not planned"*. That is somebody
  thinking, not somebody paraphrasing.
- **The content is load-bearing in ways copied text cannot be.** The correspondence blanks are
  answerable only by holding a fact from the first email while reading the second — the price,
  the head count, who dropped out. The viewpoints part is built on four positions whose
  agreements deliberately cut across each other. The discussion has a speaker who withdraws an
  argument mid-meeting when a number contradicts it. That architecture was designed here.
- **Every proper noun is invented** — Kettle Bend, Ellisburn, Bellwood, Harlow Bay, Nadia
  Farouk, Colin Beausoleil, Yusra Belkacem, the Ransome family. The real place names that do
  appear (Winnipeg, Meech Lake, Wakefield) are geography, used as setting.
- **The rubric in particular reads as ours.** The official level descriptors are third-person
  analytic bands. These are second-person questions answerable while listening back to a
  sixty-second recording — *"is there one word you reached for over and over — 'good',
  'thing', 'nice', 'a lot'?"*. That is a different rhetorical object, not a reworded one.

### The one thing I named — now settled by decision

> **RESOLVED 2026-07-31:** the user decided the four names **stay verbatim**. See the decision
> section below. What follows is the analysis that prompted the question.


**The rubric's four dimension names are the official CELPIP Speaking sub-score names.** "Task
Fulfilment", "Content & Coherence", "Vocabulary" and "Listenability" are the exam's own scoring
axes, taken over as labels.

My assessment is that this is **fine, and in the same category as everything else this phase
borrowed**: part names, item counts and timings. Short functional labels for scoring axes are
facts about how the test is marked, not protectable expression — and the whole app is built on
that class of fact. It would also be *worse* for the learner to invent different names, since
recognising the axes is part of what she is rehearsing. `rubric-speaking.ts` says all of this
in its own header, unprompted, which is the right instinct.

But it is the single place where something from the source material is reproduced verbatim
rather than rewritten, so it belonged in this record rather than in a footnote. **The user's
answer: they stay** — they are how the exam is scored, and renaming them would cost her
recognition on the day for no legal gain. Settled.

Everything else I read: original.

## Deviations from Plan

### [Rule 1 — Bug] Four explanations named an option by its position

- **Found during:** Task 1, checking the second of your two known corrections
- **Issue:** Plan 10 fixed positional prose for the fifteen **drop-down blanks**, whose options
  sit inside a `<select>` whose order the learner cannot see. It did not reach the **objective
  questions**. `rs1-corr-q2`, `rs1-info-q3`, `rs1-info-q5` and `rs1-view-q5` still said "The
  first option", "the second option", "The third option", "The last option". The file's own
  header states the rule without limiting it to blanks, so these violated it. The failure mode
  is the same one plan 10 was closing: the prose silently becomes false the moment anybody
  reorders the options it describes.
- **Fix:** All four now name the option by what it claims ("A dearer cabin is the one thing the
  email explicitly denies"). No answer key moved.
- **Files:** `src/lib/celpip/reading-set-1.ts` · **Commit:** `02f11a8`
- **Verified:** all 38 explanations scanned, **0 positional references remain**; 648 assertions,
  tsc and lint all still pass.

### [Rule 3 — Blocking/stale documentation] The research table had Reading part 2 backwards

- **Found during:** Task 1, checking the first of your two known corrections
- **Issue:** Confirmed exactly as you described. `02.1-RESEARCH.md` line 101 said the diagram
  part is "5 questions on the diagram + 3 drop-down blanks", citing a prep-site guide. The
  truth is **5 blanks + 3 questions**. Worse than a simple error: the Assumptions Log further
  down the *same file* already carried the corrected official figures, so the document
  contradicted itself and the stale half is the one a reader meets first.
- **Fix:** The row now states the correct split and carries a dated correction note pointing at
  the Assumptions Log.
- **The code was always right:** `DIAGRAM_PART` ships 5 blanks and 3 questions, and
  `READING_PART_SHAPE` gates it. Verified directly against the module.
- **Files:** `.planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md` · **Commit:** `8672bca`

### [Rule 1 — Stale record] CELPIP-10's traceability entry understated what shipped

- **Found during:** Task 3
- **Issue:** The entry still quoted Reading's coverage line as "1 set covering 2 of the 4 exam
  part shapes". The landing has served **4 of 4** since plan 10 — verified off the production
  server this run. The requirement record was more pessimistic than the product.
- **Fix:** Corrected, and noted that the line moved on its own because coverage is derived from
  bank contents rather than hand-written. That is the derivation doing its job.
- **Files:** `.planning/REQUIREMENTS.md` · **Commit:** `6a57beb`

### [Observation, not fixed] `planVoices` is all-or-nothing, and the discussion part is where it bites

Your concern is precise and the code confirms it. In `src/lib/celpip-speech.ts`:

```ts
const enoughVoices = distinct.length > 0 && pool.length >= distinct.length;
```

The discussion part has **three** distinct speakers. If the device offers only two English
voices, `enoughVoices` is false and **all three** speakers fall back to the browser default —
it does not use the two voices it has plus one pitch shift. They are then distinguished by
`PITCH_LADDER` alone: 1, 0.85, 1.15. **Six of that part's eight questions are attribution
questions.**

The authored insurance is real: plan 11 has all three speakers **named aloud within the first
seven turns**, each immediately before or after they first speak, so attribution stays
recoverable from content even if the voices are not recoverable by ear. I verified that in the
script.

I did not change this. **The browser pass could not test it either** — the machine had 5 English
voices, so three real voices were assigned and the fallback never engaged. The risk is now
*bounded* (it does not affect a well-equipped desktop) but *unexercised* on exactly the devices
where it bites. If it ever fails, the smallest honest fix is a partial assignment — use however
many real voices exist and pitch-shift only the remainder — rather than the current
all-or-nothing fallback.

### [Housekeeping] A stray zero-byte file

`C:\Repositorios\InglesAprender\3)` — 0 bytes, dated 2026-07-31 05:52, untracked, evidently a
malformed shell redirect from an earlier plan. **I left it in place** rather than deleting
something I did not create. It is safe to remove.

## Task 3 — statuses set from observation

| Req | Before | Now | Why |
|-----|--------|-----|-----|
| CELPIP-06 | `[x]` | `[x]` | **Strengthened by the browser pass** — the only section verified end to end on desktop. Render order confirmed by DOM, clocks re-arm, submission-with-blanks allowed, no positional prose on screen |
| CELPIP-07 | `[x]` | **`[~]`** | The requirement's own words are "scripts the learner **hears**". Questions were reached in a browser; the results screen, the transcript and the 55-minute clock were not |
| CELPIP-08 | `[x]` | **`[~]`** | "so the learner can **hear herself back**" — the tab and Task 3 caveat now render, but **no human has heard a recording play back**, on any prompt or device |
| CELPIP-09 | `[ ]` | **`[x]`** | **Observed end to end.** Local storage wiped entirely, everything restored from the server with per-skill metrics, Writing history untouched, 2,890 bytes and no audio payload. Went from the weakest-evidenced requirement to among the strongest |
| CELPIP-10 | `[x]` | `[x]` | **Fully observed.** Four real tabs, no "Coming soon", clean hydration, Task 3 caveat on screen; stale coverage figure corrected |

`[~]` is a new convention introduced here, defined in the file: *met with a stated limitation —
the artifact ships and its gates pass, but a criterion in the requirement's own wording rests on
an observation nobody has made yet.* Rounding those two up to `[x]` is exactly what the plan
told me not to do.

## Known Stubs

None introduced by this plan.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change.

## Task 2 — the browser pass (performed by the user, 2026-07-31)

Against `npm run build && npm start`. Recorded in the user's terms; I claim no observation here.

### Closed

**Landing (checklist item 1) — PASS.** All four tabs are real buttons that switch grids. **No
"Coming soon" or "Not yet available" anywhere.** Hydration clean. Speaking's Task 3 caveat
renders on screen. → closes `WINDOWS` 5, and the "never rendered" half of 7.

**Reading (items 5 and 6) — PASS, and both of plan 10's fixes are confirmed real.** This is the
only section verified end to end.

- **Render order confirmed by DOM position, not by reading source:** correspondence puts
  questions before blanks, matching its own instruction line.
- **Per-part clocks re-arm: 11:00 → 8:00 → 9:00 → 11:00.** Part 4 re-armed *despite sharing part
  1's 11-minute allowance* — that is precisely the latent bug `key={part.id}` was added for, and
  it means the fix is now proved rather than assumed. Total 39 minutes.
- Blanks distribute 5 / 5 / 0 / 5 selects across the four parts — the format's own shape.
- **Submission with items unanswered is allowed**: submitted at 2/38 and it graded.
- Results screen renders, scored 0/38, with 36 explanation blocks present.
- **No positional prose survives in the rendered DOM** — a regex for `the (first|second|third|
  fourth) option` returns nothing across the whole results page. My `02f11a8` fix holds where it
  matters, on screen.

→ closes `WINDOWS` 19 and 22, and the desktop half of 16.

Separately, `WINDOWS` 18 (reading id uniqueness across questions *and* blanks) turned out to
have been closed by plan 09's harness, which asserts it explicitly at
`verify-celpip-content.mts:891` and names itself "the one invariant plan 08 could not gate
itself". Marked fixed.

### Bounded, but not closed — the three-voice collapse

The test machine exposes **5 English voices** (two Canadian: Microsoft Richard and Linda), so
`planVoices` had three real voices to assign and **the collapse path did not engage**. That is
useful information and it is not a test of the thing at risk: below three English voices, all
three discussion speakers still become one voice at pitches 1 / 0.85 / 1.15, and six of that
part's eight items are attribution questions. Ledger entry kept open and narrowed to *untested
below three voices*.

### The Listening caveat — investigated rather than assumed

The user could not text-match a Listening "synthesised-audio" caveat and asked me to determine
whether it is worded differently or missing, and to say plainly which.

**It is worded differently. Nothing is missing — my paraphrase was wrong.** Evidence:

- `getSection('listening').coverage.caveat` returns a **344-character string**.
- It ships in the built client bundle — `.next/static/chunks/0iphvrge_jsif.js` — **the same
  chunk as the Speaking caveat the user watched render.**
- Both render through the same expression, `section?.coverage.caveat` at
  `CelpipLanding.tsx:256`, which is skill-agnostic. The Speaking caveat rendering is itself
  proof that path works.
- The strings `synthes`, `synthetic`, `TTS` and `robot` appear **nowhere** in it. It reads:
  *"The audio here is spoken by your browser rather than played from a recording, so it sounds
  more mechanical than the exam's and the voices are whatever your device offers…"*

I introduced "synthesised-audio caveat" as shorthand in the first gate record and the user
searched for my words rather than the product's. That phrasing is corrected in `ROADMAP.md` and
`REQUIREMENTS.md` so the mistake is not repeated. The only residual is a glance at the Listening
tab to confirm it on screen; there is no defect to fix.

### Criterion 4 — cross-device persistence, observed end to end

Production build, real Postgres, fresh account. This was the last criterion with no
observational support and it is now the best-evidenced of the six:

1. Attempts made in three sections — writing 2, speaking 1, listening 1.
2. **They reached the server:** `celpipProgress` held `attempts`, `speakingAttempts` and
   `listeningAttempts` at **2,890 bytes with no audio payload**. That is **T-02.1-59 confirmed
   on live data**, not by grep — audio bytes never entered the JSON column.
3. `fluentpath.celpip.v1` and the progress key were then **wiped entirely** — a second device
   by construction, and a stronger test than a second browser because it removes every local
   fallback the store could have hidden behind.
4. On reload everything came back from the server and rendered: *"Attempt history — 5
   attempts"*, each skill showing **its own metrics** — Listening "1/5 correct", Speaking
   "0:21 recorded · 2 self-checks ticked", Writing its own — not one shared metric leaking
   across skills. **The Writing history was untouched**: **T-02.1-62 confirmed against the live
   data it was written to protect.**
5. A subsequent full Reading attempt took the history to 6, topped by *"Reading Set 1 · 0:03
   used · 0/38 correct"*.

**All four new-skill sections round-trip.** CELPIP-09 set to complete.

### The finding that came with it — a limitation with teeth

The first Reading attempt **did not persist**, because the reviewer left the results screen by
navigating straight to sign-out rather than through an exit control.

**Close the tab from the results screen and the attempt is lost — including a full 39-minute
Reading sitting.** `finalizeAttempt` runs on results-view *exit* (Retry / Back to tasks), the
pattern Phase 1 chose for Writing; Speaking, Listening and Reading each inherited it, so **this
now spans all four skills rather than one.** Recorded as a known limitation and an improvement
candidate. **Not fixed here** — out of this plan's scope, and a phase should not grow at its
own gate.

**One correction for whoever does fix it, because the obvious reading is wrong.** It is true
that `ProgressSync.tsx:62-69` already wires `visibilitychange` and `pagehide` — but those flush
the sync **queue**: they push state that has *already been recorded*. On the results screen
`finalizeAttempt` has not run, so **there is nothing queued for them to flush.** The triggers
exist at the transport layer, not the record-the-attempt layer. The fix needs `finalizeAttempt`
itself wired to the same event pair inside `ReadingRunner`, `ListeningPlayer` and
`SpeakingRecorder`, minding `finalizedRef` and the reset-on-retry paths — a real change in
three components, not a wiring flip. It should use that pair and **not** `beforeunload`;
`ProgressSync.tsx:57-61` records why the unload-time events were deliberately avoided
(unreliable on mobile, and being removed).

### Phase-exit debt — carried forward, not blocking

**Nothing has ever run on a phone or on Safari, and of everything below this is the one that
matters.** She has a dated exam and is most likely to practise on the bus. Every mobile-specific
control built in this phase is unexercised on the devices it exists for: the MediaRecorder
WebM→MP4 container probe (written *precisely* for Safari before 18.4, which supports MP4 only),
the iPhone hardware-silent-switch guidance in the audio check, the native select as an
iOS/Android system picker, and whether four inline selects sit legibly in one paragraph at phone
widths. This is not one line on a checklist — it is a whole device class between the app and the
person it was built for.

Also open: no human has heard a Speaking recording play back; the Listening results screen,
post-answer transcript and 55-minute clock are unobserved; nobody has worked Reading set 1 at a
real pace; the OS mic indicator on stop is code-verified only; and the `planVoices` collapse is
untested below three English voices.

## Decision recorded: the rubric dimension names stay verbatim

**User, 2026-07-31.** "Task Fulfilment", "Content & Coherence", "Vocabulary" and "Listenability"
remain the official CELPIP Speaking sub-score names, unchanged: they are how she will be marked,
and renaming them would hurt her recognition on the day. This closes the one judgement call I
raised under D-06. It is the only verbatim borrowing in the app and it is now a deliberate,
recorded decision rather than an open question.

## Ledger movement

Started at 22 open. Now **20 open, 8 fixed, 28 total** — one new entry logged (the
finalize-on-results-exit limitation) and window 1 closed, the Phase 2 reconcile hand-check that
the cross-device pass superseded.

**Closed:** 5 (landing hydration and tab switch), 7 (Speaking tab and Task 3 caveat seen),
16 (reading runner seen, per-part clock re-arm), 18 (reading id uniqueness — was already gated
by plan 09's harness), 19 (drop-down blank answered), 22 (per-part item order rendered),
24 (the browser pass itself, superseded).

**Appended as narrowed successors**, because the ledger has no edit verb and a closed-then-
reopened claim would lose its precision: the Speaking record→playback half of 7, the mobile half
of 16, and a six-item list of exactly what the browser pass did not reach.

**Deliberately left open:** 10 (Listening results screen), 13 (55-minute clock), 14 (discussion
by ear), 21 (Reading at a real pace), 3 and 4 (phone/Safari, mic indicator), plus the gating
deviations 2, 6, 9, 11, 15, 17, 20 — those are harness gaps, and no amount of browser work
closes a missing gate.

## Self-Check: PASSED

Files verified present on disk:

- `.planning/phases/02.1-celpip-remaining-skills/02.1-12-SUMMARY.md` — FOUND
- `src/lib/celpip/reading-set-1.ts` — FOUND
- `AGENTS.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` — FOUND

Commits verified in `git log`:

- `02f11a8` fix(02.1-12): four explanations named an option by its position — FOUND
- `8672bca` docs(02.1-12): the research table had Reading part 2's split backwards — FOUND
- `6a57beb` docs(02.1-12): record what shipped, including what did not — FOUND

Claims verified by re-running after the last edit: tsc, lint, build, and all seven proof
scripts at 24 / 648 / 20,146 / 309 / 50 / 173 / 43.
