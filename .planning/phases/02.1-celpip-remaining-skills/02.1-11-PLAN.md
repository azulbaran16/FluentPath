---
phase: 02.1-celpip-remaining-skills
plan: 11
type: execute
wave: 8
depends_on: ["02.1-07"]
files_modified:
  - src/lib/celpip/listening-set-1.ts
autonomous: true
requirements: [CELPIP-07]

must_haves:
  truths:
    - "Listening set 1 covers all six of the exam's part shapes."
    - "The discussion part carries eight questions across roughly eight minutes — the confirmed exam figures."
    - "Three speakers are distinguishable, and at least half the questions require tracking who said what."
    - "Every question carries an in-range key and an original explanation, and no turn exceeds the word ceiling."
  artifacts:
    - src/lib/celpip/listening-set-1.ts (discussion part appended)
  key_links:
    - "turn.speaker -> the three-way voice assignment in celpip-speech.ts and the transcript labels"
    - "CELPIP_SECTIONS listening coverage -> reports six of six with no landing edit"
---

<objective>
Author the last listening part shape: the three-speaker discussion, eight questions, roughly
eight minutes.

Purpose: this part was originally scheduled last in the phase because its item count was in
open conflict between the third-party sources RESEARCH had to fall back on — two reported
eight items, one reported roughly two. **That conflict is resolved: the beta user's own
official format material confirms eight.** RESEARCH assumption A1 is closed, and this plan no
longer carries any uncertainty. What it still carries is cost: eight items with explanations
across three speakers is the second most expensive listening part, which is why it is not
first.

Because nothing else depends on it, this plan is scheduled in the same wave as plan 08 and
touches only the listening content module — so it can run in parallel with the Reading
plumbing, or be deferred to the end of the phase, without disturbing anything.

Output: Listening set 1 complete at six of six part shapes and 37 items.
(37, not 38: the official per-part counts are 8/5/6/5/8/5. The verify command in this
plan already asserts 37 — this line previously said 38 and was the outlier.)

**If execution stops before this plan runs:** Listening covers five of six part shapes and
the landing says so on its own, because its coverage line is derived from the bank. Nothing
is broken and nothing is overclaimed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-CONTEXT.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-07-SUMMARY.md

@src/lib/celpip.ts
@src/lib/celpip/listening-set-1.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The discussion part — three speakers, tracked attribution</name>
  <files>src/lib/celpip/listening-set-1.ts</files>
  <read_first>src/lib/celpip/listening-set-1.ts, src/lib/celpip.ts (the listening types), .planning/phases/02.1-celpip-remaining-skills/02.1-07-SUMMARY.md (the set's current item count and time limit)</read_first>
  <action>
    Append a part of kind `discussion` to Listening set 1: three speakers, one segment of
    roughly 300 to 380 words, and **eight** questions across a part of roughly eight minutes.
    Both figures are confirmed against the beta user's official format material — the earlier
    conflict between third-party sources over this part's item count is settled, and eight is
    correct.

    Name the three speakers distinctly so the voice assignment and the transcript labels have
    something to key on. Where the browser offers three distinct English voices the driver
    will use them; where it does not it falls back to a pitch offset plus the on-screen label,
    which is why the names matter even on a device with one voice.

    The part tests tracking who said what across three voices, so write genuine cross-talk:
    someone changing their mind, someone partly agreeing, someone restating another speaker's
    point before disagreeing with it. **At least four of the eight questions must require
    attribution rather than recall** — that is what makes this the hardest listening part and
    it is what she is here to practise.

    Turns at or under 25 words, and the harness fails the build at 35 — the ceiling that keeps
    an utterance below the engine's silent truncation point at the app's speaking rate. With
    three speakers alternating, turns are naturally short here, which is the one way this part
    is easier than the long monologues.

    Four options per question, in-range key, and an original explanation on every question
    naming which speaker's line settles it. D-06: every word original, written from the exam
    FORMAT only. The official material was read for structure — part names, item counts,
    timings — and **no sentence of it may appear anywhere in the app**.

    Update the set's `timeLimitMinutes` for the complete six-part set from the confirmed
    per-part allowances, and drop any provisional comment. Update the file's header note to
    record that the exam-format figures for this set are confirmed against official material
    rather than sourced from prep sites, so the next person to read it knows which kind of
    number they are looking at.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-celpip-content.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/celpip.ts').then(m=>{const s=m.LISTENING_SETS[0];const k=new Set(s.parts.map(p=>p.kind));const d=s.parts.find(p=>p.kind==='discussion');const sp=new Set(d?d.segments.flatMap(g=>g.turns.map(t=>t.speaker)):[]);const n=s.parts.reduce((t,p)=>t+p.questions.length,0);process.exit(k.size===6&&sp.size===3&&d.questions.length===8&&n===37?0:1)})"</automated>
    <human-check>Play the discussion part and confirm you can tell the three speakers apart well enough to answer an attribution question — by voice where the browser has three, and by label plus pitch offset where it does not.</human-check>
  </verify>
  <done>
    Six distinct listening part kinds present. The discussion part has exactly three distinct
    speakers and exactly eight questions. The landing's coverage line reports six of six with
    no edit to any component.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official format material -> authored content | Structure was read from a copyrighted document; only structure may cross, never text |
| authored script -> browser speech engine | An over-long turn is silently truncated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-52 | Repudiation / compliance | text from the official format material or the level descriptors leaking into the app | critical | mitigate | D-06: only item counts, timings and part names crossed over — those are structural facts, not expression. Every script, stem, option and explanation is written from scratch. The phase gate greps source and a reviewer reads complete scripts |
| T-02.1-53 | Denial of service | an over-long turn | high | mitigate | Harness gates every turn at 35 words; three-speaker cross-talk keeps turns naturally short |
| T-02.1-54 | Tampering | drifting from the confirmed item count | medium | mitigate | The harness asserts eight items for this part; the figure is settled fact, so a mismatch is a defect rather than a judgement call |
| T-02.1-55 | Denial of service | three speakers indistinguishable on a browser with fewer than three voices | medium | mitigate | Voice assignment is a preference with a pitch-offset and on-screen-label fallback, never a gate; a human listening pass is in the verify block |
| T-02.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages; this plan edits one content module |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-celpip-content.mts` passes and reports six part kinds for listening set 1.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` pass.
- `verify-merge` and `verify-schema` still pass at or above baseline.
</verification>

<success_criteria>
- CELPIP-07 is met at full part-shape coverage, at the confirmed exam item counts.
- Listening set 1 is complete and its time limit is derived from the confirmed per-part allowances.
</success_criteria>

<output>
Create `.planning/phases/02.1-celpip-remaining-skills/02.1-11-SUMMARY.md` when done.
Record the part id, the item count, the longest turn in words, and the set's final total.
</output>
