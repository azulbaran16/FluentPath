---
phase: 02.1-celpip-remaining-skills
plan: 01
subsystem: celpip-speaking
tags: [celpip, speaking, mediarecorder, csp, merge-algebra, persistence]
status: awaiting-verification
requires:
  - Phase 2 server-side progress contract (progress-schema.ts, progress-merge.ts, celpip-progress.ts)
  - Phase 1 CELPIP Writing components (Timer, RubricChecklist)
provides:
  - CelpipProgressState.speakingAttempts — append-only, per-prompt, merged by the existing unionRecord rule
  - canonicalEntries / entryRecord — generic over `{ date: string }`, reusable by Listening and Reading for one line each
  - CELPIP_SPEAKING_RUBRIC — the Speaking self-evaluation, Task Fulfilment dimension complete
  - media-src 'self' blob: in the served CSP, gated by scripts/verify-headers.mts
  - /celpip/speaking/{promptId}
affects:
  - next.config.ts (one new CSP directive)
  - src/lib/celpip/rubric.ts (CELPIP_RUBRIC gains a speaking key by reference)
tech-stack:
  added: []          # zero dependencies, by design
  patterns:
    - MediaRecorder container probed with isTypeSupported, container read back off the instance
    - getUserMedia from an explicit press only; every MediaStreamTrack stopped the instant recording ends
    - Blob and object URL held in refs for the session, revoked on unmount; only metadata persisted
    - Two-phase timer with the recording start driven by the prep countdown's expiry, not a button
key-files:
  created:
    - scripts/verify-headers.mts
    - src/lib/celpip/speaking-prompts.ts
    - src/lib/celpip/rubric-speaking.ts
    - src/components/celpip/SpeakingRecorder.tsx
    - src/app/(catalog)/celpip/speaking/[promptId]/page.tsx
  modified:
    - next.config.ts
    - src/lib/celpip.ts
    - src/lib/celpip/rubric.ts
    - src/lib/progress-schema.ts
    - src/lib/progress-merge.ts
    - src/lib/celpip-progress.ts
    - scripts/verify-merge.mts
    - scripts/verify-schema.mts
decisions:
  - "speakingAttempts is a separate top-level append-only field, never a widened taskType — the writing shape is byte-identical"
  - "`shape` is a bounded string with no literal list mirrored in progress-merge.ts, so a ninth exam shape can never delete a stored attempt"
  - "No new deletable map, so no second whole-field instant to fight the drafts rule"
  - "The response window is always mode=timed even in practice mode: pausing a countdown while the microphone keeps capturing would desynchronise the two"
  - "The microphone opens on the Start press and stays open through prep, so no permission dialog can eat the opening seconds; the UI says so in words because the OS indicator is lit"
metrics:
  tasks_completed: 2
  tasks_total: 3
  commits: 2
  files_changed: 13
  lines_added: 1788
  verify_merge: "4313 -> 6994"
  verify_schema: "158 -> 197"
  verify_headers: "24 (new)"
  verify_queue: "173 (unchanged)"
---

# Phase 02.1 Plan 01: CELPIP Speaking Tracer Summary

One CELPIP Speaking Task 1 prompt runs end to end — silent prep, an automatic
recording start, in-browser playback, a self-evaluation, and an attempt that reaches
Postgres through the unchanged Phase 2 merge contract — with all four of the phase's
architectural risks retired before a word of Reading or Listening content is authored.

## Status: awaiting the blocking human checkpoint

Tasks 1 and 2 are complete and committed. **Task 3 is a `checkpoint:human-verify` and has
NOT been performed** — it needs a real browser with a real microphone. Nothing below claims
that recording or playback was observed working; what was observed is recorded precisely.

## What shipped

### Task 1 — the CSP fix (commit `07c7986`)

`next.config.ts` gains exactly one directive: `media-src 'self' blob:`. The policy declared
no `media-src`, so it inherited `default-src 'self'`, and `'self'` does not match a `blob:`
URL — the object URL the recorder hands an `<audio>` element would have been refused in
production while working fine under `next dev`.

`scripts/verify-headers.mts` (new, 24 assertions) loads `next.config.ts` under
`--experimental-strip-types`, parses the served CSP into a directive map, and asserts the
widening stayed narrow: `blob:` is present in `media-src` and absent from `script-src` and
`connect-src`; `object-src 'none'`, `frame-ancestors 'none'` and `default-src 'self'` are
unchanged; `microphone=(self)` survives in `Permissions-Policy`.

Parsing into a directive map rather than grepping the whole string is deliberate — the
assertion "`blob:` appears somewhere in the CSP" is exactly the one that would pass while
`blob:` sat in `script-src`.

### Task 2 — the tracer (commit `d059139`)

**Content (all original, D-06).** One Task 1 `advice` prompt at 30s prep / 90s response, and
the Speaking rubric's Task Fulfilment dimension — four items, complete and final, not a
placeholder. Both files open with the originality header `tasks-email.ts` uses. Every rubric
id is prefixed `speaking-` so it cannot collide with an `email-` or `survey-` id in
`checkedRubric`.

**Types.** `CelpipSpeakingShape` and `CelpipSpeakingPrompt` in `src/lib/celpip.ts`, with
`SPEAKING_PROMPTS` / `getSpeakingPrompt` as siblings of `CELPIP_TASKS` / `getTask`.
`CelpipTaskType` was **not** widened. `CelpipRubric` gains a `speaking` key populated by
reference from `rubric-speaking.ts`, so `rubric.ts`'s originality claim still covers exactly
the text `rubric.ts` contains.

**Persistence — the six lockstep sites.** `speakingAttempts:
Record<string, CelpipSpeakingAttempt[]>` through `CelpipProgressState`, `CELPIP_EMPTY`,
`celpipProgressSchema`, `MERGE_CELPIP_EMPTY`, `celpipCoerce` and `mergeCelpip`. The merge
rule is one line — the same `unionRecord(x.f, y.f, (p, q) => canonical([...p, ...q]))` the
writing `attempts` field already uses, so it inherits that proof rather than needing a new one.

**The enum trap was not repeated.** `celpipAttemptEntry` hard-codes the writing task-type
pair independently of the zod enum, which is why a new `taskType` would validate on the wire
and then be silently deleted by the merge. `celpipSpeakingEntry` carries `shape` through as
`str(v.shape) ?? ""` and mirrors **no** list of shapes; the schema declares
`z.string().max(64)`, not an enum. Two assertions — one in each script — say so out loud.

**No second deletable map.** Speaking state is append-only with a natural key
(`promptId` NUL `date`), so it needs no `updatedAt` of its own and cannot fight
`pickDraftsSide` for the single instant this state carries.

**Generalisation for the plans that follow.** `canonicalAttempts` and `celpipAttemptRecord`
became `canonicalEntries` and `entryRecord`, generic over `{ date: string }` with an injected
key function. Behaviour is byte-identical: `verify-merge` reported **4313** immediately after
the refactor and before any new fixture. Listening and Reading now each add one entry
coercer, one key function and one merge line.

**Store, route, component.** `addSpeakingAttempt` routes through `persist` (the single site
that authors the D-01b instant). The route copies the writing route's shape exactly — awaited
`params` Promise, non-async `generateStaticParams`, `notFound()` called not returned, no
`"use client"`, the same `JsonLd` envelope. `SpeakingRecorder.tsx` is a `brief → prep →
respond → review` machine reusing `Timer` and `RubricChecklist` unchanged.

## Verification results — every command in the plan, run

| Command | Result |
|---|---|
| `node --experimental-strip-types scripts/verify-headers.mts` | **PASS** — 24/24 |
| `node --experimental-strip-types scripts/verify-merge.mts` | **PASS** — 6994/6994 (baseline 4313) |
| `node --experimental-strip-types scripts/verify-schema.mts` | **PASS** — 197/197 (baseline 158) |
| `node --experimental-strip-types scripts/verify-queue.mts` | **PASS** — 173/173 (unchanged) |
| `npx tsc --noEmit` | **PASS** — exit 0 |
| `npm run lint` | **PASS** — exit 0, no warnings |
| `npm run build` | **PASS** — exit 0 |
| persistence-layer no-audio grep gate | **PASS** — 0 matches |
| plan's merge gate (`>= 4313`) | **PASS** |
| plan's schema gate (`>= 158`) | **PASS** |
| dependency count (phase gate: 11 + 11) | **PASS** — 11 dependencies, 11 devDependencies, zero added |

Five new fixtures went into `celpipStates` rather than being one-off assertions, so the
O(n³) sweep exercises each against every other state under both bracketings: `speakingOnly`,
`bothSkills`, `speakingTwins`, `speakingSilentOnly`, and `legacyNoSpeakingField` — the last
being the beta user's stored row shape, with `speakingAttempts` *absent* rather than empty.

**RESEARCH assumption A6 closed by measurement, not by claim.** A worst-case realistic state
— 200 rehearsals across 8 prompts plus 40 essays, each rehearsal carrying a realistic note —
serializes to **120,646 bytes, 5.8% of the 2 MiB `MAX_BODY_BYTES`**. The number is printed
unconditionally by `verify-schema.mts` so a later shape change can be compared against it.
This matters because a 413 is classified as a permanent drop by the retry queue and would
discard the learner's entire CELPIP sync, writing history included.

## Mutation testing — and the three defects a first green run hid

A first-run green is not evidence, so every load-bearing rule was mutated. **24 deliberate
defects; 21 were caught immediately, 3 were not.** All three are now closed and re-tested.

An early batch appeared to find gaps that were not real: `progress-schema.ts` is CRLF while
`progress-merge.ts` is LF, so `\n`-anchored replacements against the schema silently no-op'd
and three "surviving" mutations had never been applied. The harness was rewritten to **fail
loudly when a mutation's anchor is not found or is ambiguous**, and every result below is
from that harness. One later mutation also proved to have hit the *writing* coercer instead
of the speaking one because the anchor matched two sites — hence the ambiguity check.

### Caught (21)

CSP: `media-src` dropped · `media-src` without `blob:` · `blob:` added to `script-src` ·
`object-src` loosened · `frame-ancestors` loosened · `default-src` widened · `microphone`
revoked.

Merge: an enum guard added to `celpipSpeakingEntry` · `speakingAttempts` dropped from
`mergeCelpip` / `MERGE_CELPIP_EMPTY` / `celpipCoerce` / `CELPIP_EMPTY` · `speakingAttempts`
selected whole-field on the shared drafts instant · sort removed from `canonicalEntries` ·
de-duplication removed · identity-less entries repaired instead of dropped · natural key
reduced to `promptId` · the coercer spreading unvalidated input · coerce-time canonicalisation
removed · `shape` defaulting to a fabricated value · `durationSeconds` / `sizeBytes` unclamped.

Schema: `speakingAttempts` dropped from `celpipProgressSchema` · `shape` tightened to an enum ·
`note` and `mimeType` unbounded.

### Survived, then closed (3)

1. **Unbounded numerics on the new shape.** Replacing
   `recordingSeconds: z.number().int().min(0).max(MAX_COUNT)` with `z.number()` produced zero
   failures — nothing asserted that a negative, fractional or 1e308 value was refused. Eight
   assertions added covering `recordingSeconds`, `durationSeconds` and `sizeBytes`; the
   mutation now fails, as do the same mutations on the other two fields.
2. **An unclamped `recordingSeconds` in the merge coercer.** No fixture fed the coercer a
   negative. A garbage-entry block now asserts every numeric is clamped, every string falls
   back, both flags coerce to booleans — and that normalising is idempotent, which is what
   stops the reconcile rewriting on every load.
3. **`checkedRubric` spread raw instead of rebuilt via `boolRecord` — in the WRITING coercer,
   and pre-existing.** No writing fixture had ever carried a non-boolean rubric value, so the
   prototype-pollution-adjacent pass-through went unnoticed. Closed with one assertion; see
   Deviations.

## Deviations from Plan

### [Rule 2 — missing validation coverage] Numeric bounds on `CelpipSpeakingAttempt`

- **Found during:** Task 2, by mutation.
- **Issue:** `verify-schema.mts` asserted the string bounds but not the numeric ones, so
  removing `.int().min(0).max(MAX_COUNT)` from any of the three number fields passed.
- **Fix:** Eight assertions covering negative, fractional, over-ceiling and non-finite values
  on `recordingSeconds`, `durationSeconds` and `sizeBytes`.
- **Files:** `scripts/verify-schema.mts` · **Commit:** `d059139`

### [Rule 2 — missing validation coverage] Coercer normalisation on the new shape

- **Found during:** Task 2, by mutation.
- **Fix:** A garbage-entry block in `verify-merge.mts` asserting clamping, string fallbacks,
  boolean coercion, rubric rebuilding, and idempotence of the whole normalisation.
- **Files:** `scripts/verify-merge.mts` · **Commit:** `d059139`

### [Rule 1 — pre-existing coverage gap in a file being modified] Writing rubric pass-through

- **Found during:** Task 2, by a mutation that hit `celpipAttemptEntry` rather than the
  speaking coercer.
- **Issue:** Replacing `boolRecord(v.checkedRubric)` with a raw cast in the **writing**
  coercer produced zero failures — no writing fixture carried a non-boolean rubric value.
  The module was already correct; only the proof was missing.
- **Fix:** One assertion that a writing attempt's non-boolean rubric values are dropped, with
  a comment recording how it was found. In scope because it is in a file this plan modifies
  and it guards the same "rebuild, never spread" property the new code depends on.
- **Files:** `scripts/verify-merge.mts` · **Commit:** `d059139`

### [Judgement call] The response window never pauses, even in practice mode

The plan says to reuse `Timer` unchanged for both countdowns. `Timer`'s practice mode renders
a Pause button, but pausing the countdown while `MediaRecorder` keeps capturing would
desynchronise the timer from the recording. The prep countdown receives the selected `mode`
(pausing to think is useful and harmless); the response countdown always receives
`mode="timed"`. The exam gives no pause there either. The brief states this in one line.

### [Judgement call] The microphone opens on Start and stays open through prep

The plan requires permission to be requested during the brief phase, `getUserMedia` to be
called only from an explicit press, and tracks to be stopped the instant recording ends. All
three are satisfied by acquiring the stream in the Start handler and holding it through prep.
The cost is that the OS microphone indicator is lit during the thinking window; rather than
hide that, the UI says it plainly — "your microphone indicator will be on through the thinking
time; nothing is captured until the countdown reaches zero".

### [Observation, not a deviation] The route is server-rendered on demand, not prerendered

The plan's done criterion says the build "pre-renders the speaking route". `npm run build`
lists `ƒ /celpip/speaking/[promptId]` — **exactly as it lists `ƒ /celpip/writing/[taskId]`**.
`generateStaticParams` is present and correct; the segment is dynamic for a reason that
predates this plan and applies equally to the shipped writing route. Behaviour matches the
established route; no change was made to chase the static marker.

## Threat mitigations applied

| Threat ID | Mitigation as built |
|---|---|
| T-02.1-01 | `getUserMedia` only in the Start handler; never in an effect. Tracks stopped in `recorder.onstop`, in `recorder.onerror`, on retry, and in the unmount cleanup. |
| T-02.1-02 | Blob and object URL in refs; `URL.revokeObjectURL` on unmount, on a new recording and on retry. Grep gate proves the persistence layer names no blob API. A merge assertion proves a client that *tries* to smuggle `audioBase64` / `blob` / `recordingUrl` fields has them dropped by the coercer. |
| T-02.1-03 | Only `media-src 'self' blob:` added; `verify-headers.mts` asserts the rest, and all seven weakening mutations fail it. |
| T-02.1-04 | Every string bounded (`mimeType` 64, `shape` 64, `note` at `CELPIP_MAX_TEXT`) and every number bounded; sizing closed by measurement at 5.8% of the cap. |
| T-02.1-05 | New maps go through `sanitizedRecord`/`sanitizedArray`; the merge coercer rebuilds rather than spreads. Poisoned-key assertions added. |
| T-02.1-06 | Separate append-only field; `shape` a bounded string with no mirrored list; fixtures in both scripts assert an unrecognised shape survives the merge and a second reconcile. |
| T-02.1-07 | `promptId` and `date` both required; identity-less entries dropped, with an assertion that a repeat merge cannot append a second copy. |
| T-02.1-SC | Zero packages installed. `package.json` still carries 11 dependencies and 11 devDependencies. |

## Known Stubs

None. The one prompt and the one rubric dimension are complete, final, original content — the
plan scopes the remaining prompts to plan 02 and the remaining three rubric dimensions to
plan 03. The route is reachable by direct URL only; plan 02 makes it discoverable from
`/celpip`, and the landing still shows Speaking as "Coming soon" until then.

## Self-Check: PASSED

All created files exist on disk; both commit hashes resolve in `git log`.

## What the checkpoint still has to establish

Everything below is browser-and-microphone dependent and was **not** verified here.

Already discharged against a real production build (`npm start`, not `next dev`):

- The served CSP contains `media-src 'self' blob:` and `script-src` contains no `blob:` —
  confirmed by `curl -sI http://localhost:3000/celpip`.
- `Permissions-Policy: camera=(), geolocation=(), microphone=(self)` is served.
- `/celpip/speaking/speaking-advice-first-winter` returns **200** and renders the prompt;
  an unknown prompt id returns **404**.

Still requiring a human at a browser:

1. Recording actually captures audio, and **playback plays it back** with no CSP violation in
   the console.
2. The OS microphone indicator turns **off** the moment recording stops — not on navigation.
3. Reload after finishing pre-fills nothing and the recording is gone.
4. Signed in, the attempt appears in a second browser on the same account.
5. A pass on a phone, where the container probe matters most.
