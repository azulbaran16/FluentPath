---
phase: 02.1-celpip-remaining-skills
plan: 07
type: execute
wave: 7
depends_on: ["02.1-06"]
files_modified:
  - src/lib/celpip/listening-set-1.ts
autonomous: true
requirements: [CELPIP-07]

must_haves:
  truths:
    - "Listening set 1 covers the information part — a structured spoken explanation testing sequence and detail."
    - "Listening set 1 covers the viewpoints part — a longer talk where the learner has to separate fact from opinion."
    - "Five of the exam's six listening part shapes are available; only the discussion part remains, and plan 11 authors it."
    - "Every new question carries an in-range key and an original explanation, and no turn exceeds the word ceiling."
  artifacts:
    - src/lib/celpip/listening-set-1.ts (information and viewpoints parts appended)
  key_links:
    - "CELPIP_SECTIONS listening coverage <- part kinds present in the set (updates with no landing edit)"
---

<objective>
Author the two longest single-speaker parts of Listening set 1: the information part and the
viewpoints part. Pure content, appended to an existing array, zero code risk.

Purpose: these are the two parts that train sustained attention to a monologue — the format
the beta user is least likely to have practised, because there is nothing on paper that
simulates it. Completing them takes the set to five of the exam's six part shapes, with only
the discussion part outstanding (plan 11).

Output: Listening set 1 covering five part shapes and roughly 30 items.

**Calendar note — this is the most droppable plan in the phase.** If time is short, the
viewpoints part in particular can be deferred and plans 08 and 09 (which give Reading its
first usable content) promoted ahead of it. That reordering is safe: nothing in Reading
depends on anything here, and the landing's coverage line is derived, so dropping a part
leaves no false claim on screen.

**If execution stops right after this plan:** the learner has five of six listening part
shapes and all eight Speaking task shapes, and no Reading — which is why Reading starts next
and the sixth listening part (plan 11) runs alongside it rather than before it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-CONTEXT.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-06-SUMMARY.md

@src/lib/celpip.ts
@src/lib/celpip/listening-set-1.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The information part — a structured spoken explanation</name>
  <files>src/lib/celpip/listening-set-1.ts</files>
  <read_first>src/lib/celpip/listening-set-1.ts, .planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md (the Listening table and the script-length targets)</read_first>
  <action>
    Append a part of kind `information`. One speaker, one segment of roughly 300 to 380 words,
    **six** questions across a six-to-seven-minute part — both figures confirmed against the
    beta user's official format material. The script is a structured explanation with a clear internal order — steps,
    stages, categories — because sequencing is what this part tests alongside detail. Write at
    least two questions that can only be answered by having tracked the order, not just the
    facts.

    **Do not reuse or adapt a listening script as a reading passage, or the reverse.** A spoken
    talk and an information text read differently, and reusing one as the other would blunt
    exactly the format fidelity D-07 is buying. This is named here so it is rejected on the
    record rather than rediscovered under time pressure.

    Turns at or under 25 words; the harness fails the build at 35. Break the monologue into
    consecutive turns by the same speaker — the chunking is what protects playback, not the
    speaker count.

    Four options per question, in-range key, an original explanation on every question. D-06:
    every word original, written from the exam FORMAT only.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-celpip-content.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/celpip.ts').then(m=>{const p=m.LISTENING_SETS.flatMap(s=>s.parts).find(x=>x.kind==='information');process.exit(p&&p.questions.length===6&&p.questions.every(q=>q.explanation&&q.explanation.length>20)?0:1)})"</automated>
  </verify>
  <done>
    The information part exists with six explained questions and every turn within the ceiling.
  </done>
</task>

<task type="auto">
  <name>Task 2: The viewpoints part — fact against opinion</name>
  <files>src/lib/celpip/listening-set-1.ts</files>
  <read_first>src/lib/celpip/listening-set-1.ts</read_first>
  <action>
    Append a part of kind `viewpoints`. One speaker, one segment of roughly 450 words — the
    longest script in the set — and **five** questions across an eight-minute part. Both
    figures are confirmed against the beta user's official format material; note that this
    part is the longest audio in the set and carries the *fewest* items, which is exactly why
    it is hard. The speaker states some things as fact and
    others as personal position, and the questions test the learner's ability to tell which is
    which and to attribute a position correctly. At least three of the five questions must
    hinge on that distinction rather than on recall; a viewpoints part answered entirely by
    recall is the wrong part shape wearing the right label.

    Same constraints: turns at or under 25 words, four options per question, in-range key, an
    original explanation on every question, D-06 originality throughout.

    Update the set's `timeLimitMinutes` to reflect five parts. At this point the figure stops
    being a placeholder for most of the set — comment what it is derived from (total script
    duration at the app's speaking rate plus answering time) and note that it will move once
    the discussion part lands.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-celpip-content.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/celpip.ts').then(m=>{const s=m.LISTENING_SETS[0];const k=new Set(s.parts.map(p=>p.kind));process.exit(k.size===5&&!k.has('discussion')?0:1)})"</automated>
    <human-check>Run the full set end to end in a built server and time it. Confirm the timer's total is not wildly shorter than the audio takes to play — that is the one thing no script can check.</human-check>
  </verify>
  <done>
    Five distinct part kinds present, the discussion part explicitly still absent, and the
    set's time limit derived rather than guessed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored script -> browser speech engine | An over-long turn is silently truncated and the questions never appear |
| authored content -> public app | Original prose ships publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-32 | Denial of service | an over-long turn in the 450-word viewpoints script | high | mitigate | Harness gates every turn at 35 words; the longest script in the set is the highest-risk one and is authored as many short turns |
| T-02.1-33 | Denial of service | a time limit shorter than the audio | medium | mitigate | Time limit derived from total script duration at the app's speaking rate plus answering time; a human timing check is in the verify block because no script can catch it |
| T-02.1-34 | Repudiation / compliance | reusing a listening script as a reading passage, or copied third-party text | high | mitigate | D-06; script reuse across skills explicitly rejected on the record; phase-gate grep plus reviewer read-through |
| T-02.1-35 | Denial of service | a question with an out-of-range key | medium | mitigate | Harness asserts every answer index is valid within its own options array |
| T-02.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages; this plan edits one content module |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-celpip-content.mts` passes and reports five part kinds for set 1.
- `npx tsc --noEmit` and `npm run build` pass.
- `verify-merge` and `verify-schema` still pass at or above baseline.
</verification>

<success_criteria>
- Five of the exam's six listening part shapes are practisable in the real format, at the confirmed item counts.
- The remaining shape is scheduled in plan 11, and until it lands the landing says five of six on its own.
</success_criteria>

<output>
Create `.planning/phases/02.1-celpip-remaining-skills/02.1-07-SUMMARY.md` when done.
Record the item count for the set so far, the measured wall-clock playback time, and the
time limit chosen.
</output>
