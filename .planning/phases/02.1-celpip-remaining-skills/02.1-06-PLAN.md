---
phase: 02.1-celpip-remaining-skills
plan: 06
type: execute
wave: 6
depends_on: ["02.1-05"]
files_modified:
  - src/lib/celpip/listening-set-1.ts
autonomous: true
requirements: [CELPIP-07]

must_haves:
  truths:
    - "Listening set 1 covers the problem-solving part, where each segment is followed by its own questions rather than the whole part being followed by all of them."
    - "Listening set 1 covers the daily-life conversation part, with two speakers the learner can tell apart."
    - "Every new question carries an in-range key and an original explanation."
    - "No turn exceeds the word ceiling that keeps an utterance under the engine's truncation point."
  artifacts:
    - src/lib/celpip/listening-set-1.ts (problem-solving and daily-conversation parts appended)
  key_links:
    - "question.segmentId -> the segment it follows (problem-solving is the only multi-segment part)"
    - "turn.speaker -> the voice assignment in celpip-speech.ts and the transcript label"
---

<objective>
Author two more parts of Listening set 1: problem solving and the daily-life conversation.
Pure content, appended to an existing array, zero code risk.

Purpose: these are the two conversational part shapes, and they are the ones the speaker-turn
model was built for — the problem-solving part is the only one in the exam where questions
follow *each* segment rather than the whole part, and the conversation part is where telling
two voices apart is the skill under test. Between them they add 13 items to the set.

Output: two more part shapes available, taking the set from one of six shapes to three.

**If execution stops right after this plan:** the learner has three of the six listening part
shapes, all eight Speaking task shapes, and nothing in Reading. The landing reports that
accurately without any edit, because the coverage line is derived from the bank.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-CONTEXT.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md
@.planning/phases/02.1-celpip-remaining-skills/02.1-05-SUMMARY.md

@src/lib/celpip.ts
@src/lib/celpip/listening-set-1.ts
@src/lib/celpip/tasks-email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: The problem-solving part — three segments, questions after each</name>
  <files>src/lib/celpip/listening-set-1.ts</files>
  <read_first>src/lib/celpip/listening-set-1.ts, src/lib/celpip.ts (the listening types), .planning/phases/02.1-celpip-remaining-skills/02.1-RESEARCH.md (the Listening table and the script-length targets)</read_first>
  <action>
    Append a part of kind `problem-solving` to Listening set 1. Two speakers, one male-coded
    and one female-coded by name so the voice assignment has something to key on. Three
    segments of roughly 200 to 250 words each, and **eight** questions in total distributed
    across the segments — each question carrying the `segmentId` of the segment it follows.
    Eight items and a six-to-seven-minute part total are confirmed against the beta user's
    official format material, not estimated; the harness gates the count.
    This is the only part in the exam where questions follow each segment rather than the
    whole part, and the player already handles it because every part is modelled as an array
    of segments; the only thing that makes it work is that each question names its segment.

    The content is a practical problem being worked through: numbers, times, procedures,
    arrangements. That is what this part tests, and it is what makes the note-taking area
    earn its place.

    Every turn stays at or under 25 words, and the harness fails the build at 35 — that is
    the ceiling that keeps a single utterance below the engine's silent truncation point at
    the app's speaking rate. Break a long speech into several consecutive turns by the same
    speaker rather than writing one long one.

    Every question gets four options, an in-range answer index, and an original explanation
    saying why the key is right and, where it helps, why the most tempting distractor is not.

    D-06: every word original, written from the exam FORMAT only. Nothing copied from any prep
    site or from study material the beta user owns.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-celpip-content.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/celpip.ts').then(m=>{const p=m.LISTENING_SETS.flatMap(s=>s.parts).find(x=>x.kind==='problem-solving');process.exit(p&&p.segments.length===3&&p.questions.length===8&&p.questions.every(q=>p.segments.some(s=>s.id===q.segmentId))?0:1)})"</automated>
  </verify>
  <done>
    Three segments, eight questions, every question bound to a real segment id, every turn
    within the ceiling, every explanation present.
  </done>
</task>

<task type="auto">
  <name>Task 2: The daily-life conversation part</name>
  <files>src/lib/celpip/listening-set-1.ts</files>
  <read_first>src/lib/celpip/listening-set-1.ts</read_first>
  <action>
    Append a part of kind `daily-conversation`. Two speakers, one segment of roughly 250 to
    300 words in a casual register, and **five** questions across a five-to-six-minute part —
    both figures confirmed against official format material. Unlike the problem-solving part, this
    one tests implied meaning as much as stated fact — what someone is hinting at, what they
    have decided without saying so outright. Write at least two of the five questions against
    implication rather than recall, because a conversation part answered entirely by recall is
    not the part shape it claims to be.

    Same constraints as task 1: turns at or under 25 words, four options per question, in-range
    key, an original explanation on every question, and D-06 originality throughout.

    Update the set's `timeLimitMinutes` to reflect the three parts now present, and keep the
    comment noting that the figure is provisional while the set is incomplete.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-celpip-content.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run build</automated>
    <automated>node --experimental-strip-types -e "import('./src/lib/celpip.ts').then(m=>{const s=m.LISTENING_SETS[0];const kinds=new Set(s.parts.map(p=>p.kind));process.exit(kinds.size===3&&kinds.has('daily-conversation')?0:1)})"</automated>
    <human-check>Play the conversation part in a browser and confirm the two speakers are distinguishable — by voice where the browser has two, and by label plus pitch offset where it does not.</human-check>
  </verify>
  <done>
    Three distinct part kinds in the set, thirteen new questions total across this plan, and
    the landing's derived coverage line updates on its own.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored script -> browser speech engine | An over-long turn is silently truncated, and under D-05 that means the questions never appear |
| authored content -> public app | Original prose ships publicly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02.1-28 | Denial of service | an over-long turn | high | mitigate | Harness gates every turn at 35 words and the build fails; authoring target is 25 |
| T-02.1-29 | Tampering | a question bound to a segment that does not exist | medium | mitigate | Harness asserts every `segmentId` resolves within its own part; a dangling id would hide a question forever |
| T-02.1-30 | Repudiation / compliance | copied third-party script or item | high | mitigate | D-06: all content original; originality header maintained; phase-gate grep plus reviewer read-through |
| T-02.1-31 | Denial of service | a question with an out-of-range key | medium | mitigate | Harness asserts every answer index is a valid index into its own options array |
| T-02.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages; this plan edits one content module |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-celpip-content.mts` passes.
- `npx tsc --noEmit` and `npm run build` pass.
- `verify-merge` and `verify-schema` still pass at or above baseline (per-commit sampling rate).
</verification>

<success_criteria>
- Listening set 1 covers three of the exam's six part shapes, including both conversational shapes.
- Adding these parts required no change outside one content module.
</success_criteria>

<output>
Create `.planning/phases/02.1-celpip-remaining-skills/02.1-06-SUMMARY.md` when done.
Record the part ids, the item counts, and the longest turn in words.
</output>
