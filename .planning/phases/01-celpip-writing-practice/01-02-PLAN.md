---
phase: 01-celpip-writing-practice
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/celpip/tasks-email.ts
autonomous: true
requirements: [CELPIP-01]
must_haves:
  truths:
    - "The Task 1 library offers ~8 original formal-email tasks with everyday-Canadian themes (work, housing, services, community)."
    - "Every email task has a scenario, exactly 3 bullet points to address, a 27-minute limit, a 150–200 word range, an original model answer, and rewritten strategy tips."
    - "No text is copied from the third-party study material or official CELPIP PDFs — every model answer and tip is original."
    - "The task library ships pre-seeded and is never empty at runtime, so no empty-state is needed for the card grid."
  artifacts:
    - path: src/lib/celpip/tasks-email.ts
      provides: "EMAIL_TASKS — ~8 original CelpipWritingTask entries (taskType email)"
  key_links:
    - "EMAIL_TASKS ids must be unique across EMAIL_TASKS and SURVEY_TASKS — getTask and generateStaticParams key on id, so a collision silently drops a route."
---

<objective>
Fill the Task 1 (formal email) task bank to ~8 original entries so the simulator has real, exam-faithful content to practice against. Pure content authoring against the CelpipWritingTask type the tracer established.

Purpose: deliver CELPIP-01's Task 1 half — original email prompts + model answers + strategy tips.
Output: `src/lib/celpip/tasks-email.ts` with ~8 tasks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-celpip-writing-practice/01-CONTEXT.md
@.planning/phases/01-celpip-writing-practice/01-UI-SPEC.md
@src/lib/celpip.ts
@src/lib/celpip/tasks-email.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 workplace & services email tasks</name>
  <files>src/lib/celpip/tasks-email.ts</files>
  <read_first>
    - src/lib/celpip.ts (the CelpipWritingTask type and its exact field names/optionality — author must conform)
    - src/lib/celpip/tasks-email.ts (the seed entry from plan 01 — match its style and id convention)
    - src/lib/content/writing.ts (tone/register precedent for original model answers in this codebase)
    - .planning/phases/01-celpip-writing-practice/01-CONTEXT.md (Task 1 format reference, IP constraints — format/strategy reference only)
  </read_first>
  <action>
    Extend `EMAIL_TASKS` (keeping the plan-01 seed) with 4 additional original Task 1 formal-email tasks, ids like "email-02" … "email-05" (ensure ids are unique and stable — they become route params). Themes: a workplace request/complaint, a service issue (utility/telecom/repair), a request to a manager or HR, and a booking/appointment matter. Each entry: taskType "email"; a concrete scenario paragraph; exactly 3 `bullets` (distinct points to address); timeLimitMinutes 27; wordRange { min: 150, max: 200 }; an original 150–200 word modelAnswer following formal-email format (greeting, purpose-first opening, one paragraph per bullet, context-referencing close, sign-off) — write it yourself, copy nothing from any third-party source; and 2–3 original strategyTips. Everyday-Canadian register, natural and idiomatic.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node -e "const {EMAIL_TASKS}=require('./src/lib/celpip/tasks-email.ts')" 2>/dev/null || npx tsc --noEmit -p tsconfig.json</automated>
    <automated>grep -rniE "IELTS.?.?PTE.?with.?Viv" src/lib/celpip | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - After this task `EMAIL_TASKS` has ≥5 entries (seed + 4), each with taskType "email", 3 bullets, timeLimitMinutes 27, wordRange { min: 150, max: 200 }.
    - Every modelAnswer word count is within 150–200 words.
    - All ids are unique within the file.
    - `npm run build` exits 0.
    - No third-party academy name appears anywhere under src/lib/celpip (grep count 0).
  </acceptance_criteria>
  <done>Four original workplace/services email tasks exist alongside the seed, all typed correctly and building clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author 3–4 housing & community email tasks (reach ~8 total)</name>
  <files>src/lib/celpip/tasks-email.ts</files>
  <read_first>
    - src/lib/celpip.ts (CelpipWritingTask type)
    - src/lib/celpip/tasks-email.ts (entries authored in Task 1 — continue the same style and id sequence)
    - .planning/phases/01-celpip-writing-practice/01-CONTEXT.md (IP constraints — hard)
  </read_first>
  <action>
    Add 3–4 more original Task 1 email tasks (ids "email-06" … "email-08"/"email-09") to reach ~8 total in `EMAIL_TASKS`. Themes: landlord/housing matter (repair request, lease question), a community/neighbour matter (noise, shared space, local event), and a school/childcare or bank/services letter. Same field contract as Task 1: 3 bullets, timeLimitMinutes 27, wordRange 150–200, original 150–200 word modelAnswer, original strategyTips. Keep every model answer original — write from the formal-email format only.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -rniE "IELTS.?.?PTE.?with.?Viv" src/lib/celpip | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `EMAIL_TASKS` totals 7–9 entries (target ~8), all taskType "email", all with 3 bullets / 27 min / 150–200 range.
    - Every modelAnswer is 150–200 words and reads as original formal-email prose.
    - All ids unique across the file; `npm run build` exits 0.
    - No third-party academy name under src/lib/celpip (grep count 0).
  </acceptance_criteria>
  <done>The Task 1 email bank holds ~8 original tasks; the /celpip Task 1 library will render a full grid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → app bundle | Original static content; the risk here is IP contamination, not runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-IP | Repudiation/Compliance | EMAIL_TASKS model answers & tips | high | mitigate | All content authored original from exam FORMAT only; automated grep blocks any third-party academy name; reviewer confirms no copied text (Phase success criterion 5) |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — no new libraries |
</threat_model>

<verification>
- `npm run build` exits 0 with the enlarged bank.
- IP grep over src/lib/celpip returns 0 hits for the third-party academy name.
</verification>

<success_criteria>
~8 original Task 1 formal-email tasks exist in EMAIL_TASKS, fully typed, exam-faithful, and free of any third-party text.
</success_criteria>

## Artifacts this phase produces
This plan produces: `EMAIL_TASKS` (~8 entries) in `src/lib/celpip/tasks-email.ts`. It consumes the phase types (`CelpipWritingTask`) from `src/lib/celpip.ts` created by plan 01. See plan 01-01 for the full phase artifact roster (types, storage, routes, components, storage key `fluentpath.celpip.v1`).

<output>
Create `.planning/phases/01-celpip-writing-practice/01-02-SUMMARY.md` when done
</output>
