---
phase: 01-celpip-writing-practice
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(catalog)/celpip/page.tsx
  - src/components/celpip/TaskCard.tsx
  - src/components/celpip/CelpipTabs.tsx
  - src/components/celpip/CelpipLanding.tsx
autonomous: true
requirements: [CELPIP-04, CELPIP-05]
must_haves:
  truths:
    - "The /celpip landing is free for all users (no account required) and explains what CELPIP Writing practice is in a compact hero that keeps the task cards above the fold."
    - "The landing shows Task 1 and Task 2 as active tabs (sky accent) plus disabled Speaking/Reading/Listening tabs that stay visible with a Coming soon badge."
    - "Each task type renders a grid of task cards; each card shows title, a scenario snippet, the time limit, and an attempt-status indicator (not started / in progress / completed)."
    - "Long task titles or scenario text truncate to two lines (line-clamp-2) on the card, with the full text shown on the simulator page."
    - "The coming-soon tabs wrap to a second row on narrow mobile widths and are never horizontally clipped."
    - "Attempt history renders reverse-chronologically; each row shows date, duration used, and word count, with a link into that attempt's results."
    - "With zero attempts the history shows the empty-state copy No attempts yet and the supporting body text; one attempt reads 1 attempt and many read {n} attempts."
    - "A dismissible, non-blocking notice tells mobile users the real exam runs on desktop."
    - "The task library ships pre-seeded (~8 Task 1 + ~8 Task 2) and is never empty at runtime, so the card grid needs no empty state."
  artifacts:
    - path: src/app/(catalog)/celpip/page.tsx
      provides: "Server landing route: generateMetadata (canonical + openGraph), JsonLd, compact hero, renders CelpipLanding"
    - path: src/components/celpip/TaskCard.tsx
      provides: "WorldCard-style task card with type icon, snippet, time limit, ProgressRing attempt status"
    - path: src/components/celpip/CelpipTabs.tsx
      provides: "Task 1 / Task 2 active tabs + disabled coming-soon Speaking/Reading/Listening tabs"
    - path: src/components/celpip/CelpipLanding.tsx
      provides: "Client landing body: tabs, card grids, attempt history + empty state, mobile notice"
  key_links:
    - "CelpipLanding uses useCelpipProgress selectors (attemptsForTask, completedTasks) — attempt status/history depend on the store from plan 01."
    - "TaskCard Link → /celpip/writing/[task.id] — the only navigation into the simulator."
---

<objective>
Build the free `/celpip` landing: what the section is, Task 1 / Task 2 task libraries with per-task attempt status, disabled coming-soon tabs for the other skills, attempt history with a proper empty state, and a mobile desktop-recommendation notice. This delivers CELPIP-05 (landing) and the attempt-history UI half of CELPIP-04.

Purpose: give learners a real entry point that shows their progress and routes them into any of the ~16 tasks.
Output: the full landing page plus TaskCard, CelpipTabs, and CelpipLanding components.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-celpip-writing-practice/01-CONTEXT.md
@.planning/phases/01-celpip-writing-practice/01-UI-SPEC.md
@.planning/phases/01-celpip-writing-practice/01-PATTERNS.md
@src/lib/celpip.ts
@src/lib/celpip-progress.ts
@src/app/(catalog)/celpip/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskCard + CelpipTabs components</name>
  <files>src/components/celpip/TaskCard.tsx, src/components/celpip/CelpipTabs.tsx</files>
  <read_first>
    - src/components/WorldCard.tsx (Link + accent + ProgressRing + hover-lift composition to mirror)
    - src/components/ProgressRing.tsx (value 0–100, color prop)
    - src/components/SkillPill.tsx (LevelBadge / small-badge pattern for the status badge)
    - src/lib/icons.tsx (icon wrapper pattern, strokeWidth 1.75 — pick lucide icons for email vs survey)
    - .planning/phases/01-celpip-writing-practice/01-UI-SPEC.md (Color: --sky accent + reserved uses; Copywriting: Coming soon; task-card truncation line-clamp-2)
    - .planning/phases/01-celpip-writing-practice/01-PATTERNS.md (TaskCard + disabled-tab guidance)
  </read_first>
  <action>
    Create `src/components/celpip/TaskCard.tsx`: a Link to `/celpip/writing/${task.id}` styled like WorldCard (rounded border, bg-card, shadow-soft, hover lift, rise animation with index delay). Show a task-type icon chip using the color-mix accent pattern with var(--sky), the task title, a scenario snippet truncated with line-clamp-2, the time limit (e.g. "27 min"), and an attempt-status indicator: a ProgressRing with color var(--sky) plus a small badge reading "Not started" / "In progress" / "Completed". Props accept the task plus a status value and attempt count so the card stays presentational (status is computed by CelpipLanding).

    Create `src/components/celpip/CelpipTabs.tsx`: a pill/tab row. Active tabs "Task 1" and "Task 2" use the --sky accent for the selected one (base the active/inactive pill on WritingDesk's picker row). Add three disabled tabs — Speaking, Reading, Listening — each visibly muted, cursor-not-allowed, non-interactive, with a small "Coming soon" badge; keep them visible (not hidden). The row uses flex-wrap so tabs drop to a second line on narrow mobile widths instead of clipping.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "line-clamp-2" src/components/celpip/TaskCard.tsx | grep -qvx 0</automated>
    <automated>grep -c "flex-wrap" src/components/celpip/CelpipTabs.tsx | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - `TaskCard.tsx` links to `/celpip/writing/${task.id}`, truncates the snippet with line-clamp-2, and renders a ProgressRing with color var(--sky) plus a status badge.
    - `CelpipTabs.tsx` renders Task 1 / Task 2 active tabs and three disabled Coming soon tabs (Speaking/Reading/Listening) inside a flex-wrap row.
    - Accent color used is var(--sky) only (no new color token introduced).
    - `npm run build` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>A reusable task card and the tab bar (with coming-soon tabs) exist and build clean.</done>
</task>

<task type="auto">
  <name>Task 2: CelpipLanding client body — grids, attempt history, mobile notice</name>
  <files>src/components/celpip/CelpipLanding.tsx</files>
  <read_first>
    - src/lib/celpip.ts (getTasksByType, CELPIP_TASK_META)
    - src/lib/celpip-progress.ts (useCelpipProgress: attemptsForTask, lastAttempt, completedTasks, ready)
    - src/components/celpip/TaskCard.tsx, src/components/celpip/CelpipTabs.tsx (the components to compose)
    - .planning/phases/01-celpip-writing-practice/01-UI-SPEC.md (Copywriting: empty-state heading/body, mobile notice, zero-one-many attempt counts; Visual hierarchy: card grid is the primary anchor)
  </read_first>
  <action>
    Create `src/components/celpip/CelpipLanding.tsx` ("use client"): uses useCelpipProgress. Renders CelpipTabs with local active-tab state (default Task 1). For the active tab, render a responsive grid of TaskCard for getTasksByType(active), computing each card's status from the store — no attempts → "Not started", attempts but not completed → "In progress", completed (in completedTasks) → "Completed" — and passing the attempt count. Below the grid, an attempt-history section: gather all attempts across tasks, order most-recent-first, and render each row with the task title, date, duration used, and word count, linking into that attempt's results view (/celpip/writing/[taskId] with the attempt selected). When there are zero attempts, show the empty state — heading "No attempts yet" and body "Pick a task below and complete your first timed practice — your attempt history will show up here." Label the count as "1 attempt" for one and "{n} attempts" for many. Add a dismissible, non-blocking mobile notice with the copy "The real CELPIP exam runs on a desktop computer — for the closest simulation, try this on a larger screen. You can still practice here." Guard against the pre-hydration state using the store's `ready` flag so SSR renders the empty baseline. Render all dynamic text through JSX interpolation only.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "No attempts yet" src/components/celpip/CelpipLanding.tsx | grep -qvx 0</automated>
    <automated>grep -c "larger screen" src/components/celpip/CelpipLanding.tsx | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - CelpipLanding renders TaskCard grids for the active tab with status computed from useCelpipProgress.
    - Zero attempts shows the "No attempts yet" empty state with its body copy; counts read "1 attempt" / "{n} attempts".
    - Attempt history is ordered most-recent-first with date, duration, and word count per row.
    - The dismissible mobile desktop-recommendation notice is present.
    - `npm run build` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The landing body shows tasks with live status, a proper attempt history with empty state, and the mobile notice.</done>
</task>

<task type="auto">
  <name>Task 3: /celpip landing route — metadata, JsonLd, compact hero</name>
  <files>src/app/(catalog)/celpip/page.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/page.md (Next.js 16 page + metadata conventions)
    - src/app/(catalog)/skill/[skill]/page.tsx (generateMetadata canonical + openGraph + icon-badge header pattern)
    - src/app/(catalog)/layout.tsx (the public CatalogLayout this route sits under)
    - src/components/JsonLd.tsx and src/lib/site.ts (JsonLd + absoluteUrl helpers)
    - src/components/celpip/CelpipLanding.tsx (the client body to render)
  </read_first>
  <action>
    Replace the minimal plan-01 landing stub in `src/app/(catalog)/celpip/page.tsx` with the real server component: export `generateMetadata` returning title/description with `alternates.canonical` "/celpip" and matching openGraph (type "website"), mirroring the skill page. Render a JsonLd LearningResource for the CELPIP Writing section with isAccessibleForFree true and absoluteUrl("/celpip"). Render a COMPACT hero (icon badge using the color-mix var(--sky) pattern + a short original explanation of what CELPIP Writing practice is) sized so the task cards sit above the fold, then render `<CelpipLanding />`. Keep the route under the (catalog) group so it inherits the public AppShell — do not add auth gating (the section is free for all users).
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "canonical" src/app/(catalog)/celpip/page.tsx | grep -qvx 0</automated>
  </verify>
  <acceptance_criteria>
    - `/celpip` exports generateMetadata with canonical "/celpip" and openGraph, and renders JsonLd with isAccessibleForFree true.
    - The hero is compact and the page renders CelpipLanding; no auth gate is applied.
    - `npm run build` statically generates `/celpip`; `npm run lint` exits 0.
  </acceptance_criteria>
  <done>The free /celpip landing is complete — metadata, SEO structured data, compact hero, and the interactive body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (fluentpath.celpip.v1) → landing UI | Client-owned attempt data drives status badges and history rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Information Disclosure | Attempt history rendering stored text/metadata | medium | mitigate | History rows render date/duration/word-count and titles through JSX interpolation only; no raw-HTML injection API |
| T-01-06 | Tampering | Reading attempt data for status/history | low | mitigate | Store already falls back to CELPIP_EMPTY on corrupt data (plan 01); landing tolerates empty/partial state via the ready guard |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — no new libraries |
</threat_model>

<verification>
- `npm run build` statically generates `/celpip`; `npm run lint` exits 0.
- Empty-state, mobile-notice, and canonical metadata strings present (grep). Interactive status/history behavior is validated in the human checkpoint (plan 01-06).
</verification>

<success_criteria>
The free /celpip landing renders the compact hero, Task 1/Task 2 libraries with live attempt status, disabled coming-soon tabs, attempt history with a correct empty state and counts, the mobile notice, and proper SEO metadata.
</success_criteria>

## Artifacts this phase produces
This plan produces: the full `/celpip` route (`src/app/(catalog)/celpip/page.tsx`), `TaskCard.tsx`, `CelpipTabs.tsx`, `CelpipLanding.tsx` (all under src/components/celpip/). It consumes `getTasksByType`, `CELPIP_TASK_META`, and `useCelpipProgress` from plan 01. See plan 01-01 for the full phase artifact roster and the `fluentpath.celpip.v1` storage key.

<output>
Create `.planning/phases/01-celpip-writing-practice/01-05-SUMMARY.md` when done
</output>
