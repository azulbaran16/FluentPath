---
phase: 01-celpip-writing-practice
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/celpip/tasks-survey.ts
autonomous: true
requirements: [CELPIP-01]
must_haves:
  truths:
    - "The Task 2 library offers ~8 original survey/opinion tasks with everyday-Canadian themes."
    - "Every survey task has a scenario, two options (A/B) to choose between, a 26-minute limit, a 150–200 word range, an original model answer, and rewritten strategy tips."
    - "No text is copied from the third-party study material or official CELPIP PDFs — every model answer and tip is original."
  artifacts:
    - path: src/lib/celpip/tasks-survey.ts
      provides: "SURVEY_TASKS — ~8 original CelpipWritingTask entries (taskType survey)"
  key_links:
    - "SURVEY_TASKS ids must be unique across EMAIL_TASKS and SURVEY_TASKS — getTask and generateStaticParams key on id."
---

<objective>
Fill the Task 2 (survey / opinion response) task bank to ~8 original entries so learners can practice the second CELPIP Writing task under real conditions. Pure content authoring against the CelpipWritingTask type.

Purpose: deliver CELPIP-01's Task 2 half — original survey prompts + model answers + strategy tips.
Output: `src/lib/celpip/tasks-survey.ts` with ~8 tasks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-celpip-writing-practice/01-CONTEXT.md
@.planning/phases/01-celpip-writing-practice/01-UI-SPEC.md
@src/lib/celpip.ts
@src/lib/celpip/tasks-survey.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 community & lifestyle survey tasks</name>
  <files>src/lib/celpip/tasks-survey.ts</files>
  <read_first>
    - src/lib/celpip.ts (the CelpipWritingTask type — note `options` is a two-element tuple for surveys)
    - src/lib/celpip/tasks-survey.ts (the seed entry from plan 01 — match its style and id convention)
    - src/lib/content/writing.ts (original model-answer tone precedent)
    - .planning/phases/01-celpip-writing-practice/01-CONTEXT.md (Task 2 format reference + IP constraints)
  </read_first>
  <action>
    Extend `SURVEY_TASKS` (keeping the plan-01 seed) with 4 additional original Task 2 survey tasks, ids "survey-02" … "survey-05". Each presents a community/lifestyle question with two clear options to pick between. Themes: public spending priority (park vs library), transit vs road funding, remote vs in-office work, a community facility choice. Each entry: taskType "survey"; a scenario paragraph framing the survey; `options` as a two-element tuple [Option A, Option B]; timeLimitMinutes 26; wordRange { min: 150, max: 200 }; an original 150–200 word modelAnswer that picks one option and argues it (intro paraphrase + thesis, two body paragraphs idea→explain→example, complex-sentence conclusion) — write it yourself, copy nothing; 2–3 original strategyTips. Everyday-Canadian register.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -rniE "IELTS.?.?PTE.?with.?Viv" src/lib/celpip | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - After this task `SURVEY_TASKS` has ≥5 entries (seed + 4), each with taskType "survey", a two-element options tuple, timeLimitMinutes 26, wordRange { min: 150, max: 200 }.
    - Every modelAnswer word count is within 150–200 words and commits to one option.
    - All ids unique within the file; `npm run build` exits 0.
    - No third-party academy name under src/lib/celpip (grep count 0).
  </acceptance_criteria>
  <done>Four original community/lifestyle survey tasks exist alongside the seed, correctly typed and building clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author 3–4 work & services survey tasks (reach ~8 total)</name>
  <files>src/lib/celpip/tasks-survey.ts</files>
  <read_first>
    - src/lib/celpip.ts (CelpipWritingTask type)
    - src/lib/celpip/tasks-survey.ts (entries from Task 1 — continue style and id sequence)
    - .planning/phases/01-celpip-writing-practice/01-CONTEXT.md (IP constraints — hard)
  </read_first>
  <action>
    Add 3–4 more original Task 2 survey tasks (ids "survey-06" … "survey-08"/"survey-09") to reach ~8 total in `SURVEY_TASKS`. Themes: workplace policy choice (four-day week vs flexible hours), a services trade-off (paperless vs paper billing, self-checkout vs staffed), an education/training choice, a technology-adoption question. Same field contract: two-element options tuple, timeLimitMinutes 26, wordRange 150–200, original 150–200 word modelAnswer arguing one side, original strategyTips.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>grep -rniE "IELTS.?.?PTE.?with.?Viv" src/lib/celpip | wc -l | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - `SURVEY_TASKS` totals 7–9 entries (target ~8), all taskType "survey", each with a two-element options tuple / 26 min / 150–200 range.
    - Every modelAnswer is 150–200 words, original, and argues one option.
    - All ids unique across the file; `npm run build` exits 0.
    - No third-party academy name under src/lib/celpip (grep count 0).
  </acceptance_criteria>
  <done>The Task 2 survey bank holds ~8 original tasks; combined with plan 02 the app exposes ~16 tasks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored content → app bundle | Original static content; primary risk is IP contamination, not runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-IP | Repudiation/Compliance | SURVEY_TASKS model answers & tips | high | mitigate | All content original from exam FORMAT only; automated grep blocks any third-party academy name; reviewer confirms no copied text |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs this phase — no new libraries |
</threat_model>

<verification>
- `npm run build` exits 0 with the enlarged bank.
- IP grep over src/lib/celpip returns 0 hits for the third-party academy name.
</verification>

<success_criteria>
~8 original Task 2 survey tasks exist in SURVEY_TASKS, fully typed, exam-faithful, and free of any third-party text.
</success_criteria>

## Artifacts this phase produces
This plan produces: `SURVEY_TASKS` (~8 entries) in `src/lib/celpip/tasks-survey.ts`. It consumes the phase types (`CelpipWritingTask`) from `src/lib/celpip.ts` created by plan 01. See plan 01-01 for the full phase artifact roster.

<output>
Create `.planning/phases/01-celpip-writing-practice/01-03-SUMMARY.md` when done
</output>
