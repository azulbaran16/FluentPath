---
phase: 02-server-side-progress
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/progress-merge.ts
  - scripts/verify-merge.mts
autonomous: true
requirements: [PROG-02, PROG-05]

must_haves:
  truths:
    - "A learner with a 30-day streak abandoned in January and a fresh 1-day streak today ends up with the fresh streak, not a fabricated 30."
    - "Three states from three days merge to the same streak whichever order they are merged in — two devices plus the server cannot disagree about the number."
    - "A vocabulary card un-marked on one device does not reappear after the next reconcile, including when both sides share the same calendar day."
    - "Today's XP ring never shows yesterday's total after a reconcile."
    - "An SRS item merged across devices keeps the box and due date that were computed together — never a box from one side with a due date from the other."
    - "A question the learner just got wrong is not marked resolved by a merge with an older attempt record."
  artifacts:
    - path: "src/lib/progress-merge.ts"
      provides: "The complete D-01a per-field rule set, including the tuple and whole-field carve-outs"
      exports: ["mergeProgress", "progressEqual"]
    - path: "scripts/verify-merge.mts"
      provides: "Assertions for every hazard case D-01a was written to prevent"
  key_links:
    - from: "scripts/verify-merge.mts"
      to: "src/lib/progress-merge.ts"
      via: "direct node import with an explicit .ts extension — the module stays React-free and alias-free so it can be exercised without a test runner"
      pattern: "progress-merge"
---

<objective>
Complete `mergeProgress` to the full D-01a per-field rule table — the streak/`lastActive` pair, the `todayXp`/`xpDay` tuple, the `srs` and `attempts` per-entry rules, and the `goalXp` carve-out — and prove each hazard case by command.

D-01a is authoritative and was written by the user *after* research specifically because a blanket union/max is wrong for `streak`, `vocab`, drafts and `goalXp`; D-01c amends it after plan review to delete the adjacent-day streak case. This plan transcribes that table; it does not re-derive it and it adds no case of its own.

Purpose: 02-01 proved the architecture with the append-only rules and a coarse "later activity wins" for everything else. That is safe but not yet correct: a naive pairing fabricates streaks that then *stick*, because `recordActivity` recomputes the next streak from `lastActive`.
Output: a complete, still-total, still-commutative, still-idempotent merge, plus assertions covering every case D-01a exists to prevent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-server-side-progress/02-CONTEXT.md
@.planning/phases/02-server-side-progress/02-RESEARCH.md
@src/lib/progress.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The streak pair, the daily-XP tuple, and the daily-goal carve-out</name>
  <reversibility rating="costly">Implements D-01a as amended by D-01c: this rule set becomes the merge contract for all stored learner data, and reverting it means re-deriving which side each field came from (rating carried from 02-CONTEXT.md).</reversibility>
  <files>src/lib/progress-merge.ts, scripts/verify-merge.mts</files>
  <read_first>
    - .planning/phases/02-server-side-progress/02-CONTEXT.md (D-01a the authoritative per-field table, D-01b the sub-day instant, D-01c the amendment deleting the adjacent-day streak case — read all three before touching the streak rule)
    - src/lib/progress.ts lines 180-188 and 196-217 (recordActivity and complete — how streak is recomputed from lastActive, which is why a fabricated streak sticks)
    - src/lib/progress.ts lines 240-252 and 292-306 (setGoalXp and markVocab — note that setGoalXp writes the value alone and never touches lastActive, which is the source of the goalXp tie asymmetry documented below; markVocab is the only vocab delete site)
    - src/lib/progress-merge.ts (the module from 02-01)
  </read_first>
  <behavior>
    - Equal `lastActive` on both sides: the pair keeps that day and the larger streak.
    - Different `lastActive`, by any distance: the streak and the day are taken together from the side with the later `lastActive`. There is no special case for adjacent days.
    - Order independence: with `a = (Jan 10, streak 30)`, `b = (Jan 11, streak 1)` and `c = (Jan 12, streak 1)`, both `(a ⊔ b) ⊔ c` and `a ⊔ (b ⊔ c)` give `(Jan 12, streak 1)`.
    - `todayXp` and `xpDay` move as a pair: later `xpDay` wins both, equal `xpDay` takes the larger `todayXp`, a null `xpDay` loses.
    - `goalXp` comes from the side with the later `lastActive`; on a tie the lower value wins, so a deliberately lowered daily goal is never ratcheted back up.
    - `vocab` is taken as a whole map from the side with the later D-01b instant, so an un-marked card does not come back even when both sides share a calendar day.
    - All four properties from 02-01 (idempotence, commutativity, associativity, totality) still hold after these rules land.
  </behavior>
  <action>
    In `src/lib/progress-merge.ts`, replace the coarse whole-field handling from 02-01 with the exact rules above, implemented as small named helpers (`mergeStreakPair`, `mergeDailyXp`, `pickGoalXp`) so each rule is readable next to the D-01a row it encodes; `pickVocab` already landed in 02-01 on the D-01b instant and needs no change here beyond the assertions below. Compare day strings lexically with the existing `laterDay` helper. Every helper stays value-only: none may know which argument came from the network. Keep each one total, returning a sane pair when either side is null, missing or unparseable.

    `mergeStreakPair` implements D-01c and nothing more: later `lastActive` takes both the day and its own streak, equal days take that day and the larger streak. Do not add a case for days that happen to be adjacent, and do not compute a day distance at all — that means the merge module still needs no value import from the schema module, which is the property that keeps it loadable standalone under node. Record in a comment why the adjacent-day case was considered and rejected: it is not associative, so three states merged in different orders produce different streaks, and taking the later day together with the larger streak is precisely the `max(lastActive)` + `max(streak)` shape D-01a forbids. Record the accepted cost too — practising on two devices on consecutive days may not compound the streak, which the user chose over order-dependence.

    Extend the block comment at the top of the module to state, per field group, what the merge guarantees and what it deliberately gives up. Include three give-ups by name. First, `max` on `xp` means offline XP earned on two devices does not sum, which is unavoidable because `addSkillXp` keeps no per-event record to reconstruct from, and summing would break the idempotence D-02 requires. Second, the streak give-up above. Third, `goalXp` is asymmetric on a same-day tie: it is the only field whose write does not touch `lastActive` at all (`setGoalXp` at `progress.ts:248` sets the value alone), so two devices that both changed the goal today are indistinguishable and the rule keeps the lower number. A learner who raises the goal twice in one day on two devices therefore keeps the lower value. That is deliberate and is the safe direction — a goal silently raised is a goal the learner cannot meet — and it is written down here so a later reader does not file it as a defect.

    Extend `scripts/verify-merge.mts` with a fixture set covering each behaviour, including the exact fabrication case: one side holding a long streak with an old `lastActive`, the other a one-day streak with today's date, asserting the result carries the fresh pair. Add the three-state order-independence case with the literal fixtures `(Jan 10, 30)`, `(Jan 11, 1)` and `(Jan 12, 1)`, asserting both bracketings give `(Jan 12, 1)` — that is the case the rejected adjacent-day rule failed, so the assertion is what stops it being reintroduced. Add a case where a vocabulary key present on the earlier-instant side and absent from the later-instant side stays absent after the merge while both sides carry the same `lastActive` day, and one where a lowered `goalXp` on the newer side survives a merge with a higher value on the older side. Re-run the algebraic assertion groups from 02-01 after the new rules so any regression in idempotence, commutativity or associativity surfaces immediately.
  </action>
  <!-- planner-discipline-allow: daysBetween -->
  <!-- The == 0 gate below is the mechanical proof that D-01c's deletion stuck: the rejected
       adjacent-day rule cannot be written without a day-distance calculation, and the module
       has no other reason to import one. The comment filter keeps an explanatory comment in
       the source from invalidating it. -->
  <verify>
    <automated>node --experimental-strip-types scripts/verify-merge.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>grep -v '^\s*//' src/lib/progress-merge.ts | grep -c 'daysBetween' | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The merge module performs no day-distance calculation at all (grep count 0 outside comments), which is what makes the rejected adjacent-day rule unwritable.
    - The verify script exits 0 with the streak-fabrication, three-state order-independence, same-day vocab-non-resurrection, lowered-goal and daily-XP-tuple cases all asserted.
    - The streak rule has exactly two branches — later day wins the pair, equal day takes the larger streak — with no distance calculation anywhere in the module.
    - The idempotence, commutativity, associativity and totality groups from 02-01 still pass unchanged.
    - `npx tsc --noEmit` and `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Streaks, daily XP, vocabulary and the daily goal merge exactly as D-01a and D-01c specify, with each hazard case proven by an assertion and each accepted cost written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-entry rules for the SRS queue and the attempt stats</name>
  <files>src/lib/progress-merge.ts, scripts/verify-merge.mts</files>
  <read_first>
    - src/lib/progress.ts lines 254-290 (recordAttempt — the only writer of srs, which writes srs[id] and attempts[id] in the same update; reviewItem is a thin wrapper)
    - src/lib/progress.ts lines 14-31 (BOX_DAYS, SrsItem and AttemptStat shapes)
    - .planning/phases/02-server-side-progress/02-CONTEXT.md (D-01a row 1 — srs and attempts are append-only and union-safe; the per-entry rule below decides only which side's entry wins)
  </read_first>
  <behavior>
    - `attempts[id]`: `tries` and `wrong` take the larger of the two, preserving the invariant that wrong never exceeds tries; `updatedAt` takes the later day; `resolved`, `lastWrongOption`, `topic` and `level` come as a group from the side with the later `updatedAt`, so a still-open mistake is never flipped to resolved by an older record.
    - `attempts[id]` on a same-day tie: the side with more `tries` wins the point-in-time group; a further tie is broken on the lexicographically greater `topic`.
    - `srs[id]`: the whole `{box, due}` entry is taken from the side whose paired `attempts[id].updatedAt` is later — box and due are computed together at write time and must never be mixed across sides.
    - `srs[id]` with no paired stat or a same-day tie: the entry with the earlier `due` wins; a further tie takes the lower `box`. Both tiebreaks fail safe for learning — one extra review costs less than hiding an item the learner just got wrong for up to thirty days.
    - Both merges read the same *unmerged* input pair: merging `srs` against an already-merged `attempts` would make the `updatedAt` comparison meaningless.
  </behavior>
  <action>
    Add `mergeAttemptStat` and `mergeSrsItem` to `src/lib/progress-merge.ts` and wire them into `mergeProgress` through the existing `unionRecord` primitive. Compute the `attempts` union and the `srs` union from the same original pair of input states and pass the two original `attempts` records into the `srs` picker explicitly, so the ordering hazard cannot be introduced by a later refactor — say so in a comment on the call site. Keep both pickers value-only and total: a missing or malformed entry on one side yields the other side's entry unchanged, and a malformed entry on both sides is dropped rather than fabricated.

    Extend `scripts/verify-merge.mts` with cases asserting: an open mistake stays open when merged with an older resolved record and vice versa; `tries` and `wrong` both take the maximum and the invariant still holds; an SRS entry follows its paired attempt timestamp as a unit; the no-paired-stat fallback selects the earlier due date and then the lower box; and merging the same pair twice yields an identical result so the union additions did not break idempotence.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-merge.mts</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The verify script exits 0 with the attempt-stat and SRS-entry cases asserted alongside every earlier group.
    - `mergeProgress` still passes idempotence, commutativity, associativity and totality.
    - `npx tsc --noEmit`, `npm run lint` and `npm run build` exit 0.
  </acceptance_criteria>
  <done>The review queue and the mistake notebook survive a cross-device merge with their entries intact and their sub-fields never mixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored progress ↔ incoming snapshot | Two independently-authored states meet inside `mergeProgress`, on both the client and the server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | `streak` / `lastActive` merge | high | mitigate | The pair moves as a unit from the later day with no adjacency case, so no merge can credit a run the learner never had, the fabricated value cannot stick through `recordActivity`, and three states converge to the same streak in any order (D-01c) |
| T-02-06 | Tampering | `vocab` merge | medium | mitigate | Whole-field selection by the D-01b millisecond instant, never key union and never map size, so the single delete site at progress.ts:298 is not silently undone by a same-day reconcile |
| T-02-07 | Tampering | `attempts[].resolved` merge | medium | mitigate | Point-in-time fields travel as a group from the later `updatedAt` side and are never OR'd, so an open mistake cannot be marked resolved by a stale record |
| T-02-08 | Tampering | `srs[]` box/due merge | medium | mitigate | The entry moves as a unit; the fallback tiebreak biases toward reviewing sooner, so the worst case is one extra review rather than a hidden failed item |
| T-02-09 | Denial of Service | Write amplification from a non-convergent merge | low | mitigate | Every rule is value-only and asserted commutative, so client and server converge to the same value and the reconcile stops writing |
| T-02-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-merge.mts` exits 0 with every D-01a hazard case asserted.
- `npx tsc --noEmit`, `npm run lint` and `npm run build` exit 0.
</verification>

<success_criteria>
`mergeProgress` implements the D-01a table in full, remains a total idempotent commutative join, and each rule that exists to prevent a specific defect has an assertion naming that defect.
</success_criteria>

<output>
Create `.planning/phases/02-server-side-progress/02-02-SUMMARY.md` when done
</output>
