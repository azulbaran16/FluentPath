---
phase: 04-native-level-depth
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - src/lib/content/scenario-vocabulary.ts
  - src/lib/content/scenario-lessons.ts
  - src/lib/curriculum.ts
  - scripts/verify-scenario-content.mts
  - scripts/fixtures/scheduled-item-ids.json
autonomous: false
requirements: [CONT-04]

must_haves:
  truths:
    - "native/idioms' recall deck holds 24 cards selected on the same criteria as its warm-up, so the scenario reads as one bank rather than as a good half and a textbook half (D-04)."
    - "The scenario stops saying the same thing on three surfaces: its briefing no longer worked-examples an expression its own phrase bank teaches."
    - "Every scenario in the app resolves to a briefing written for it, and that is asserted — so the generic fallback underneath is provably unreachable rather than merely unused."
    - "The scenario advertises a sitting it can keep at 18 phrases and 42 cards."
  artifacts:
    - src/lib/content/scenario-vocabulary.ts
    - src/lib/content/scenario-lessons.ts
  key_links:
    - "scenario-lessons.ts native/idioms -> phrases.ts native/idioms: the briefing's worked examples and the phrase bank must stop being the same two expressions"
    - "getScenarioLesson FALLBACK -> the harness: dead today, and asserted dead rather than trusted dead"
---

<objective>
The other half of the re-selection. 04-03 fixed the warm-up; this fixes the deck behind it and the
briefing above it, so the scenario stops making the same point on three surfaces at once.

Research measured the triple: the briefing re-uses two of the six phrases as its worked examples, so
one canonical list appears in the briefing, in the warm-up and in the deck of a single C1 scenario.
The warm-up is already fixed. A deck still drawn from the same rarest-in-English class would leave
the scenario visibly inconsistent with itself, and a briefing still quoting a retired expression
would be teaching material the app no longer holds.

**If execution stops right after this plan**, `native/idioms` is finished and is the phase's proof
that the quality bar is reachable: 18 phrases and 24 cards chosen on frequency, register and sense,
a briefing that agrees with them, and an honest advertised sitting. Two scenarios of five are done.

Purpose: finish the re-selection, and stop three surfaces from agreeing.
Output: a 24-card deck, a rewritten briefing, and an assertion that no scenario is served a generic one.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-native-level-depth/04-CONTEXT.md
@.planning/phases/04-native-level-depth/04-RESEARCH.md
@.planning/phases/04-native-level-depth/04-03-SUMMARY.md
@AGENTS.md

@src/lib/content/scenario-vocabulary.ts
@src/lib/content/scenario-lessons.ts
@src/lib/content/phrases.ts
@src/lib/content/scenario-reading.ts
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: confirm the vocabulary half of the retirement list, now that its replacement is visible</name>
  <decision>
    Whether the vocabulary retirements approved at 04-03 still stand, unchanged, before any card is
    deleted.
  </decision>
  <context>
This is a confirmation, not a re-litigation, and it exists because the approval at 04-03 was given
before a single replacement had been written. The eighteen phrases now exist and can be read, so the
question — is the new material worth losing a learner's schedule on the old — can be answered against
something real rather than against a promise.

Present three things and nothing more: the vocabulary ids approved for retirement at 04-03 with the
reasons recorded then; the eighteen phrases as they now read, so the standard of the replacement is
visible; and the two facts that make this irreversible — deleting is safe for the store and lossy for
the learner, and re-pointing an id onto replacement content is never an option, so a schedule lost
here cannot be handed to anything else.

If the answer amends the list, apply the amended list and nothing else. If it stands, proceed exactly
as approved.
  </context>
  <options>
    <option id="stands">
      <name>The list stands as approved at 04-03</name>
      <pros>No further deliberation, and the deck is re-selected on the same criteria as the warm-up above it</pros>
      <cons>Any learner who studied a retired card loses her schedule on it, permanently</cons>
    </option>
    <option id="amend">
      <name>Amend it — name the cards to keep and the cards to retire</name>
      <pros>The judgement is made with the replacement standard actually in view rather than promised</pros>
      <cons>Needs the eighteen phrases read first</cons>
    </option>
  </options>
  <resume-signal>Reply "stands", or "amend" followed by the ids to keep and the ids to retire</resume-signal>
</task>

<task type="auto">
  <name>Task 2: the recall deck re-selected to 24</name>
  <files>src/lib/content/scenario-vocabulary.ts, src/lib/curriculum.ts, scripts/fixtures/scheduled-item-ids.json</files>
  <reversibility rating="one-way">Any deletion here orphans a learner's stored schedule under that key permanently. The list was approved at 04-03 Task 1 and confirmed against the finished replacement at Task 1 above; this task applies it and invents no deletion of its own.</reversibility>
  <read_first>
    04-03's summary — the approved retirement decision verbatim, and the eighteen phrases now in the
    warm-up, so nothing here restates one. RESEARCH §1's six criteria. The existing deck comment in
    `scenario-vocabulary.ts`, which states a rule ("the six phrases are the six best-known idioms;
    these are eight more") that this change supersedes.
  </read_first>
  <action>
Apply the vocabulary half of the retirement list approved at 04-03 Task 1 — exactly that, inventing
no deletion of your own — and record each deleted id in the id-stability fixture's retired list with
its reason. Then bring the `native/idioms` deck to twenty-four cards.

The additions are authored against the same criteria the warm-up was, because a deck drawn from the
old criteria under a warm-up drawn from the new ones is the inconsistency this plan exists to remove:

- Frequency-attested rather than dictionary-attested: expressions a native would produce this month.
- Non-transparent but decomposable — the class that is both frequent and teachable — rather than the
  fully opaque set pieces this bank is being rescued from.
- Sense-specific. `ScenarioVocabCard` is `{id, term, es, example}` and stays exactly that, so the
  sense lives in the `term` and in an `example` that could not be reworded to carry a different one.
- Chosen because a C1 learner gets that particular thing wrong. For this scenario that is
  mis-placement and fixedness: deploying an expression where a plain sentence belongs, and varying a
  form that does not vary.

Constraints, all hard:

- Nothing here repeats any of the eighteen phrases from 04-03, and nothing repeats the briefing's
  worked examples as they will read after Task 2 — coordinate the two, since they are one commit
  apart and the whole point is that the three surfaces stop agreeing.
- Nothing here names an expression the scenario's reading passage deliberately withholds. 04-03's
  appended assertion covers the vocabulary bank as well as the phrase bank, so this will fail loudly
  if it happens, but it is cheaper to read the passage's header first.
- The `example` is the largest prose surface in this bank and the first place authoring fatigue
  shows. Twenty-four examples in one sitting is where item twenty is item eight with the nouns
  changed; write them, then read them consecutively and rewrite anything that shares a frame.
- New authored slugs only. A surviving id keeps its content byte for byte, in every field — the
  fixture's hash covers the whole authored record, so rewriting a surviving card's `example` under
  its existing id is a re-point and fails, exactly as rewriting its `term` would.

**Regenerate the id fixture in this same commit — both halves.** The approved retirements go into the
retired list with their reasons; the twenty-four cards go in through
`node --experimental-strip-types scripts/verify-id-stability.mts --update`, because an id in the tree
and not in the fixture fails as an unrecorded addition. Read the diff before committing it: additions,
declared retirements, and **no changed hash on a surviving id** — nothing else is legitimate.

Rewrite the deck's header comment so it describes the deck that is now there, including why the
selection criterion changed, and delete the sentence that defers this world's depth to a later phase
— that phase is this one.

Then set `minutes` for `native/idioms` to the smallest whole minute the session-length invariant
allows at 18 warm-up phrases and a 42-card deck.

Before committing, run a corpus-wide exact and Jaccard scan of this deck's terms and examples
against all 35 scenarios, and record the highest pair.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>node --experimental-strip-types scripts/verify-id-stability.mts</automated>
    <automated>BEFORE=$(git hash-object scripts/fixtures/scheduled-item-ids.json); node --experimental-strip-types scripts/verify-id-stability.mts --update; AFTER=$(git hash-object scripts/fixtures/scheduled-item-ids.json); test "$BEFORE" = "$AFTER" || { echo 'FAIL: update mode changed the fixture, so it was NOT regenerated alongside the content in this change'; exit 1; }</automated>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint</automated>
  </verify>
  <done>
    `native/idioms` resolves to 24 cards. Exactly the vocabulary half of the approved list was
    deleted, each with a recorded reason, and the id-stability gate confirms no retired id is still
    emitted and no surviving id changed content in any field. The fixture was regenerated in this
    same commit, so all twenty-four ids are inside the gate, and its diff is additions plus declared
    retirements and nothing else. Nothing in the deck repeats a phrase or names an
    expression the passage withholds. The header comment describes the deck that is there.
    `curriculum.ts` advertises a sitting the invariant allows. Harness, `tsc` and `lint` all pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: the briefing stops teaching from its own phrase bank, and no scenario is served a generic one</name>
  <files>src/lib/content/scenario-lessons.ts, scripts/verify-scenario-content.mts</files>
  <read_first>
    `scenario-lessons.ts` in full, including `getScenarioLesson` and the generic record underneath it.
    RESEARCH §1's paragraph on the three surfaces. 03-11's argument for deleting a fallback rather
    than leaving it unreachable, and its rule that the replacement is an assertion rather than a
    comment.
  </read_first>
  <action>
**A. Rewrite the `native/idioms` briefing.** Its intro and its three tips currently demonstrate the
teaching point on two expressions the phrase bank also teaches, which is how one canonical list came
to appear on three surfaces of one scenario. The rewrite:

- keeps the intro's actual advice — learn a few well rather than many badly — because that advice is
  correct and is what stops eighteen items becoming a list to be recited; state it in a way that is
  consistent with a bank of eighteen rather than in tension with it;
- demonstrates its points on expressions that appear in NO other surface of this scenario: not in the
  phrase bank, not in the deck, not in the rehearsal, and not among the expressions the reading
  passage asks the reader to recover;
- says the thing the new selection makes teachable and the old one did not — that a native
  expression carries a restriction on who may say it to whom, and that the cost of getting that wrong
  is not being misunderstood but being misplaced.

**B. Assert that no scenario is served a generic briefing.** The module resolves an unknown key to a
single generic record. All 35 scenarios have their own briefing today, so that record is dead — the
same situation `phrases.ts` was in before 03-11, and the same trap: the next author to add a scenario
gets filler that reads exactly like curated content on the page.

Assert it dead rather than delete it. Append a group asserting that every scenario in the curriculum
resolves to a briefing of its own — with a non-blank intro and at least two non-blank tips — and that
no scenario's briefing is byte-identical to the generic one. Deleting the record itself would change
the accessor's return type and every call site, which is real churn in files this plan does not own
and this requirement does not need; the assertion buys the whole safety property at none of it.
Record the deletion as a question for the phase gate to file, not for this plan to take.

**C. Assert the three surfaces stay apart.** For `native/idioms`, assert that no briefing tip
contains any phrase text or any vocabulary term from the scenario's own banks. Measure it clean
before writing it; if it does not come back clean, the briefing is what moves.
  </action>
  <verify>
    <automated>node --experimental-strip-types scripts/verify-scenario-content.mts</automated>
    <automated>npx tsc --noEmit &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>
    The `native/idioms` briefing demonstrates its points on material no other surface of the scenario
    uses, and says what register costs. The harness asserts every one of the 35 scenarios resolves to
    its own non-blank briefing distinct from the generic record, and asserts that this scenario's
    briefing quotes neither its phrases nor its cards. The assertion count is strictly above 04-03's;
    `tsc`, `lint` and `build` exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retirement list → learner's stored `srs` | The vocabulary half of an already-approved list; each deletion orphans a live key |
| briefing → the other three surfaces of the same scenario | The briefing is prose nothing gates today, so a repeat there is invisible to every existing check |
| unknown scenario key → generic briefing | A future scenario without its own briefing is served filler that reads as curated on the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | `native/idioms` vocabulary slugs | critical | mitigate | Only the already-approved deletions are applied, each recorded with a reason; `verify-id-stability.mts` fails on a changed hash, an undeclared deletion, or a retired id coming back |
| T-04-09 | Repudiation | `scenario-lessons.ts` generic fallback | medium | mitigate | Task 3 asserts all 35 scenarios resolve to their own non-blank briefing, distinct from the generic record, making it provably unreachable; the deletion itself is filed rather than taken here |
| T-04-10 | Repudiation | the same material on three surfaces | medium | mitigate | Task 3 asserts no briefing tip quotes this scenario's phrases or cards, measured clean before the assertion lands |
| T-04-02 | Denial of Service | progress body cap | high | mitigate | 04-01's committed payload assertion runs on every harness invocation |
| T-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero packages installed; dependency count stays 11 + 11 |
</threat_model>

<verification>
- `node --experimental-strip-types scripts/verify-scenario-content.mts` — exits 0, count above 04-03's
- `node --experimental-strip-types scripts/verify-id-stability.mts` — exits 0
- `npx tsc --noEmit`, `npm run lint`, `npm run build` — all exit 0
</verification>

<success_criteria>
1. `native/idioms` holds 18 phrases and 24 cards, all selected on the same criteria.
2. Its briefing, warm-up, deck and passage teach four disjoint sets of material.
3. Only the human-approved ids were retired, each auditable with a reason.
4. Every scenario in the app is asserted to have a briefing of its own.
5. The scenario advertises a sitting it can keep.
</success_criteria>

<output>
Create `.planning/phases/04-native-level-depth/04-04-SUMMARY.md` when done. Record: the ids retired
in this commit with their reasons, the new assertion count, the highest exact/Jaccard pair from the
corpus-wide scan, and the generic-briefing deletion as an explicit open question for the phase gate.
</output>
